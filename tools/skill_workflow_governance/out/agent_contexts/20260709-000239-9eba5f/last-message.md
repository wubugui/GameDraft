当前不要看原始列表，按治理包推进：
- P0 断链修复包：14 项，交给 agent 执行：能确定的路径直接修；明显过期的引用删；无法判断的只列成确认清单。
- P1 规则漂移复核包：59 项，交给 agent 对比规则和当前实现：过时就改规则，仍然正确就标记已验证。
- P1 Skill 触发条件补齐包：1 项，交给 agent 批量补 when-to-use / when-not-to-use，不改变 skill 正文语义。
- P2 生命周期元数据补齐包：12 项，交给 agent 批量插入统一生命周期块，默认 owner 为 shared，验证日期用本次审计日期。

建议先引用 P0/P1 治理包问我，我会继续拆成 agent 可执行任务。