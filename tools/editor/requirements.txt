# 主编辑器 + 内嵌对话图（与 tools/dialogue_graph_editor/requirements.txt 一致）
# 使用 Python 3.11：OdenGraphQt 0.7.4 依赖 stdlib distutils，CPython 3.12+ 已移除
PySide6>=6.5.0
OdenGraphQt>=0.7.4
qtpy>=2.3.1
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
PyOpenGL>=3.1.7
pyopengltk>=0.0.4
grandalf>=0.8  # 对话图自动布局 Sugiyama 分层算法
# 启动游戏前的端口冲突预检(tools/dev/game.py describe_port_occupants):占用进程的
# 命令行/用户/父进程链靠它取；缺了会降级为"只有 PID+进程名"，弹窗仍可杀可取消。
psutil>=5.9
# tools/animation_pipeline 的 bake_normal_atlas / form_align / matte_illustration 是**硬
# import**（不是可选降级），而编辑器测试 test_anim_normal_bake_config.py 直接 import 它。
# 此前任何 requirements 都没声明 scipy：装了才绿、没装就是一条 collect ERROR，
# 看起来像"测试坏了"而不是"环境缺件"。
scipy>=1.11

# 测试（根 pytest.ini 的 addopts 默认带 -n auto --dist loadfile，缺 xdist 会全线报
# "unrecognized arguments"，所以它是硬依赖不是可选项）
pytest>=8
pytest-xdist>=3.6
# 静态护栏 test_no_undefined_names.py 用（缺它该护栏自动 skip，不会误红）：
# 拦「引用了不存在的名字」的死代码路径——踩过一个 100% 抛 NameError 的
# 「删除本条件」按钮，从抽方法起就一直是死的，全套交互测试都没抓到。
pyflakes>=3.1
