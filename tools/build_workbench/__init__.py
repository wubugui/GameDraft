"""构建管理工作台：定期自动出发行包 + 归档整理。

职责边界（与 `tools/build/` 的分工）：

* `tools/build/` 与 `scripts/*.mjs` 是**构建本身**——抽取、验收、出包。
  它们不知道"定期"这回事，每次调用都要显式传 `--out-dir`。
* 本包是**构建的调度与留档**——什么时候构建、构建到哪、旧的怎么归档。
  它是 `scripts/release.mjs` 的调用方之一（另一个是主编辑器的构建按钮）。

只做发行档（ship）。dev 档是开发自用的，走主编辑器手动构建。
"""
