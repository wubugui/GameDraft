# 验证记录 · 2026-09-09 复核

## 撤回此前的 3D 验收结论

此前测试只验证 canvas、mesh、点标记存在，截图没有逐项检查朝向。这不足以证明坐标正确；此前将画面问题解释为源深度拉伸也不充分。旧实现把左手 M-world 直接放入右手相机，且初始机位在场景背面。本记录取代此前“3D 画面通过”的结论。

## 本轮实测

| 项目 | 结果与边界 |
|---|---|
| TypeScript / Vite | 通过；只构建新目录。原经典脚本有不打包提示；Three 主 bundle 有体积提示 |
| Node 回归 | 10 通过：3 组俯角／尺度／work/native 比例的投影、射线、旧错机位反例；7 项原状态／保存／灯光冲突保护 |
| 原保存链集成 | 6 通过，`test_backend.py`；原场景、轨迹、声学 writer，仅 pytest 临时工程 |
| 真实场景坐标 | test_room_b、bridge_underpass、dev_teahouse_alive、mountain_pass 四场景通过：网格对运行时投影、画面右、世界 Y 上、朝画面内 |
| 网格 UV | 逐场景检查约 128 个点；允许原网格 UV 的半 work 像素中心偏移，未把该偏移误当为坐标零误差 |
| 原生 3D 操作 | 游戏机位鼠标移动 45×20 px，灯的投影跟随 45×20 px；Y 保持；右键旋转后再拖 25×12 px 同样跟随；两次拖动均一次撤销、文档精确复原 |
| 机位切换 | 俯视 +Z 向上；回游戏机位重新通过坐标检查 |
| 声学 | 新声源、旧 Geo 反射面、一阶反射抽头、路径与 IR 波形真实绘制；仅作者态，没有验成游戏内实时试听 |
| 轨迹 | 原生最高点拖动调用旧抛体操作；整条和多点变换／撤销；旧烘焙预览；原生播放时间递进；播放前后作者文档不变 |
| 桌面策略 | 两次原生自测均核实 off-the-record、NoCache、禁 LocalStorage、无需手势播放配置 |

截图已实际打开检查：游戏机位恢复原画面方向；轨迹时间轴、反射面与 IR 面板在真实窗口显示。游戏机位用原图补齐深度壳省略的断层三角形；旋转或俯视时关闭补图，没有修改原网格和烘焙数据。

## 可复查证据

本地生成产物在 Git 忽略的 `evidence/`：

- `coordinates-3d.log / coordinates-3d.png`：四场景坐标和原生拖动、自由视角、俯视、声学。
- `authoring-3d.log / authoring-3d.png`：轨迹手柄、变换、撤销和时间轴。
- `backend-recheck.log`：6 项旧 writer 集成。

原生检查只建立内存草稿，无生产保存请求；没有重启用户原先打开的窗口。窗口需要关闭后从 `start.cmd` 重新打开才能载入新前端。

## 不能从本轮测试推出的结论

- 不能推出所有旧工作台已迁移；具体缺项见 `FUNCTION_AUDIT.md`。
- 不能把采样标记说成真实角色动画预览。
- 不能把作者态 IR 计算说成游戏内试听或声学同步已接入。
- 本轮没有重复完整旧编辑器测试与运行时预览测试。前轮记录的 2672 通过／3 跳过、运行时灯光收发、素材审计属于此前测试；它们没有发现坐标错误，不能替代本次针对性检查。
- 前轮原数据校验报 40 错误、281 警告；本轮按范围不改原数据，未把这些存量问题算作修复。
