# 私有信号 + 批量盖章 · 终审复审判决与修复（2026-08-09，macOS 本机）

> 承接 `私有信号-终审复审交接-2026-08-09.md` §4 的复审任务。复审 agent 全程本机实测
> （双树基线对照 + 攻击探针 + 变异复验 + QtWebEngine 真两页联动 + 真机注入），
> 判决当日由制作人拍板修复方案并全部落地。本文= 判决存档 + 修复落点 + 验收结果。

## 一、复审判决（原 3 阻断 + 4 高）

**全部实测成立，无一回滚。** B1 四道防线在真两页联动下各自有效且反事实证实威胁真实
（掐掉 reload 通知，3 张作曲真被抹）；B2 逐字节绿；B3 链路存活；H1 真机显式 owner 压过
origin、47 节点注入回归未复发；H4 raw 三方逐条一致；H5 幂等注册；M5/M9/M11/H2 变异
红绿闭环；真机定向投递/fail-loud 六用例全过。

## 二、复审新抓的问题与修复（同日全部落地）

| # | 发现 | 修复落点 | 验证 |
|---|---|---|---|
| G-1 | 交付测试 `test_entry_stages_one_graph_per_selected_entity` 从未真跑绿（无宿主→H6 哨兵弹 warning→离屏挂死；打桩则被闸拦） | 该用例补 `_FakeHost`（干净叙事页，`_web_editor_dirty_state` 恰返 False）+ 打桩 warning | 全文件本机 offscreen 24 passed / 1.4s（此前挂死 15 分钟） |
| G-2 | 机制卡硬契约 3 不设防：51/55 张图 owner 成对非空（含全部 flow/scenario），"成对判"全放行 | 判据改 ownerType 白名单 `PRIVATE_SIGNAL_LISTENER_OWNER_TYPES = ['npc','hotspot','zone']`：TS 权威 + narrative_state_editor.py + validator.py 三面逐字对齐；新增四镜像语义对账测试（含 BATCH_ENTITY_KINDS）；机制卡契约 3 重写 + 补 ownerNoGraph（L-3） | 成对 owner flow 图监听 = error（TS+Py 新用例）；白名单矩阵用例；变异（白名单+flow）必红复原绿；现网零私有信号 validate-data 增量 0/0 |
| G-3 | B3 修复行零护栏（scope 判据改坏 59 测试全绿） | 新增 `test_signal_scope_survives_normalize_then_stamp`：raw→normalize→stamp 全链路 private 存活、global 不落键 | 复审员原变异（`=="__never__"`）重放必红、复原绿 |
| G-4 | 种子模板不声明 signals，安全螺栓靠策划记性；忘勾私有=100 箱共用全局 taken 串线 | `pickable_object_archetype` 补 `signals:[{id:'{{takenSignal}}',notes,scope:'private'}]`（params 与 composition 之间，规范键序）；takenSignal 参数 note 改"盖章自动登记" | 端到端三形状：零操作盖出即私有 / 第二份幂等复用 / 已有全局同名 error 拦串线；字节保真门绿 |
| Z-2 | 模板通道 scope 拼写错（PRIVATE）静默 fail-open 成全局 | `validate_templates_file` raw 层新增 `template.signal.scope.invalid` error（缺键/null 合法缺省不响） | 五种坏值全响、四种合法形状零误报 |
| 可用性② | noemit warning 甩 blackbox/meta.emits 字段名且不指路 | 文案改「有人听、还没有人发：给它接一张发射端对话图（对话里加「发出信号」动作）…」 | — |
| 可用性③ | 模板列表/预览暴露 composition、dialogueStubs 英文字段名 | 批量盖章对话框 `_PRODUCT_LABELS` 映射：状态机图/任务/对话桩（两处） | — |

### 收尾门顺带抓出的两条分支回归（复审报告之外）

- **`emitNarrativeSignal.ownerId` 未登记 `ENTITY_REF_PARAMS`**（H1 加参漏了条件性第五件）：
  补 `"emitNarrativeSignal": {"ownerId": "owner"}`（与 startDialogueGraph 同 spec，按成对
  ownerType 判类别后才级联）。parity 测试恢复绿。
- **`sync_source_reference_mode` 闭包晚绑定**：`w` 是参数循环复用变量，ownerType/ownerId
  追加到 sourceId 之后后，切 sourceType 必抛 `AttributeError`（跳转开关全程不刷新）。
  改默认参绑定。`test_dialogue_graph_jump` 恢复绿。

## 三、修后全门（2026-08-09 本机实测）

- tsc 0 error；vitest 4 failed = 基线存量集合（3×XungouMainFlow + 1×PackageDirector +
  2 anim_preview 空文件），717 passed（基线 716 + 本轮新增 1 条 TS 用例）
- 编辑器全量（钦定 deselect）**1264 passed / 0 failed**（复审时全量门不可信、有 2 真实回归）
- dge 156 passed（往返保真含内）；调试器+xref+json_lang：6 failed 同名存量 / 214 passed
- validate-data `96 error(s), 316 warning(s)` 与基线逐字相同（增量 0/0）；素材审计 issues 0
- narrative_templates.json 字节往返门绿

## 四、维持后置（复审排序确认）

Z-1（H4 编辑器 normalize 路径残缝，现库零触达）、Z-3（produces/from 无 UI）、
Z-4（edgeRouting NUL）、L-1~L-5（低档清单见复审报告）。
另：emitNarrativeSignal ownerId 仍是裸 QLineEdit（违选择器铁律），已挂独立任务待做。

## 五、结论

修复后判决条件全部满足：**终审通过，master 当前状态成立**。
