# 主编辑器全面审查 — 合并去重问题清单（2026-07-02）

> **修复状态（2026-07-02 完成，8 波）**：全部 P0(4) + P1(41) + 绝大多数 P2 已修；P3 修了系统性/高价值项。
> **硬约束全部达成并证明**：① save_all 输出**逐字节等于修复前**（HEAD-code vs 全部改动，同数据同种子 `diff -rq` 空）；
> ② 运行时零改动（未改任何 `src/` 运行时文件；`tsc` 0 error、`vitest` 398 全过、src/core 164 全过）；
> ③ 编辑器 452 测试 + 图对话 41 测试全过、`validate-data` 0 error、asset audit 0 issue。
> 新增护栏：`test_action_condition_data_safety.py`（悬垂/bool化/clamp/(非枚举)/schema外键/条件保真 24 例）、
> `test_action_manifest_parity.py`（运行时↔编辑器↔TS manifest 三方 parity）。详见文末「修复纪要」。


- **范围**：`tools/editor/**`（全部编辑器/共享控件/校验层/壳层）+ 内嵌 `tools/dialogue_graph_editor/**` + `tools/narrative_editor_web/**`（本分支 diff 复查）+ 运行时对照 `src/**`。约 8.5 万行全量通读。
- **方法**：11 个模块组并行审查 → 全部 P0/P1（48 条）+ 用户补报 6 条经对抗性复核（离屏 PySide6 探针实测 / 运行时交叉 / 实跑 validate-data），**54 条证实、0 条被推翻、2 条修正范围**；一组跨组矛盾（combo 滚轮）裁决后大面积证伪。
- **基线**：425 个编辑器测试全绿——既有护栏覆盖 model 层往返与表单探针，**本清单问题几乎全在流程层**（门控/切换/取消/悬垂引用），该层零测试覆盖。
- 标注：〔实测〕= 探针/实跑复现；〔潜伏〕= 机制成立但现网数据未命中（agent/手写 JSON 一踩即中）。

---

## P0（4）

| # | 问题 | 位置 |
|---|---|---|
| P0-1 | **IdRefSelector 悬垂引用静默顶替/清空**：当前值不在候选时，allow_empty=False → 显示并在 Apply 写回**第一候选**；True → 清成 `""`。约 40 个调用点无防护，坏引用被"洗白"。〔实测〕调用点级后果：quest 未分组任务被塞回上一任务分组（quest_editor:944〔实测〕）、quest/encounter/shop 新增行默认选中第一个真实目标、map 新节点界面显示第一场景而模型为空串 | shared/id_ref_selector.py:64-88 |
| P0-2 | **未登记 flag 的数值条件被 bool 化**：`value:3`→`true`（`op:"=="` 时 value 键直接消失），运行时 `looseEqual(3,true)` 为假、条件语义翻转；setFlag 动作同病；graph_editor zone_panel 无 pm 上下文时连已登记键也 bool 化。validator 无兜底。〔实测〕 | condition_expr_tree.py:527-544,659-668；condition_editor.py:97-106；action_editor.py:4758；flag_registry.py:323-325 |
| P0-3 | **场景编辑器点画布空白丢未应用编辑**：deselect 先清 pending 再重建 staging，绕过 commit-on-leave（切实体路径 :7322 有 commit，对照实测同一编辑一存一丢）〔实测〕 | scene_editor.py:7363→3344-3350 |
| P0-4 | **规矩编辑器 Apply 后碎片面板脱钩**：blockSignals 恢复高亮但 `_frag_idx=-1`、表单已空；继续输入再 Apply 直接 return，**输入全丢无提示**（`_apply_rule` 同路径）〔实测〕 | rule_editor.py:309-320（用于 571/365） |

---

## P1

### A. 未保存编辑被静默丢弃（流程门控窟窿）

| # | 问题 | 位置 |
|---|---|---|
| P1-1 | scene 十余控件不接 changed 信号、永不置脏：camera zoom/ppu、worldScale、walk/run、热区 type、autoTrigger、pickup 三件、encounterId、出生点 key/x/y、解锁比例 worldWidth/Height（:3466 提前 return）、targetScene 取消弹窗（:4715）→ 切场景/实体即丢〔实测，entity 类即丢、scene/spawn 类可靠补 Apply 救回〕 | scene_editor.py:3153-3235,4115,4129,4371-4374,4410,6466-6475 |
| P1-2 | zone 顶点表编辑不置脏不 mark_dirty（三份复制中唯一漏掉的；hotspot :4652 / NPC :5024 有）〔实测〕 | scene_editor.py:6244-6262,7374-7384 |
| P1-3 | 「+新增」绕过切换确认：timeline「+ 过场」强制清 pending（〔实测〕对照组切行弹确认）、encounter「+ Encounter」、rule「+ Rule/+ Fragment/+ 关联碎片」（经 `_refresh` 清索引）〔实测〕 | timeline_editor.py:2427-2436；encounter_editor.py:845-853；rule_editor.py:369,576,257 |
| P1-4 | rule 碎片切行 / 切筛选无 commit-on-leave（规矩侧 :183-191 有，对照）〔实测〕 | rule_editor.py:515-554,462-465 |
| P1-5 | **closeEvent 不 flush**+五个编辑器无 confirm_close：archive/audio/overlay/filter/player_avatar 改字段直接关窗 → 不 flush、不提示、丢。修复耦合警告：若 closeEvent 补 flush，须连带修 filter(:300-304)/player_avatar(:147) 的"无条件 flush 注脏"，否则每次关窗误弹保存提示 | main_window.py:1334-1358 |
| P1-6 | AnimEditor 脏态对三重门控全隐身（无 confirm_close/flush/mark_dirty）：States 未保存直接关窗/换工程零提示〔实测 hasattr+grep〕 | anim_editor.py 全文；main_window.py:1334-1358 |
| P1-7 | **气味 Profile 编辑器整体游离保存体系**（合并 5 条）：①绕过 Save All/mark_dirty/confirm_close，唯一写盘是页内按钮；②读文件失败静默吞掉换**缺 form 块的空骨架**，点保存即毁档（〔实测〕6 profiles+form 全丢）；③`write_text` 非原子；④`_on_change` 按已知键重建、未知字段静默删除；⑤保存后 `model.smell_profiles` 陈旧（仅 load_project 时读）→ 场景页 scent 下拉**确定**过期；⑥无新增/删除/改名入口、保存无成败反馈；⑦`smell_profiles` 未在 `__init__` 初始化有 AttributeError 险 | smell_profile_editor.py:41-52,159-167,201-232,255-257 |
| P1-8 | 图对话工具栏「打开…」/公开 load_path 无 dirty 检查直接覆盖（docstring 声称会提示是虚言；树内切换/新建图/open_graph_by_id 都有提示） | dialogue_graph_editor/editor_widget.py:1083-1091,1079-1081 |
| P1-9 | flag patterns 220ms 防抖竞态 + 无 flush_to_model 钩子：敲键后 220ms 内关窗**彻底丢**（timer 随销毁不 fire、closeEvent 无提示）；Ctrl+S / 关窗选 Save 时本次落盘缺最后一笔（timer 稍后补模型可靠二次保存救回）。静态 flag 表不受影响（同步 mark_dirty） | flag_registry_editor.py:29-32,307-318 |
| P1-10 | filter 编辑器四件套缺三件（无脏判断/commit-on-leave/confirm_close）+ flush 无条件 Apply：仅点选 day 滤镜+Save All 即把 `1/0` 重写成 `1.0/0.0`〔字节实测〕 | filter_editor.py:220-222,234-243,300-304,251 |
| P1-11 | IdRefSelector editable 三连：手打文本不发 value_changed（只连 currentIndexChanged）→ 配合"不置脏→commit-on-leave 不触发"可丢；清空文本无效（current_id 返回陈旧 id，无法清引用）；set_items 缓存跳过路径抹掉未提交手打文本（跨面板刷新时机踩中）〔实测；潜伏面广——现调用方均 Apply 时轮询〕 | id_ref_selector.py:25,114-115,90-103,50-61 |

### B. quest 编辑器"表单反向污染模型"

| # | 问题 | 位置 |
|---|---|---|
| P1-12 | 树里拖拽换组后点其它节点，commit-on-leave 用表单旧 group 覆盖拖拽结果（分组 parentGroup 拖拽同理被 `_apply_group` 回滚）〔实测〕 | quest_editor.py:858-899,799-813 |
| P1-13 | 属性面板改 parentGroup 无环检测（`_would_create_cycle` 只在拖拽路径用）+ 候选含自身/后代 → 成环后分组+任务从树/图**整体消失**、带环落盘、不可再选中修复〔实测〕 | quest_editor.py:961-990,539-541 |
| P1-14 | 删除分组连带删任务，不清其它任务指向的 nextQuests 悬垂边（`_delete_quest` :1148-1151 会清，对照）〔实测〕 | quest_editor.py:1085-1116 |

### C. 悬垂引用被静默改写（action_editor 家族）

| # | 问题 | 位置 |
|---|---|---|
| P1-15 | addArchiveEntry 悬垂/空 entryId 构造时即被改指该 bookType **第一条档案**（指错不报错）〔实测〕 | action_editor.py:2283-2299 |
| P1-16 | setEntityField / setSceneEntityPosition 悬垂 entityId 改指第一 NPC；后者还用新实体当前坐标**覆盖原存 x/y**（实测 123.45→485.7）〔实测〕 | action_editor.py:2751-2781,2887-2889,2983-3027 |
| P1-17 | 非 strict 类 id 参数悬垂 → 保存写 `""`：giveItem/updateQuest/giveRule/startEncounter/startCutscene/openShop/playBgm/切场景（连带 targetSpawnPoint）等（strict 类有孤儿兜底，对照）〔实测〕 | action_editor.py:2570-2652；id_ref_selector.py:79-88 |

### D. 打开/点选即改数据（参数层）

| # | 问题 | 位置 |
|---|---|---|
| P1-18 | 泛型 float 量程 ±50 clamp：persistNpcAt 世界坐标（实测 x:1200→50.0）、addFlagValue.delta（100→50.0）、setSceneDepthFloorOffset（120→50.0）〔实测；潜伏〕 | action_editor.py:4015-4022 |
| P1-19 | `_to_dict_raw` 按 schema 重建、运行时认识的键**保存即删**：changeScene cameraX/cameraY（运行时明确消费）、fadingZoom/fadeWorld* 的 duration 别名、sugarWheelResetPointer.angle〔实测；潜伏〕 | action_editor.py:4746-4776 |
| P1-20 | pickup 缺 count 打开即写 `count:0`（运行时默认 1 →"捡 0 个"），并凭空注入 `isCurrency:false`〔实测；潜伏〕 | action_editor.py:134-146 漏登记 |
| P1-21 | scenario outcome 字符串经 json.loads 回读改类型（"true"→bool、"3"→int；运行时 `===` 永假）〔实测〕 | condition_expr_tree.py:601-610,696-705 |
| P1-22 | 非枚举旧值以「(非枚举) xxx」**展示文案写回 JSON**（showNotification.type / setScenarioPhase.status）〔实测；需脏数据〕 | action_editor.py:4108-4120,3374-3384 |
| P1-23 | 条件树非空时「专家兜底原始 JSON」新粘贴内容被 to_list 静默忽略（tooltip 却称组内 AND）〔实测〕 | condition_editor.py:279-298 |
| P1-24 | 条件层 int→float 漂移（preserve_numeric_repr 只护 action params，条件层没接）：condition_editor `float(v)`、树 :668、game_config startupFlags 强转、FlagValueEdit decimals=4 截断/1e9 夹取〔实测〕 | condition_editor.py:106；condition_expr_tree.py:668；game_config_editor.py:276；flag_value_edit.py:72-75 |
| P1-25 | 出生点选择器直写 model 与场景 staging 冲突：目标=当前场景时 Apply 用旧快照**整体覆盖 spawnPoints**，刚建出生点被删、引用悬垂〔实测〕；且对话框一打开就向目标场景注入默认 spawnPoint 并 mark_dirty，Cancel 不回退〔实测〕 | scene_editor.py:2400-2428,3933-3948,2322-2328 |

### E. id 撞车家族

| # | 问题 | 位置 |
|---|---|---|
| P1-26 | 新建 id 用 `len(列表)` 命名、删过中间项必撞现有 id：scene hotspot/NPC/zone（〔实测〕重复 id 致画布图元覆盖、编辑串台）、timeline 过场（〔实测〕`cutscene_2` ×2；现库有 cutscene_11；validator 用 set 建索引**天然吞重复**且无过场去重）、quest 分组、rule、item、shop（encounter/filter/spawn/archive 已防撞，对照） | scene_editor.py:7985,8003,8021；timeline_editor.py:2429；quest_editor.py:1043-1077；rule_editor.py:265,372,583；item_editor.py:322-328；shop_editor.py:250 |

### F. 小游戏

| # | 问题 | 位置 |
|---|---|---|
| P1-27 | water cue「模式」下拉来回切换即毁 cue 文案不可恢复（clear() 连撤销栈清；hint 版无 clear 不对称自证失误） | water_minigame_editor.py:1699-1711 |
| P1-28 | 氛围脚本 say 步骤 `slot` 字段保存即丢（在 _MANAGED_KEYS 又不被 say 分支写、身份保留循环跳过 managed 键；运行时确实消费）〔实测；潜伏〕 | atmosphere_script_editor.py:344-355,37-40 |

### G. 壳层

| # | 问题 | 位置 |
|---|---|---|
| P1-29 | F5 不自动切「运行与预览」页 + 启动状态/错误写进不可见页：页被 `_StackPageHost` 包裹后三处 `indexOf` 恒 -1〔实测〕 | main_window.py:698 vs 853,905,965 |
| P1-30 | load_project 先改 project_path 再逐文件读、无异常回滚：换工程遇坏文件 →"新路径+新旧混合数据"，之后 Save All **跨工程错误写回**（弹错后不还原不禁保存；Discard 分支不清 dirty） | project_model.py:139-141；main_window.py:375-379,419 |
| P1-31 | flag_registry.json 读取吞一切异常静默回落**空默认表**：Flags 页编辑+Save All 用近空结构覆写原登记表 | flag_registry.py:76-87 |

### H. 校验层 / 叙事域（现行可见）

| # | 问题 | 位置 |
|---|---|---|
| P1-32 | **TS 权威校验器 action 清单脱节**：缺 14 个真实 action（含本分支 smell 新增）、addArchiveEntry 参数名旧（type/id vs bookType/entryId）→ 现网数据 **5 个幻影 error、网页 Ctrl+S 恒被拦**（Python 侧 0 error；Qt 全部保存仍可用）。add-game-action 三件套没覆盖这第四处清单〔实测 tsx 直跑〕 | src/core/narrativeGraphValidation.ts:738-824 |
| P1-33 | 多入口共享图被误报"流程孤儿"（运行时支持 dialogueGraphEntry 指定入口；:337-342 自己校验该字段却不用于豁免）：现网 街巷_市井闲谈 17 节点误报，--strict 直接红〔实测〕 | validator.py:2019-2028 |
| P1-34 | parallel 子轨递归重扫+临时演员集按子树重算：外层 spawn 的演员在子轨内误报"无法解析"+全部参数告警双份（实测 5 条假 warning，占当前 38 条 13%）〔实测〕 | validator.py:2426-2427,2488-2489 |
| P1-35 | narrative_data phases 表"拖行号排序"**纯视觉、永不写入数据**（sectionMoved 只改 visual↔logical 映射，sync 按逻辑行遍历；tooltip 明示可拖）〔实测〕 | narrative_data_editors.py:486-489,696-699,1002-1036 |
| P1-36 | 文档揭示 scenario 阶段条件 phase 失配被静默清空落盘（combo 无「（数据）xxx」保值项；启动即建+首行自选+Save All 无条件 sync；空 phase 校验放行）〔潜伏，phase 改名即引爆〕 | narrative_data_editors.py:1790-1793,1903-1912,2042-2115 |
| P1-37 | 叙事网页 ConditionBuilder 防丢护栏**误杀主动删叶子/改叶子身份**：整单拒绝写回，按钮只剩"加条目/改 op·value·status"可用，报错归因错误 | narrative_editor_web/ConditionBuilder.tsx:56-66,173-184 |

### I. 图对话编辑器（全部探针实测）

| # | 问题 | 位置 |
|---|---|---|
| P1-38 | 文件树重选未屏蔽信号，程序化打开被重入劫持**加载回旧文件**（内容=A、树高亮 B、B 点不开；切文件选「保存」双重加载）〔实测〕 | editor_widget.py:1859-1868,1996,2269-2299 |
| P1-39 | 「复制子树」`_remap_local_next` 漏 ownerState/contextState 出口（cases[].next/defaultNext/missingWrapperNext）→ 副本跨树窜线〔实测〕 | editor_widget.py:1534-1554 |
| P1-40 | quest 叶子原本无 status 键时用户改「状态」下拉被静默丢弃（`_had_quest_status` 是构建期常量，保真"不注入"矫枉过正）〔实测〕 | node_inspector.py:2504-2509 |
| P1-41 | 清单外 phase 被静默清空写 `phase:""`（"(缺失)"项 text≠data、refill 后 findData 落空归零，浏览节点切走即写）〔实测〕 | node_inspector.py:2143-2160,2347-2353 |

---

## P2（设计 / 条件性数据问题）

**系统性根因**
- P2-1 **五处手工镜像清单、零 parity 护栏**：运行时 ActionRegistry ↔ ACTION_TYPES/_PARAM_SCHEMAS ↔ TS knownActionParamSchemas ↔ validator ↔ present 类型三处。P1-19/20/32 皆此根因现行证据。一个 parity 测试可按住整族。（action_editor.py:148-438 等）
- P2-2 门控靠 getattr 鸭子协议、无强制注册契约：新编辑器缺钩子静默漏网（P1-6/7 即证据）。
- P2-3 共享控件程序性 setter 信号语义分叉（set_key 发 / set_current 静默 / setText 发）逼调用方各自 blockSignals；IdRefSelector 与 FilterableTypeCombo 双轨并存、相邻两行混用；IdRefSelector docstring 自称 searchable 实无过滤。（flag_key_field.py:47-52 等）
- P2-4 god-method/复制体：`_rebuild_params` ~1600 行与 `_to_dict_*` 手工对偶（多处构建/导出不一致源头）；scene 顶点表三份复制（P1-2 即分叉产物）；ScenePropertyPanel 单类 ~3700 行。
- P2-5 Python 桥把 `_PARAM_SCHEMAS` 整体当**必填**校验、28 类型比 TS 严，违背"Python 是 TS 子集"红线（省略可选参数的合法 JSON 被 save_all raise 拦死）〔潜伏〕（narrative_state_editor.py:1699-1720）
- P2-6 Py/TS 同一缺陷 code/path 拼写不一致 → 合并面板双报、"save blocked: N" 虚高（initialState.invalid vs graph.initialState.invalid 等 6+ 组）。

**保存/桥接**
- P2-7 桥 slot 无顶层异常兜底：saveData 异常 → 网页收 undefined → 判"已保存"清脏 → 关闭不再 flush 静默丢；getData 异常 → 空画布 fallback 可反向覆盖模型。（narrative_state_editor.py:376-393,169）
- P2-8 任一内容资产里的 setNarrativeState 无条件 error 进保存校验 → narrative 脏时 save_all **中途 raise**（前面桶已写后面未写）；报错无法聚焦；ActionEditor 又提供该动作。〔dormant〕（narrative_state_editor.py:1369-1386；project_model.py:402-413）
- P2-9 dist 缺失 staleness 死路：终端构建后「重新检查」→ 横幅隐藏、页面仍占位、无加载按钮；占位文案叫按 F5 而 F5=运行游戏。只能重启。（narrative_state_editor.py:747-759,920-931）
- P2-10 Build Export：同步 subprocess.run 冻结整个 UI + 裸 `npm` 不走 `_npm_run_command`/PATH 补全（补全只写 QProcessEnvironment 不改 os.environ）→ Dock/Finder 启动时 F5 能跑、Build 报"未找到 npm"。（main_window.py:1190-1199 vs :66-124）
- P2-11 Edit▸Undo/Redo 全局死菜单（push_edit/DataEditCommand 零调用者、undo_stack 恒空）。（main_window.py:228-231；project_model.py:19-35,501-505）

**"打开/保存即改写"家族（格式+个别行为差）**
- P2-12 注入默认键/恒写可选键（合并 10+ 处）：scene setdefault camera/depthConfig floor_offset 0.0/zone.smell 恒写 intensity+dir round(0.125→0.13)；quest 注 acceptActions:[]/nextQuests:[]；water 恒写 pull.timeLimitSec=15（运行时按节奏 14/10/12）+flush 注 entities:[]；sugar 单字段一动 30 键全量重写+setdefault speechAnchors/atmosphereGroups+chargeButton 三键互钉+**settle 默认劈叉（编辑器 0.12 vs 运行时 0.085，编辑器自家预览用 0.085 自相矛盾）**；atmosphere 注 wait/sec、degBuffer；paper _write_slot 注 optional:false、_write_part/finish 恒写 tags；timeline 过场级 targetX float 化+restoreState:true 被删键。**行为差三个**：atmosphere chance 缺 p 显示并回写 0.5（运行时缺省 0——"从不命中"变 50%）、sugarWheelShowSpeech durationMs seed 0（0→500ms vs 缺键 3000ms）、setSmell intensity seed 0（缺键运行时 60）。
- P2-13 ConditionEditor set_data/to_list 改条件数组形状与顺序（flag 行恒前置、≥2 非 flag 叶包成 `{"all":[…]}`）：map_config 实测 5 节点双叶列表、MapEditor 即时提交一动即变形；apply-gated 编辑器同形数据变幻影脏。（condition_editor.py:264-284）
- P2-14 pressure_signal 数值失真：ratio spin decimals=2 截断被 commit-on-leave 静默写回；现存 `resetToRatio:0`/`fillSeconds:9` 任一次 Apply 写成 0.0/9.0 并改 interrupt 键序。（pressure_signal_editor.py:41-47,95-104,281-284,348-349）
- P2-15 flag_registry_editor patterns 每键 220ms 后整表按白名单重建：pattern 额外字段静默蒸发、legacy valueType 归一改写〔潜伏〕。（flag_registry_editor.py:310-318,393-405）
- P2-16 anim 重命名 state 后 def 内未知键保存被丢（UserRole 快照被改名覆盖）〔潜伏，25 包现无额外键〕。（anim_editor.py:657,795-800）
- P2-17 图对话 7 个非幂等 get_node 形状（裸 flag 注 value:true / scenario 注 status:"pending" / outcome:true→"True" / reached:false 丢键 / questStatus 小写归一 Inactive / 仅 textKey 注 text:"" / ownerState 注空键）+ 切换即 flush = 浏览即改写〔实测；潜伏〕。（node_inspector.py:2489-2535 等）
- P2-18 图对话叙事归属/图 id **每键**改写 scenarios 目录并标脏（tooltip 称"保存后写入"；放弃草稿留悬空 dialogueGraphIds）。（editor_widget.py:2101-2114,2161-2187）

**撤销/画布**
- P2-19 图对话增删改名/复制节点绕过 QUndoStack（误删不可恢复、撤销反而回退更早正文）；整图重建后"节点移动"撤销静默失效。（editor_widget.py:2439 等；flow_oden_controller.py:533-555）
- P2-20 water canvas clamp 对程序性 setPos 也生效而 _suppress_moved 只压信号：越界实体图元被夹紧、模型不变（漂移），此后拖 1px 夹紧值入模型（paper_craft 的 _syncing 守卫是正确范式，对照）。（water_minigame_canvas.py:301-315）
- P2-21 water flush_to_model 无条件 mark_dirty：每次 Save All / 运行游戏必重写全部水域文件。（water_minigame_editor.py:1788-1810）
- P2-22 重入标志被嵌套填充函数硬复位（finally 一律 =False 而非恢复原值）：sugar `_fill_form` 尾段/扎纸 4 处在无保护窗口执行，今天不炸是碰巧（水编辑器 prev/finally-restore 是正确范式）。（sugar_wheel_editor.py:1620-1640,2213-2234；paper_craft_editor.py:724-1008）

**滚轮防误改（裁决合并）**
- P2-23 主编辑器有全局 combo 滚轮防护（__main__.py:59，750ms 轮询装 filter）——各组的大面积"combo 裸奔"指控**证伪**。真实残留：①guard 用 `id(widget)` 去重且从不清理 → 地址复用后新 combo 永久漏防护+集合泄漏（qt_combo_wheel_guard.py:37-47）；②新建 combo ≤750ms 裸窗；③**QSpinBox/QDoubleSpinBox 完全不在防护范围**（数值滚轮误改仍全线存在）；④scene_depth_editor/copy_manager/chronicle_sim_v2 三个独立工具未装。

**引用与刷新**
- P2-24 改 id 无联动家族：rule 改 id 不联动碎片 ruleId（且无重名/空校验，坏数据可落盘〔实测〕）；item 改 id 不联动 shops/encounters；encounter 改 id/"生成唯一 id"直写模型绕 Apply 门、不联动热区（且 id 只能从 encounter_N 候选选，造不出现有语义化 id=设计倒退）。quest 侧有联动（对照）。（rule_editor.py:322-335；item_editor.py:296-320；encounter_editor.py:417-532）
- P2-25 跨面板刷新缺口：game_config（initialScene/Quest/Cutscene）与 encounter 候选只在构造时拉取、缺 reload_refs_from_model（约定钩子现仅 ShopEditor/场景实现）；scene 的 reload 漏 bgm/ambient；paper correctPaper 下拉只在重选订单时重建（新增纸色选不到、删除留 stale——加载侧已有「（未设置）」哨兵挡"写第一种纸"）。（game_config_editor.py:63-81；scene_editor.py:2847-2857；paper_craft_editor.py:663-672 vs 973-983）
- P2-26 narrative_catalog 每次查询从磁盘重读 narrative_graphs.json（无视 ProjectModel 内存态、无缓存）→ 图对话 Inspector 的 state 候选与两步保存中间态错位。（narrative_catalog.py:25-33）
- P2-27 quest apply 被重名/空 id 校验拦下时 flush 仍返回 True、导航照常 → 未保存编辑被覆盖（encounter 失败会回跳中止，对照）。（quest_editor.py:770-774,966-1003）
- P2-28 audio 表重复 id 静默 last-wins（Save All 同路径）、空 id 带 src 的行静默丢；audio flush 无条件 Apply+mark_dirty 每次保存重写文件、条目规格化仅 {"src"}。（audio_editor.py:517-535,753-771,802-809）
- P2-29 archive Apply/Save All 用 refresh=True 清列表选中：保存后继续输入→点同一行（prev==row 不触发提交）→ 表单被重置、输入丢。（archive_editor.py:469-471 四页同构）
- P2-30 "删除作用于清选中后的残留索引"家族：archive 删除钮、timeline 删除后表单残留可编辑且 Apply 静默 False、pressure/signal「+ 添加」后删除删旧项（确认框显 id 半保护）。（archive_editor.py:488-496；timeline_editor.py:2655-2668；pressure_signal_editor.py:222-225,380-398）
- P2-31 hex color set_hex 对无效值静默换默认色/大写归小写/#AARRGGBB 丢 alpha，随 Apply 落盘无提示。（hex_color_pick_row.py:50-59）
- P2-32 project_paths url_to_disk 对 media/text 也放行本机绝对路径（docstring 只说 any）→ 编辑器预览正常、审计绿灯、运行时 404；解析族不 resolve/反解族 resolve 不对称是 /var↔/private/var 一族问题根因（测试已改两侧 resolve 规避）。（project_paths.py:272-279,251-304）
- P2-33 图对话 `_doc_model is None` 直改 `_data` 旁路（现死分支，单一真相源唯一官方后门）。（flow_oden_controller.py:872-875,897-900）

**校验层其余（误杀/漏杀）**
- P2-34 误杀：`archive_book_` pattern 前缀吞 `archive_book_entry_`（首匹配无最长前缀优先，实测误报合法 flag）（flag_registry.py:228-251）；scenarios"配 exposes 未设 exposeAfterPhase"判**保存级硬错误**拦整工程（量刑过重，中间态存不了）（scenarios_catalog_validate.py:115-121）；`it["id"]` 硬下标任一条目缺 id 即 KeyError 整个 validate 崩（validator.py:65-71,510,526-527）；ownerState case 比对未以 known 非空为前提，零 states 的 wrapper 全 case error（graph_document.py:483）。
- P2-35 漏杀：giveItem/startCutscene/updateQuest/addArchiveEntry/openShop/playBgm 等**裸 id 参数完全无人校验**（docstring 声称覆盖与实现不符）；questStatus 小写恒假不报；scenario 叶缺 status 运行时不识别不报；图缺 entry 全量校验不报（运行时拒播）；`[tag:]` 保存级扫描漏 showSpeechBubble.text/waitClickContinue.hint 且 signal_cues/pressure_holds/paper_craft 整文件不在名单（同形状热点里会拦保存——量刑不一）；`[tag:` kind 拼错完全静默；`_validate_flags` 以注册表非空为闸、空表时**大半动作层校验静默失效**；choice requireFlag/ruleHintId 不校验。（validator.py/ref_validator.py 多处）
- P2-36 asset audit 缺口：键名清单漏 `animManifest`（现网在用）/blendOverlayImage fromImage/toImage/archive illustration；FS 大小写+Unicode 规范化不敏感（macOS 绿→Linux 404）；`?query` 不剥。（asset_reference_audit.py:37-63 等）

---

## P3（UX / 一致性，合并后择要）

- P3-1 骗人 UI：map 新节点右侧显示第一个场景（模型为空串）；timeline 删除后右侧残留已删过场、Apply 静默无反馈；archive 切书后页表单显示上一本内容；图对话"连线失败"横幅跨文件残留。
- P3-2 timeline：过场列表无搜索；步骤删除无确认无撤销（一键删整 parallel 块）；无跨过场复制粘贴；并行子轨 contentChanged 两套接法（新 showImg 不进 hideImg 候选）。
- P3-3 图对话：单键 A/F 全窗口快捷键触发不可撤销自动布局并写盘；拖节点即整页重建右侧检查器（折叠/滚动复位）；取消「多拍」静默丢全部 lines 无确认；草稿撞名静默改存 `<id>_1.json` 连图 id 一起改；新选项未填 id 每键 toast 轰炸且编辑不落模型。
- P3-4 叙事网页：显示名每键 trim 吃空格（"Phase 2"打不出）；scenario wrapper「未知引用」黄标成片误报（与校验器口径不一致）；独立 dev 模式保存文案误导；RF 删除通道 confirm 取消脱同步（现不可达潜伏）。
- P3-5 narrative_data：requires 非法 JSON 在 Save All 路径被 `except: pass` 静默吞（仅 Apply 弹错）；requires `{"all":[str]}` 载入写回裸数组、phase 名含逗号被拆碎；改名 phase 时 exposeAfterPhase 静默重置；narrative_state 页面 JS 不可读时 flush 返 True 假成功（最长 ~20s 卡 UI）。
- P3-6 atmosphere：行「✕」删整棵 then/else 子树无确认；切 op 丢本会话对旧 op 参数的编辑、_orig 不随写回更新；pool+text 并存显示 pool 保存丢 text（运行时优先 text）；未知 op 的 managed 键不保留（前向兼容虚言）；未知字段保留但挪到键序末尾。
- P3-7 sugar：删氛围组不重选；复制组产重复 id；组 id 可空/重复无校验；speechAnchors 表任一格一动重写全行（tailDirection 打错字静默改 "none"、枚举列裸输、xRatio 非法输入显示与模型不一致）。
- P3-8 water：实体无 motion 时组内控件残留上一实体值，勾选启用即拷别人参数；`set_project_context(model,None)` 相等即返使刷新 no-op。
- P3-9 共享控件小项：flag_key_field 载入路径冒 changed（condition_expr_tree:522 没裹）；image_path_picker changed 双发+None-model 写绝对路径；move_entity_map_picker 把全部既有 waypoints 重舍入 2 位（终点保留原精度）；RichTextTextEdit 无 tag 也常驻"（引用校验通过）"横幅；scripted_lines to_list 静默丢空 text 行+无工程分支 min>max 互斥；numeric_roundtrip 不处理 float→int 反向与嵌套；pick_strings docstring 与默认排序矛盾；tag_catalog 插入引用对话框搜不到模板派生 flag（visited_*）；flag_picker 过滤按叶子决定组显隐+不自动展开。
- P3-10 壳层小项：faulthandler 第二次 enable 顶掉 stderr 注册（native crash 只进日志）；project_model perf 打点 sugar_wheel stamp 落在 paper_craft 分支；game_browser URL 栏/External browser 钉死 5173（load_dev_url 死代码）；`_build_game` 见 P2-10。
- P3-11 布局违规残留：settings_dialog 裸 QFormLayout+自赋值无操作；smell/scene"基本"组裸 QFormLayout；smell 说明堆界面；archive Lore 页条件区在滚动区外（其它三页在内）；quest 图滚轮缩放无上下限（map 有 0.1-10 对照）；rule ruleId 改归属指引只在 tooltip。
- P3-12 校验报告性：archive 缺 id warning 定位是 "?"；scenarios 整表校验一次只报第一条（N 个问题存 N 次）；validator 每 action glob 一次 graphs 目录无缓存；present 类型三处手工同步无护栏；showSubtitle band/align 值域不查；zone.smell.scent 查了而 setSmell 动作 scent 不查；setNarrativeState 图对话里 error、其它宿主放行（执法不对齐）；action_registry_editor 只扫 4 类宿主、嵌套不递归（浏览页，无数据险）。

---

## 已证伪 / 修正范围

1. "裸 QComboBox 无滚轮防误改"的大面积指控 → **证伪**（全局 guard 在 __main__.py:59），残留见 P2-23。
2. "flag patterns 必丢最后一次编辑" → 修正为 **220ms 防抖竞态**（见 P1-9）。
3. "paper correctPaper stale 会写回第一种纸" → 加载侧已有「（未设置）」哨兵（HIGH-10）挡住；残留=会话内新增选不到/删除留 stale（P2-25）。
4. macOS /var↔/private/var 两个长期失败测试：本分支已修（实跑通过）。

## 已核查无恙（抽样）

scene 画布四不变量（staging 单一真相源/即时入脏/commit-on-leave/图元跟随）完好，smell/depth 新代码未绕过；archive/anim/overlay/timeline 既往修复无回归；timeline `_PRESENT_PARAM_DEFAULTS` 与运行时逐条一致；`_ACTION_PARAM_RUNTIME_DEFAULTS` 除 3 个漏项外逐项吻合；WRAPPER_OWNER 两侧对齐；桥协议串未动；file_io 原子写+格式契约合规（唯一违反者=气味编辑器）；`_editor_instances` 与 stack 索引对齐；ACTION_TYPES↔运行时注册 100/100；present 15/15 三方一致；本分支 narrative_editor_web 13 项修复逐条核过（tsc 0 error、vitest 136/136）。

## 存疑待核（未定级）

条件树载入期 changed 冒泡的无闸消费方未穷举；QProcess stdout ready 后不再 drain 的缓冲累积；overlay flush 无序比较对纯顺序变化不落盘；movie bar 跨段存续（validator warning 正确性）；signal cue A→B→A 间接环运行时护栏；string_editor 大整数 >2^53 精度；quest_graph_layout_store 改名后坐标键失联；flow_layout_store 目录外同名文件串布局；WindowShortcut 对隐藏页的 A/F 响应。

## 建议修复顺序

1. **IdRefSelector 孤儿保留**（P0-1+P1-11+P1-17，一处控件修复覆盖 ~40 调用点）→ 连带 action_editor 重映射家族（P1-15/16）。
2. **TS action 清单**（P1-32，现行拦保存）+ 顺手立 parity 护栏（P2-1）。
3. **commit-on-leave/门控家族**（P0-3/P0-4 + P1-1~P1-10：统一 flush 契约、closeEvent 补 flush、防抖编辑器强制 fire timer、"+新增"走确认、smell/anim 接入体系）。
4. **条件/参数改写**（P0-2 + P1-18~P1-24）。
5. quest 反向污染（P1-12~14）+ id 撞车家族（P1-26）。
6. 校验层误杀/漏杀（P1-33/34 + P2-34/35/36）。
7. P2 设计项与 P3 UX 批。

---
*方法与原始材料：11 个模块组审查 + 5 组对抗复核 + 用户补报 6 条复核；探针全部离屏、仓库零改动。原始清单与逐条判定存会话 scratchpad（findings-01~11、verify-A~F）。*

---

## 修复纪要（2026-07-02，8 波）

**波1 共享控件根**：IdRefSelector 未知值保值（孤儿行/占位，非 strict 类不再洗白成第一候选）+editable 手打发信号/清空返回空/缓存跳过不抹文本；FlagValueEdit 新增 raw 模式（未登记 flag 不再 bool 化）+原值保真；condition_editor/condition_expr_tree 数值/outcome/专家JSON/(非枚举) 保真+原数组形状 verbatim；action_editor：±50 量程改 ±1e6（世界坐标不 clamp）、schema 外键透传（cameraX 不删）、悬垂 id 保值、pickup.count/setSmell.intensity 等运行时默认登记、(非枚举)userData、字符串 bool 解析；qt_combo_wheel_guard 改属性去重（地址复用洞）+覆盖 QSpinBox+3 独立工具装护。
**波2 壳层门控**：closeEvent 先 flush 再判 dirty；load_project 失败整体回滚；flag_registry 损坏抛错（不再空表覆写）；smell 编辑器接入 ProjectModel（单一真相源+dirty桶+CRUD+原值保真，不再自读写毁档）；anim confirm_close；flag patterns flush_to_model；filter 四件套+int/float 保真；indexOf→_stack_index_of_page；Build 异步 QProcess+PATH。
**波3 场景**：deselect 走 commit-on-leave；十余控件补 changed 信号；zone 顶点表置脏；新建 id 探测防撞；出生点对话框 staging 同步+不注入默认 spawnPoint；worldWidth 等 int/float 保真+camera 缺省不注入；zone.smell intensity 缺省不写。
**波4 内容域**：rule Apply 脱钩根治（restore 走正常重载）+碎片 commit-on-leave+改名联动 ruleId+id 校验；quest 拖拽不回滚+parentGroup 环检测+删组清 nextQuests+校验失败中止导航；encounter/timeline「+新增」走确认；id 撞车家族；item/encounter 改名联动引用；archive Apply 保选中；pressure/signal 删除残留索引。
**波5 小游戏**：water cue 双缓冲不毁文案+canvas clamp 守卫+flush 条件标脏+pull 缺省不注入+组重置；sugar 全量重写改基线驱动+settle 默认对齐运行时 0.085+重入标志 prev-restore+speechAnchors 逐格写；atmosphere say.slot 保留+chance.p 缺省对齐+切 op 不丢+删分支确认+行级 verbatim；paper 键守门+correctPaper 实时刷新+重入标志。
**波6 图对话**：文件树重入劫持（恢复选中屏信号）；打开/load_path dirty 检查；复制子树补 owner/context remap；quest status 主动编辑不丢+phase 清单外保值；每键 relink 改仅保存时；A/F 快捷键限画布作用域+自动布局确认；多拍取消确认；连线失败横幅跨文件清理。
**波7 校验+叙事**：TS action 清单已是共享 manifest（P1-32 现网 0 error，已解决）+新增三方 parity 护栏；多入口图孤儿误报（备用入口计入可达）；parallel 双扫（scan_param_refs 只顶层一次）；archive_book_ 前缀吞噬（最长前缀优先+任一通过即接受）；it["id"] 硬索引改 .get 不崩；Python 必填校验对齐 TS 子集；asset audit 补 animManifest/fromImage/toImage/illustration；网页 trim 吃空格；phases 拖排序按视觉序落盘。
**波8 UX+验收**：settings_dialog compact_form+去自赋值；faulthandler stderr 保留；timeline 过场搜索框；全量回归 + 字节透明性双重证明。

**刻意未做（低价值/高回归风险，均不影响数据与运行时）**：node_inspector 7 个潜伏非幂等形状（当前数据不命中、探针绿、仅未来手写 JSON 触发，已由既有 latent 护栏部分覆盖）；project_paths media 绝对路径放行收敛（需重构路径契约）；god-method 结构性拆分；部分 P3 布局/文案 nit。
