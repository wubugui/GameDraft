# 运行时深度审查 — 修复记录（2026-07-03）

**对应审查：** `运行时深度审查-2026-07-02.md`（~130 条发现）
**修复方式：** 按报告第四节 6 大系统性根因分组，9 个批次顺序/并行执行，每批独立验证后合入；全程未提交 git、未做任何临时性绕过修复。
**最终验证门（2026-07-03 00:33）：** `npx tsc --noEmit` 0 错误；`npx vitest run` **50 文件 / 407 用例全绿**（基线 398，新增 9 个锁行为用例）；`./dev.sh validate-data` **0 error** / 31 warning（全为存量内容项）；`asset_reference_audit --strict` issues 0；各批次含真实运行时冒烟（详见下）。

---

## 各批次修复要点

### Wave1-α 叙事/条件/校验核心
- **R7**：5 类条件叶子抽成 trace/非 trace 共享的单一实现，`reached:true` 语义分裂根除（27 处 switch 数据点恢复正确）；热路径零新增分配。
- **R16**：NarrativeStateManager.deserialize 先复位到注册基线再恢复（读旧档不再残留"未来进度"）；补存活实例读档用例。
- **R17+D1**：新建 `src/core/actionParamManifest.ts` 为 TS 侧动作参数唯一权威源（99 个动作全量对照运行时注册建表），校验器改 import；strict 模式对在库内容 0 error。
- **B6**：非法条件 op fail-closed + 去重告警；**B23**：signal 路径迁移提交前复核 from、嵌套排空互斥；**B8**：reactive 迁移订阅 flag:changed / deserialize 后 / ctx 注入时补评（"沉睡"根除）。
- P3：unhandled rejection 路由、destroy 完整化、ScenarioStateManager trim 一致、FlagStore.addNumericFlag（供 addFlagValue 接线）。

### Wave1-β 渲染层
- **R9 渲染侧**：CutsceneRenderer 全面 epoch 化——skip/cleanup 后被放弃的演出函数在每个 await 后判废收束，不再残留图片/空引用崩溃/真实时长拖尾。
- **L7**：blend Mesh 显式释放自建 geometry+shader；**T2**：调试滤镜懒挂载（生产零全屏 pass）；**T3**：depthLog DEV 门控+字面 `\n` 修复。
- **F4**：删除 depthPixels 死负载（数十 MB/场景）与深度图双份解码；删 uFloorA/uFloorB 死 uniform 与 skewX/drape 死解析。
- **F1**：sprite.filters 仅边界切换时赋值；lightEnv merge 改逐层继承；过场几何响应 resize；showImg 同 id 竞态按序号后发覆盖；阴影调参注册表（Game 接线见 η2b）。

### Wave1-γ UI 层
- **R23**：点选穿透（同一次点击跳满打字机）以"事件已消费"标记根除，Dialogue/Encounter 统一。
- **B19** 任务追踪改有序列表（完成回退不清空）；**B20** InspectBox 重入收尾旧会话；**B21** 五处滚轮/滑条统一 rect 换算 helper；**D8** 渲染函数去副作用；**D9** 删 setTimeout 即时重建。
- P3 全修：mask 生命周期、滑条监听清理、fadeIn 可停、wheel 区域判定、空台词推进、DOM 面板来源过滤、DialogueLogUI 增量渲染、NotificationUI 置顶、「调试」chip DEV 门控。
- **L4**：PressureHold/ActionChoice/TouchControls 文案迁 strings.json（字节级契约验证）。

### Wave2-δ 世界系统（模型级修复）
- **R1/R2/R4**：实体显隐拆四通道（派生基底∧条件∧会话覆盖∧!pickedUp）单点合成——拾取复活、NPC 弹回、条件隐藏误记三族问题共根根除；拾取即时入 sceneMemory。
- **R3**：serialize 先 flush 当前场景运行态（存档复制修复）；**R14** onEnter 内切场景改 fire-and-forget 排队（死锁根除）；**R15** setZones 差分更新（不再重放 onExit/onEnter）；**R12** 空选项集优雅结束遭遇；**B10** loadScene 失败回退+移除黑幕；**B11** 发 scene:entitiesRebuilt。
- **R19**：Inventory/Quest deserialize 补发 restored 事件（HUD 读档刷新）；**L2** tether 抑制键提为配置。
- P3：autoTrigger 按范围维护、巡逻零距离早退、SmellSystem/DayManager 重 init 一致、读档清会话态、重建竞态 sceneEpoch、Zone 单遍求值。
- **冒烟**：拾取→连按 E 不复活→原地存档→读档不重生不复制，全链路实测。

### Wave2-ε 小游戏
- **D2+B12**：三 Manager 会话骨架收敛为 `MinigameSessionManagerBase`（重复启动竞态、destroy 恢复状态一并根除）；**B13** 动作播放门（输入锁+状态拉回）泛化到扎纸/挑水。
- **R21** 漂浮物消费语义（无限刷钱堵死）；**R22** 氛围脚本相位优先级（start 播完再接 spinning）；**L3** 内容正则改数据字段；**L5** 滤镜逐局释放；**T5** KeyD 门控；**B14** 加载期校验；**F6** charging 只重画蓄力环；**L4** 30+ 文案键入库。
- **冒烟**：三个小游戏 preview 实玩全过。

### Wave2-ζ 音频/资源
- **B3** addAmbient 代次守卫（快速切场不叠音/不复活）；**B16** LRU 跳过在播音频；**B15** resolveAssetPath 幂等（缓存键分裂根除）；**D5** 场景媒体路径单源化；**F5** Archive 重评微任务批处理+收敛循环；archive 预热接线；气泡迁出滤镜容器+owner 归属清理 API；音量乘数三通道统一；falsy 缓存修复。
- **冒烟**：双场景切换音频链路实测（旧 BGM 正确停止、无 `././` 分裂键）。

### Wave3-η1 对话/演出流
- **R5+R6**：`dialogue:start/end` 带 `source/willContinue/nestedInGraph`，EventBridge 仅在两管理器均不活跃时恢复状态；deferred 链单 runner+最终 end 恰一次——嵌套台词不再打断图对话、链式图不再裸奔 Exploring。
- **R8** 路由环 1000 步护栏；**R10+R9 管理侧** stepEpoch+worldEpoch 双代际（过场中读档不悬死、不回滚快照污染读档态；skip 后被放弃轨道被终止）；**B18** destroy 作废在途 op；**D6** 全禁用选项优雅收束；GDM 存读档对称化（transient 不入档、静默收束）。
- **冒烟**：嵌套链（寻狗_看进山路 n_night）保持 Dialogue 态、过场 parallel 中 Esc 干净收束、过场中读档 ok:true 不悬死。

### Wave3-η2a 状态机/存读档/输入/桥接
- **R11+D3**：GameStateController.closePanel 统一「关面板+弹栈」通道，ruleUse:apply 无 resultText 也恢复状态（软锁根除）；**R18** load 坏档先拒+快照回滚原子性、save/load 返回 boolean+UI 真实反馈、localStorage 全防（+7 测试用例）；**R20** 新游戏=净化 URL 整页重启（与首启同路径零残留）；**B1** 失焦清键；**B2** 订阅者快照遍历；**B4** ui:confirm/cancel 发射接线；**B22** encounter 热点按 encounter:start 事件判定成功才消费；**T4** 切场调试标签 DEV 门控。
- **冒烟**：存读档成功/失败分支、规矩牌全流程状态平衡、失焦清键实测。

### Wave3-η2b Game 装配/动作注册表（26 项）
- **R13** startEncounter 先验证再切状态（软锁根除）；**L1** ActionExecutor 执行策略栈——cutscene 禁改存档在唯一执行入口递归强制（在库内容扫描零受影响）；**B7** 货币金额严格解析+Inventory 守卫；**B17** 序章 doneFlag 写入；addFlagValue 接线登记表校验。
- 全部交接项消化：B8 注入顺序、reloadScene 不盖自动演出、escapeFallback 收敛、scene:entitiesRebuilt 重挂滤镜/巡逻、阴影注册/aoContact/ShadowSource 缓存（F2 性能）、巡逻空转、GameConfig.health、HUD save:restoring、T1 调试门控（DEV-only）、命令通道按 id 定向消费（多开不丢命令）、enterNarrativeWarp BFS、dev 启动就绪信号化、main.ts 生产错误提示、D1 运行时一致性审计（实测零漂移）、参数解析样板收敛。
- **冒烟**：teahouse 完整开场链（信号→过场→对话）零 error、策略栈下 50 步过场零误伤、F2 DEV 可用、7 条命令定向消费不丢。

---

## 明确延后/未修项（非糊弄，均有理由）

| 项 | 理由 |
|---|---|
| 面板开窗时 resize 重排 | 20+ 面板逐个重构，收益低风险面大，延后单独立项 |
| F3 逐实体阴影 BlurFilter 性能 | 需先 profiling 定位真实开销，盲调有视觉回归风险 |
| T6 扎纸结算 debugAlert 占位 | 内容侧待策划填正式结算（设计决策非机械修复） |
| 三小游戏退出确认语义差异 | 与各自消耗风险匹配（已修掉"蓄力中 Esc 反被扣费"真问题），刻意保留 |
| scene:entitiesRebuilt enter 阶段不重启巡逻 | 过场拥有实体动线，语义正确；未来需求另开参数 |

## 跟进项（已立项/建议立项）

- **编辑器侧**：水域实体 `displaySize/hitRadius` GUI 控件；扎纸订单 `paperOptions/finishOptions` 非空强制；场景编辑器 drape/skewX 废弃字段清理（已在另一会话进行）+ `types.ts` SceneLightEnv 同名字段随之删除；`stopSceneAmbient.id` 参数编辑器缺口。
- **内容侧**：`茶馆_自动事件` n_3 空 `next`（运行时 warn 收束）；`wrapper_graph_2` ownerState 归属告警；validate-data 31 条存量 warning（画布黑盒声明漂移等）。
- **契约**：新增 action 三件套 → **四件套**（+ `actionParamManifest.ts`，DEV 启动审计兜底）；`GameConfig.health` 与 `HealthConfig` 结构复制需同步（types.ts 注释已声明）。
