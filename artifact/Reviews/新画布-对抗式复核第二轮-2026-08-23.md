> **处置状态（2026-08-23）**：本文 19 条全部已修，修复与回归见提交
> `fix(scene_v2): 第二轮复核的 19 条 + 五条"有提示没接线"的交互`。
> 复核期间另发现两条本文没列的：`rebuild_all()` 漏建场景级图元（光曲线打开场景
> 就看不见）、面板 flush 与 commit 互相触发导致**进程栈溢出硬崩**。
> 沉淀进 `agent_docs/editor-tools/mechanisms/scene-canvas-v2-document-view-command.md`。



## 1 [high] 面板控件编辑从不落库：桥读的 staging 根本没被控件写过，编辑静默丢失

**tools/editor/editors/scene_v2/panel_bridge.py:177**

### 复现
已实测（offscreen Qt 脚本）：加载场景 → doc.set_selection([EntityRef('hotspot','h1')]) → page._props._hs_label.setText('NEW') → processEvents。结果：staging label 仍是 'OLD'、model label 仍是 'OLD'、undo_stack.count()==0。取消 autoTrigger 勾选同样：page._props._hs_auto.setChecked(False) 后 model['autoTrigger'] 仍为 True。

### 为什么真
老面板绝大多数控件的信号只接到 `_emit_props_changed()`（例：scene_editor.py:7426 `_hs_id.textChanged`、7437 `_hs_label.textChanged`、7473 `_hs_auto.stateChanged`），它们**不写 staging**；staging 只在 `flush_active_panel_widgets_to_staging()`（scene_editor.py:6269）或 `save_*_props()` 里由 `_write_*_widgets_to_dict` 一次性刷新。而 `commit_panel_edits` 直接 `getattr(self._panel, '_staging_hotspot')` 取那份「载入时的深拷贝」（panel_bridge.py:177）再 `diff_fields`，scene_v2 全目录没有任何一处调用过 flush/save（grep 证实）。于是 diff 恒为空、命令永不构造。配合 page.py:449 `flush_to_model` 恒返回 True、page.py:462 `confirm_close` 恒返回 True，用户切页/关窗时连一句提示都没有。现有测试全部是手动往 `_staging_hotspot` 里塞值再调 `commit_panel_edits()`（test_scene_v2_panel_bridge.py:130-134），刚好绕开了这条真实通路。


## 2 [high] 面板 staging 从不随文档变更刷新，下一次面板编辑把画布拖动/撤销结果整份写回旧值

**tools/editor/editors/scene_v2/panel_bridge.py:109**

### 复现
已实测：hotspot h1 (x=100,y=120) 选中（面板已载入）→ move_tool 从 (100,120) 拖到 (150,170)，model 变成 150.0/170.0，而 `_hs_x/_hs_y` 与 staging 仍是 100/120 → 用户把 y 微调框 +1（`_hs_y.setValue(121)`）→ model 变成 x=100.0, y=121.0。x 被从 150 悄悄退回 100，y 也不是 171。撤销后同理：Ctrl+Z 之后面板仍显示撤销前的值。

### 为什么真
`PanelBridge._on_document_changed`（panel_bridge.py:109-113）只处理 `SelectionChanged`，`EntitiesChanged` / `SceneReloaded` 一概不管；`reload_current()`（panel_bridge.py:140，注释里恰好写明「不刷的话面板还显示旧值，用户下一次编辑会把旧值连同新编辑一起写回去，等于把撤销悄悄撤销了」）与 `begin_gesture()`（:160）在整个 tools/editor 下 grep 零调用点，是死代码。page.py:414 `_on_doc_changed` 也没接。触发只需一个确实会写 staging 的控件（`_on_hs_xy_live_refresh`，scene_editor.py:8149 把 `_hs_x/_hs_y` 的**陈旧**值写进 staging），diff_fields 于是把陈旧的 x 当成一次真实变更提交。这正是重建声称已消灭的「两层真相」事故形态。


## 3 [high] duplicate_selected 在「id 与 id_N 同时被选中」时死循环，编辑器卡死

**tools/editor/editors/scene_v2/tools_structure.py:144**

### 复现
已实测（5 秒看门狗触发，进程需强杀）：场景 npcs 为 [{id:'a'},{id:'a_2'}]，两个都选中后调用 duplicate_selected(document) → 永不返回。

### 为什么真
`unique_entity_id` 的 taken 集来自 `existing_ids(document, kind)`，即**模型里现有的 id**；本批新 id 还没入模型。复制 'a' 得 'a_3' 并加入本地 `taken`；复制 'a_2' 时 `unique_entity_id(doc,kind,'a_2')` 按 `^(.*?)_(\d+)$` 拆出 stem='a'、n=3，'a_3' 不在模型里于是返回 'a_3'；进入 `while new_id in taken` 后再调 `unique_entity_id(doc, kind, 'a_3')`，'a_3' 依旧不在模型 taken 里，原样返回 'a_3' —— 循环条件永远为真。tools_structure.py:144-145 这个 while 本身没有推进量，任何「返回值等于入参」的情形都会锁死。库内数据里 `fx_steam_2` / `fx_steam_2_copy_2` 这类 `_N` 后缀 id 大量存在（teahouse.json）。


## 4 [medium] diff_fields 只遍历 staged.items()，面板删键（取消勾选/清空路径）完全看不见

**tools/editor/editors/scene_v2/panel_bridge.py:57**

### 复现
已实测：h1 带 collisionPolygon + collisionPolygonLocal，选中后模拟面板取消「碰撞面」勾选（`_on_hs_collision_toggle` 的行为：staging.pop('collisionPolygon'); staging.pop('collisionPolygonLocal')）→ `commit_panel_edits()` 返回 False，model 里 collisionPolygon 仍在。

### 为什么真
`diff_fields`（panel_bridge.py:47-66）的循环只从 staging 侧出发，模型里有、staging 里已被删掉的键落不进 `out`，命令因此构造不出来。而老面板的写盘器**大量用 pop/del 表达「回落缺省」**：`_on_hs_collision_toggle`（scene_editor.py:8203-8206）、`_sync_hs_display_to_dict_and_refresh`（:8178 `hs.pop("displayImage", None)`）都是即时写 staging 且当场 `_emit_props_changed()`；`_write_hotspot_widgets_to_dict` 里更有 scale/rotation/planes/phases/cutsceneIds/conditions/autoTrigger/castShadow/shadowBindings 十余处 pop/del（scene_editor.py:8834-8927）。这些「关掉一个开关」的编辑在新画布上一律静默不生效，重新选中时勾选状态又弹回来。


## 5 [medium] 整组位移把没有位移的那条轴也重新四舍五入到 1 位小数，篡改未改动的数值

**tools/editor/editors/scene_v2/tools_transform.py:227**

### 复现
已实测：hotspot {x:1308.14, y:218.02, group:'g1'}、npc patrol.route[0]={x:111.26,y:222.37} → translate_group(doc,'g1', 1, 0)（纯水平）→ x=1309.1、**y 由 218.02 变成 218.0**，patrol 点由 (111.26,222.37) 变成 (112.3,222.4)。UI 路径：GroupBoxTool 选中组框后近乎水平地拖一下，tools_overlays.py:195 把 dy 先 `round(dy,1)`，微小竖直位移直接变 0.0，随后每个成员的 y 仍被 `_shift` 重写。

### 为什么真
`translate_group`（tools_transform.py:191-204）对每个成员**无条件**同时写 x 与 y（以及 polygon / patrol 全部顶点），而 `_shift`（:216-229）在 delta 为 0 时也走 `round(out, 1)` 分支——只有 int 原值才被保护，float 原值一律被截断。库内实测 445 个坐标是 float，其中 `城门口.json` 的 `hs_城门口北路` (1308.14, 218.02)、`mountain_pass` 的 (1468.02, 515.99) 等带两位小数。这直接违反本文件注释自己写的「保留原始数值表示」与黄金往返的「未改动的数值键按原始表示回写」。


## 6 [medium] 新画布的碰撞多边形不施加实例 transform，与运行时/老面板口径相反，拖顶点写出错位数据

**tools/editor/editors/scene_v2/view.py:351**

### 复现
已实测：hotspot {x:500,y:500,scale:2.0, collisionPolygon:[(-10,-10),(10,-10),(0,10)] (local)} → 新画布 `view.item_for(ref,'collision').points()` 给出 [(490,490),(510,490),(500,510)]；运行时 `anchorCollisionPolygonToWorld` 与老面板 `_hotspot_collision_local_to_world`（scene_editor.py:232-243，含 scale/rotation 正变换）给出的是 [(480,480),(520,480),(500,520)]。随后用 PolygonEditTool 把某顶点拖到画布上的 (520,480)，写回的 local 是 (20,-20)，运行时会把它渲染/求值到 anchor+2*(20,-20)=(540,460)。

### 为什么真
`SceneView._collision_world_points`（view.py:351-361）只做 `anchor + local`，`PolygonEditTool._write_values` 的 collision 分支（tools_builtin.py:260-264）只做 `world - anchor`，两边互逆但**都漏了 scale/rotation**。shared/scene_migrations.py:52-65 的文档明确区分了这两件事并警告「拿纯平移那支去处理 scale:2 的实体，命中面会缩小一半，而画面完全看不出来」，`collision_polygon_world_to_local`（拖拽写回专用、含反变换）在 scene_v2 里一次都没被引用。当前库里带碰撞面的实体恰好都没写 scale/rotation，所以是潜伏缺陷——任何人给这类热点/NPC 加一次缩放就会静默错位。


## 7 [medium] 面板 x/y 提交把未改动的那一维整数坐标写成浮点，破坏数值表示保真

**tools/editor/editors/scene_v2/panel_bridge.py:64**

### 复现
已实测：hotspot h2 {x:300 (int), y:320 (int)} 选中 → `page._props._hs_x.setValue(301.0)` → commit → 模型变成 x=301.0, **y=320.0**（原本是 int 320）。

### 为什么真
`_on_hs_xy_live_refresh`（scene_editor.py:8149-8150）无条件 `hs['x']=float(...)`、`hs['y']=float(...)`，绕过了老面板专为此写的 `_keep_num`（`_write_hotspot_widgets_to_dict` 里才有）。`diff_fields` 又刻意把 int/float 表示差异判为变更（panel_bridge.py:64 `type(old) is not type(value)`），于是没被用户碰过的 y 也被当成一次真实变更提交。库内现存 11 个 int 坐标，改一次 x 就把同一实体的 y 一起漂成 `.0`，diff 里多出一行、黄金往返红。


## 8 [high] load_scene() leaks the old PanelBridge, which stays connected to the shared property panel and to a deleted document

**tools/editor/editors/scene_v2/page.py:164**

### 复现
SceneEditorV2(model); load_scene(A); doc.set_selection([EntityRef('hotspot','h1')]) (bridge._loaded is now set); then either load_scene(B) or reload_from_model() (main_window.py:1369-1371 calls the latter on every switch back to the scene page); flush DeferredDelete; then edit anything in the property panel (page._props._staging_hotspot['x']=555; page._props.changed.emit()). The stale bridge's _on_panel_changed runs and raises: RuntimeError: libshiboken: Internal C++ object (PySide6.QtGui.QUndoStack) already deleted -- panel_bridge.py:189 -> document.py:207. Bridge count measured after 5 reload_from_model() calls: panel.changed receivers 1 -> 3 -> 5 -> 7 -> 9 -> 11. In the window before deleteLater is flushed the old doc is still alive and no exception is raised: editing SceneB's hotspot 'h1' instead wrote x=555 into SceneA's hotspot 'h1' and marked SceneA dirty.

### 为什么真
PanelBridge is constructed with parent=self (the page), not the document, so Qt ownership keeps every previous bridge alive for the life of the page; nothing disconnects panel.changed (panel_bridge.py:93) and nothing disconnects document.changed. Each stale bridge still holds _loaded (an EntityRef into the previous scene) and _doc (the SceneDocument that page.py:162 deleteLater()'d). commit_panel_edits() is pure Python up to document.push(), so it happily builds a command against the old scene's dict before hitting the dead QUndoStack. Because reload_from_model() loads twice, the number of stale bridges grows by 2 per tab visit, so the failure count grows without bound.


## 9 [high] Rubber-band release calls pick_rect() on content items, which do not have it

**tools/editor/editors/scene_v2/tools_builtin.py:119**

### 复现
Load any scene containing a hotspot with a displayImage (or an NPC whose animFile resolves), so the view ledger holds a DisplayImageItem/SpritePreviewItem. Then drag a rubber band on empty canvas: select_tool.mouse_pressed(QPointF(500,500), LeftButton, NoModifier); mouse_moved(QPointF(10,10), ...); mouse_released(QPointF(10,10), ...) -> AttributeError: 'DisplayImageItem' object has no attribute 'pick_rect'. 9 shipped scenes under public/assets/scenes/ contain displayImage.

### 为什么真
SelectTool.mouse_released iterates self._view.entity_items(), which returns the entire (ref, part) ledger including content parts. DisplayImageItem/SpritePreviewItem derive from CanvasItem (content_items.py:76), not EntityItem, and pick_rect is only defined on EntityItem (items.py:111). The press path is safe because it goes through AbstractTool.entities_at()'s isinstance(it, EntityItem) whitelist; the release path bypasses that whitelist entirely, so it both crashes and would have selected content items' refs.


## 10 [low] reload_from_model() loads the scene twice, doubling document/view/bridge churn per tab switch

**tools/editor/editors/scene_v2/page.py:478**

### 复现
page.reload_from_model() -> refresh_scene_list() reads current_scene_id at page.py:131 and ends with load_scene(current) at page.py:139-140; then page.py:480 calls load_scene(sid) again. Instrumenting shows two SceneDocument + two SceneView + two PanelBridge constructions per call (bridge count grows by exactly 2 per reload_from_model, measured 1->3->5->7->9->11).

### 为什么真
main_window.py:1369-1371 invokes reload_from_model() on every switch back to a scene page, so each tab visit tears down and rebuilds the whole QGraphicsScene twice and doubles the leak rate of finding 1. The second load also discards the selection the first load just established, which is why reload_from_model has to restore it manually afterwards.


## 11 [low] Advertised canvas interactions have no caller: rubber-band overlay, live drag/transform preview, right-click vertex delete, arrow-key nudge, delete/duplicate

**tools/editor/editors/scene_v2/view.py:288**

### 复现
grep across tools/editor: SceneView.rubber_band and SelectTool.band_rect are never connected, so RubberBandItem.set_rect is never called and the band is invisible during framing. MoveTool.drag_offset, TransformTool.preview, GroupMoveTool.offset and GroupBoxTool.offset are never read by the view, so drags have no live preview. PolygonEditTool.delete_vertex_at, GroupBoxTool.nudge, GroupMoveTool.nudge, page.delete_selected and page.duplicate_selected have no caller outside tools/editor/tests/.

### 为什么真
The tools' status_hint strings shown in the status bar promise all of these ('拖拽空白处框选', '右键顶点删除', '方向键微移'), and SceneView.keyPressEvent forwards to ToolManager.key_pressed whose base implementation only handles Escape. The page installs no QShortcut/QAction for delete or duplicate and never forwards right-click to delete_vertex_at, so these paths are reachable only from unit tests -- the same 'hint written, code not wired' failure mode the module docstring in tools.py calls out for the old canvas.


## 12 [high] 新画布加载带光曲线的真实场景直接抛 ValueError（lightEnvCurve 是 {"points": [...]} 而不是 list）

**tools/editor/editors/scene_v2/view.py:341**

### 复现
跑 repro2.py 的 F 段：读 public/assets/scenes/梦_夜路.json（lightEnvCurve 为 dict，keys=['points']），构造 SceneDocument+SceneView 后调 view._sync_entity(EntityRef('scene','s'))（等价于 page.refresh_scene_geometry()，load_scene 里必调）→ ValueError: could not convert string to float: 'p'。_part_points 把 dict 原样返回，_PointsItem.set_points 迭代 dict 得到字符串 'points'，再取 p[0]='p' 转 float。

### 为什么真
老画布属性面板写盘格式是 sc['lightEnvCurve'] = {'points': lc_pts}（scene_editor.py:6471），读取 _load_lightcurve 也只认 dict+points（:6987-6999）。仓库里 梦_夜路.json 就是这个形状。写回方向同样错：tools_builtin.py:290 的 _write_values 返回 {'lightEnvCurve': [...]} 裸列表，老面板读它会静默当成空曲线，属于数据丢失。


## 13 [high] 橡皮筋框选在任何有展示图/NPC 精灵的场景里抛 AttributeError

**tools/editor/editors/scene_v2/tools_builtin.py:119**

### 复现
跑 repro.py 的 A 段：场景里给一个热点配 displayImage（image 非空、worldWidth/Height>0），SelectTool 依次 mouse_pressed(5,5) / mouse_moved(790,590) / mouse_released(790,590) → AttributeError: 'DisplayImageItem' object has no attribute 'pick_rect'。

### 为什么真
mouse_released 里的推导式直接遍历 view.entity_items()（= list(self._items.values())），而 _sync_content_part（view.py:223）把 DisplayImageItem / SpritePreviewItem 也登记进同一本账。这两类继承 CanvasItem，不是 EntityItem，没有 pick_rect。同文件 _hits 走的是 entities_at()，那里有 isinstance(EntityItem) 白名单；释放这条路漏了同一道闸。现有用例 test_scene_v2_tools.py 的 _scene() 既没有 displayImage 也没有 animFile，所以永远走不到。


## 14 [medium] 出生点被位面轴过滤掉：切到 exclusive 位面视图后所有 spawn 从画布消失

**tools/editor/editors/scene_v2/page.py:321**

### 复现
跑 repro2.py 的 G 段：场景含 spawnPoint 与 spawnPoints.north，set_presence_filter(lambda ref,ent: passes_view_filters(ref.kind, ent, ViewAxes(plane_id='dream', plane_exclusive=True))) 后，spawn:default/handle 与 spawn:north/handle 的 isVisible() 都是 False。spawn dict 没有 planes → passes_plane 落到 `return not axes.plane_exclusive`（scene_view_filters.py:93）→ False。

### 为什么真
老画布明确不让出生点吃位面/时段轴：add_spawn（scene_editor.py:3186-3191）从不调 _record_entity_view，refresh_entity_presence（:3418-3432）对未登记实体直接 return，注释写死「未登记的实体（如出生点，本来就不吃位面/时段轴）直接跳过」。新画布的谓词对所有 ref 一视同仁，语义反了。


## 15 [medium] Zone 整体平移丢失：选中 Zone 拖动是空操作，老画布可以拖内部平移整条多边形

**tools/editor/editors/scene_v2/tools_builtin.py:174**

### 复现
跑 repro.py 的 C 段：doc.set_selection([EntityRef('zone','z1')]) 后 MoveTool.mouse_pressed(QPointF(650,150), LeftButton, NoModifier) 返回 False（movable 为空，zone 没有 x/y 键），手势根本没起来，后续 move/release 也不会产生命令。

### 为什么真
老画布 _EditableZonePolygon 支持 _drag_body（scene_editor.py:1119-1126），press 时 _point_in_polygon 命中就整条点列跟着鼠标平移，release 提交（类 docstring :840 写明「Zone：世界坐标闭合多边形；拖顶点、拖内部平移…」）。新画布 MoveTool 只认有 x/y 的实体，PolygonEditTool 只处理单顶点，两边都没接管，能力整块消失。


## 16 [medium] 多边形命中用 AABB 而不是多边形形状，且装饰层 z 全部并列导致 Zone 抢走首次点击

**tools/editor/editors/scene_v2/items.py:117**

### 复现
跑 repro.py 的 B/B2 段：zone 三角形 (600,100),(800,100),(600,300)。B2 在 (790,290)——明显在三角形外、但在包围盒内——SelectTool._hits 返回 [zone:z1]，点下去选中 zone。B 在 (650,150) 处 hits 依次是 zone:z1(PolygonItem,z=300000)、hotspot:h_broken(HandleItem,z=300000)、hotspot:h_inzone(HandleItem,z=300000)，三者 z 完全相等，稳定排序后 hits[0] 恒为 zone。

### 为什么真
EntityItem.pick_rect 回落 boundingRect()，_PointsItem.boundingRect（entity_items.py:159-166）是点列 AABB + pad。老画布 _EditableZonePolygon.shape()（scene_editor.py:981-990）是真多边形路径 + 顶点圆，包围盒外的点根本不命中。叠加 Z_DECOR_BASE 单一常量（items.py:47/97）让 entities_at 的 z 排序退化成 _items 插入序（rebuild_all 的 zone→hotspot→npc→spawn），大而不规则的 Zone 会在整个包围盒范围内吃掉首次点选。


## 17 [medium] 场景光曲线无条件排在 _target_parts 最前，抢走选中实体的顶点拖动

**tools/editor/editors/scene_v2/tools_builtin.py:293**

### 复现
跑 repro2.py 的 H 段：zone z1 顶点 (100,100)，场景光曲线控制点 (101,101)。选中 zone 后 PolygonEditTool.mouse_pressed(QPointF(100,100)) → _target_parts 顺序为 [('scene','s','lightcurve'), ('zone','z1','polygon')]，抓到的是 scene/lightcurve 的 vertex 0；拖到 (150,150) 释放后 zone.polygon 原封不动，lightEnvCurve[0] 变成 (150,150)。

### 为什么真
命中半径是 10 屏幕像素（renderer.HANDLE_PICK_PX），视图缩小后换算成的世界半径成比例放大，抢占范围随之扩大。_target_parts 既不检查曲线图元的 isVisible()，也没有「选中的实体优先」这一层，delete_vertex_at / mouse_double_clicked 走同一个生成器，右键删点与双击插点也会先落到曲线上。


## 18 [medium] 缺件 displayImage 的热点把手被派进内容层 z，沉到别的热点贴图底下

**tools/editor/editors/scene_v2/sorting.py:40**

### 复现
跑 repro.py 的 D 段：热点 h_broken 配 displayImage={'image':'','worldWidth':0,'worldHeight':0} → _content_spec 返回 None（view.py:270），没有 display 图元。content_sort_entries 回落到 item_for(ref,'handle')，而 :44-46 的 displayImage 闸只判 dict 非空 → 进入排序。assign_content_z 后 h_broken/handle 的 zValue 从 300000.0 变成 -100000.0，而同场景 h_disp/display 是 -99999.0，把手排在别人贴图下面。

### 为什么真
回落分支还顺带弄错档位：texture_loaded = getattr(item,'texture_loaded',True) 对 HandleItem 恒为 True，而运行时此刻没有 displaySprite、根本不给档位（content_items.py 模块文档自己写了这条口径）。结果既让把手看不见、又让它在 entities_at 的 z 排序里掉到最后，与运行时次序也不一致。NPC 那一支（sorting.py:61）没有回落，只有热点这条有。


## 19 [low] 右键删顶点没有接线：状态栏提示写了，代码路径不存在

**tools/editor/editors/scene_v2/tools_builtin.py:367**

### 复现
跑 repro2.py 的 I 段：PolygonEditTool.mouse_pressed(pos, RightButton, NoModifier) 返回 False；'contextMenuEvent' not in SceneView.__dict__。全仓 grep delete_vertex_at 只有定义处与 .pyc，没有任何调用方。

### 为什么真
tools_builtin.py:228 的 status_hint 写着「右键顶点删除」，SceneView 的输入转发（view.py:446-473）只有 press/move/release/doubleclick/key/wheel，右键按下会被 PolygonEditTool 直接拒绝、落回 QGraphicsView 默认行为。这正是该模块 docstring 指责老画布的那种「提示写了、代码没接」——老画布至少还有 Shift+点顶点删除（scene_editor.py:1095-1108）与 contextMenuEvent（:1207/:1429/:1760）两条真实路径。
