# 叙事状态机全面审查 — 修复记录（2026-07-17，与审查同日完成）

> 对应问题清单：[叙事状态机全面审查-2026-07-17.md](叙事状态机全面审查-2026-07-17.md)。本记录按块列出每条修复的落点、验证与刻意不修项。**全部代码 bug 已修；内容数据类清理与纯文档项按下方"刻意不修/移交"处理。**

## 验收门（全过）

| 门 | 结果 |
|---|---|
| `npx tsc --noEmit` | 0 error |
| 全量 vitest（`npm test` 口径） | 289 passed / 1 skipped（36 文件）+ anim-preview 4 passed |
| 编辑器 Python 全量 | **716 passed**（tools/editor/tests 全套，offscreen） |
| 叙事 web vitest + `build:narrative-editor` + `typecheck:narrative-editor` | 118 passed / dist 已重建 / 0 error |
| `./dev.sh validate-data` | exit 0 / 0 error / **53 warning（=基线，未新增）** |
| 素材引用审计 `--strict` | issues: 0 |
| 叙事编辑器字节幂等探针 | `_json_text(_normalize_file(load(disk))) == disk` True（删 auto-mark 后复验） |
| `git diff --check` | 干净 |
| **真机验证**（runtime norms 义务；本会话独立 5178 实例 + 页内 `__game` 直读） | 启动 49 图加载 ✓ / `isIdle()`=true ✓ / Exploring 下 canSave()=true ✓ / emit `tingshu_kicked` → 听书 kicked_out + 主线 s01_tingshu ✓ / save(2)→推进 beishi_hired→load(2)：背尸回滚 not_started、主线保持 s01、idle=true 无楔死 ✓ / localStorage 测后还原 |

## 运行时（NarrativeStateManager.ts + Game.ts）

- **R3 存档丢在飞信号**：新增 `isIdle()`（queue 空 + 无在飞排空/嵌套/动作深度）；`Game.ts` canSave 谓词追加叙事空闲判定——级联在飞时存档按钮拒绝，半态档从入口杜绝。
- **R2 旧时间线污染**：新增 `generation` 代际 + `invalidateInFlightTimeline()`（reject 积压队列、resolve 已完成项）；挂三处——`deserialize` 开头、`registerGraphs` 开头（顺带修 **W2** 的静默清队列）、`save:restoring` 事件钩子（老档缺 narrative 条目的兜底，构造/destroy 对称接线）。`enterState` 在 onExit/onEnter 两个 await 后比对代际，不一致即放弃置态/广播；`processTrigger` 逐图 await 间同样比对。
- **W1 排空异常悬挂**：`drainAvailableQueue` 异常路径补 `resolveCompletedQueueItems()`；`conditionsMet` 对 ctx 工厂调用套 try/catch（新 issue 码 `condition.ctxFactory.threw`，保守拒绝守卫迁移）。
- **回归测试**：NarrativeStateManager.test.ts 新增 6 条（R2 读档隔离 / R2 换册隔离+W2 reject / R3 isIdle 门 / W1 工厂抛错不楔死 / save:restoring 钩子 / W5 保留信号校验）。审查期的三条临时实证测试（R1/R2/R3）语义已固化进上述回归（断言反转为"修复后行为"）。

## 编辑器 Python 壳（narrative_state_editor.py）

- **P-F2 auto-mark 删除**：`_apply_derived_broadcast_auto_mark` 整段删除+调用点移除，normalize 处补"刻意不代写"注释——对齐 2026-07-16 用户裁决与 web 侧既删行为；`state.broadcast.missing`（error）从死代码复活，agent 直写未勾广播的数据现在被正确拦下而非静默"修好"。
- **P-F1 plane 叶子**：`_is_condition_shape` 补 plane 分支（非空即合法，空串仍错=与 TS 同级）；quest/scenario/scenarioLine 缺伴随字段改为与 TS 同口径静默容忍（键存在即认形状）。
- **P-F4 activePlane 校验**：`_validate_graph` 补形状检查（`state.activePlane.invalid`，error）；`validate_project_context` 新增 `_validate_active_plane_refs`（对照 `model.all_plane_ids()` 报 `state.activePlane.unknown`，error；planes.json 缺失时跳过=与 TS 不传目录同语义）。
- **P-F5 模板重复 id**：`saveTemplates` 改为**先对归一前原始输入校验**再归一（撞名检测按其设计走原始输入），重复模板 id 现在 ok:false 拦下而非静默丢第二份。
- **P-F6 schemaVersion 防护**：新增 `_coerce_schema_version`（垃圾值按 0 走迁移，绝不抛）；`saveData` 的 normalize+校验段套 try/except 返回 `save blocked: internal error:`（协议前缀兼容），杜绝 Qt slot 抛异常返回空串被网页当假成功。
- **P-F8/P-F9 对齐**：scenarioSubgraph 缺 graph 不再报 error（TS 无此 issue）；wrapper ownerType 为空跳过 warning（TS `el.ownerType &&` 同口径）。
- **回归测试**：test_narrative_state_editor.py 新增 10 条（52→62 通过）。

## 重构引擎+发射目录（signal_refactor.py + narrative_catalog.py）

- **P-F3 archive_lore**：`EMIT_SOURCE_BUCKETS` 与 `_EMIT_SOURCE_ATTRS` 双表补登记（从 CONDITION_EXTRA_SOURCES 移入发射面；见闻 firstViewActions=LoreBookUI 真实执行面，对齐 emitted-signal-catalog 卡"archive_*"口径）；既有 parity 测试自动锁定。
- **W-E4 改名级联三缺口**：新增 `_rewrite_meta_state_refs`/`_rewrite_meta_graph_refs`（元素 `meta.commands` 的"图.状态"/"图:状态"/裸图引用 + `meta.emits` 派生信号声明，scan 计数与 rename 改写全接入，结果报 `metaCommands`/`metaEmits`）；`@owner/@scene` 相对叶子新增 `relativeTokenSuspects` 疑点报告（state 同名即报、**只报不改**——归属图静态不可知，兑现了 docstring 里"扫描单独报出"的空头承诺）。
- **回归测试**：test_signal_refactor.py 新增 4 条（14→18 通过）。

## Web 编辑器（tools/narrative_editor_web，dist 已重建）

- **W-E2 盖章撤销击穿**：`handleStamped` 补 `resetHistory()`（与 adoptRefactoredNarrative 同款跨文件保护），Ctrl+Z 无法再把画布叙事回退到盖章前而留下劈叉三件套。
- **W-E3 撤销合并端点**：合并逻辑抽成纯函数 `pushSnapshotWithMergeWindow`（合并窗口内**不动栈顶**，保留连发起点前快照）+ 新测试文件 4 条锁语义；`wrapUpdater` 改为 updater 先跑、成功才推快照（异常不再留空转撤销步）。
- **W-E6/W-E7 默认键注入**：broadcastOnEnter 取消勾选改删键；策划备注清空改删键；`createState` 不播 `label:id`、`createTransition` 不播 `priority:0`、`createComposition`/`createElement` 不播 label==id 状态标签与 `category:''`。
- **W-E9 信号行保留**：`normalizeAuthorSignals` 不再删空 id/保留字行（只 trim），交 `signal.id.empty/reserved/duplicate` 三个 error 报——web 侧两个不可达校验码复活、与 Python 侧行为收敛；目录/选择器对保留字已有独立过滤不受影响。测试断言已反转。

## 校验增强（三处登记面）

- **W5 保留前缀发射拦截**：TS `validateActionDef` 新增 `action.signal.reserved`（error）；Python `_validate_action_def` 同码同级；项目级 `validator.py` 把旧的"state:* 直接跳过"盲区改为显式 error。现网内容 0 违规（升级前全量验证）。
- **P2-5 模板占位符隔离升 error**：`template.param.undeclared`（存模板）与 `stamp.placeholder.unknown`（盖章，进 errors 禁盖）双双从 warning 升 error——字面 `{{name}}` 再也进不了运行时数据。现有 1 个模板 0 issue（升级前验证）。

## 刻意不修/移交（均有据）

| 项 | 处置 | 理由 |
|---|---|---|
| P1-3 主线里程碑 reactive 兜底 | 不做 | 属内容数据改动非 bug；R3 已在根因（canSave 门+读档清队列）闭合，冗余兜底徒增主线 22 条迁移噪声。要做属内容侧决策。 |
| R1 顺序反转改 FIFO | 不改代码 | 已实证为现行语义、当前内容零暴露；改运行时排队语义风险远大于收益。已写 inbox 记录建议入 narrative-signal-spine 卡（治理流程归卡）。 |
| W3 plane 手动覆盖唤醒 / 新叶子唤醒义务 | 移交治理 | 现网 reactive 条件 100% narrative 叶子，零现实影响；属规范条款新增，走 inbox→治理。 |
| W4 prod 加载失败静默空跑 | 不改 | 是否"prod 致命化"是产品决策（现行 norms 默认 prod 容错留痕）；单方面改 prod 崩溃语义越权。 |
| 死代码 canLeaveGraphRemotely / enqueueTriggerKey | 不删 | `godot_port/tools/audit_architecture_parity.py` 把两符号登记在跨端架构对齐清单——删除牵动 Godot 移植 parity，非本轮范围。 |
| W-E8 web normalize 结构默认键注入收敛 | 不改 | 改 normalize 注入面波及全 UI 假设+需同步迁移现网数据保幂等，风险/收益失衡；现实影响限于"最小 agent JSON 首次打开+保存"。已记录在审查报告，规模化前专项做。 |
| P-F7 目录不读暂存面 | 不改 | emitted-signal-catalog 卡已把"逐盘读、暂存不进目录、落盘自愈"记为已知特性（非缺陷）。 |
| W-E10 删状态静默改指 initialState | 不改 | 跨图残引有 error 校验兜底，图内起点重指是既有交互设计；审查报告留档备查。 |
| 数据清理（death_tether 悬垂发射 / quest_return_ring.completed 死状态） | 不动 | death_tether=记忆确认的保留设计钩子；死状态无任何运行时危害，内容数据"只能由用户改"（2026-07-16 裁决精神）。 |

## 改动文件清单（本轮新增改动，不含分支既有）

运行时：`src/core/NarrativeStateManager.ts`、`src/core/Game.ts`（canSave 谓词）、`src/core/narrativeGraphValidation.ts`（action.signal.reserved）、`src/core/NarrativeStateManager.test.ts`（+7 测试）
编辑器 Python：`tools/editor/editors/narrative_state_editor.py`、`tools/editor/validator.py`、`tools/editor/shared/signal_refactor.py`、`tools/editor/shared/narrative_catalog.py`、`tools/editor/shared/narrative_templates.py`、`tools/editor/tests/test_narrative_state_editor.py`（+10）、`tools/editor/tests/test_signal_refactor.py`（+4）
编辑器 Web：`tools/narrative_editor_web/src/NarrativeEditorApp.tsx`、`src/editorModel.ts`、`src/editorModel.test.ts`、`src/hooks/useEditorHistory.ts`、`src/hooks/useEditorHistory.test.ts`（新）；`dist` 已重建
知识库：`agent_docs/_meta/inbox/` 偏差记录 2 条（auto-mark 半落地已修结案 / R1+W3 卡片条目候选）
