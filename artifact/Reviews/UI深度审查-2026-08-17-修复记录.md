# UI 深度审查 · 修复记录（2026-08-17）

> 对照 [UI深度审查-现状盘点-2026-08-17.md](UI深度审查-现状盘点-2026-08-17.md) 的批0~批5 路线**全量落地**。
> 本文是修了什么的账；验证门结果见文末。执行形态：主会话 + 6 个子代理分工，全程只走既定机制通道。

## 批0 · 发行阻断

- `main.ts`：`?mode=dev` 及整个 dev 直达参数族（devScene/narrativeWarp/play_cutscene/各预览/visualCapture）叠 `import.meta.env.DEV` 门控——生产构建玩家改 URL 拿不到任何直达通道。F2 调试坞经查已有 DEV 门控（Game.ts T1），无需再修。

## 批1 · 导航救急

- **1a HUD 常驻入口（P0 第一刀）**：右下角 8 钮入口条（任务/行囊/规矩/对话录/书架/舆图/用规矩/菜单），图标+键帽即键位图例，悬停出名签；触屏端不建（与 TouchMobileControls 判据共用互斥）。铜钱芯片→行囊、任务芯片→任务面板可点。配套新增 6 枚民俗白剪影图标（戒尺/折扇/铜镜/指示手/线团/广锁，LibTV 批产，风格锚定现役图标，复现材料 `tmp/ui_assets_2026-08-17_icons/`）。任务芯片折行不再压三把阳火（metaColumn 动态让位）。
- **1b Esc 语义统一为退一层**：`ToggleablePanel.handleEscapeStep` 钩子 + 真实打开顺序栈（替掉注册序倒扫）。菜单子页退回进入页、书架子册退回书架、阅读器轶闻退回本章；对话/遭遇态放行对话录（KeyL）与暂停菜单；过场 Esc 改二次确认（3s 窗口 + 系统级 toast 提示，电影化静默豁免通道）。
- **1c 确认框**：新组件 `UIConfirmDialog`（模态捕获吞键、罩层点击=取消、默认焦点=取消、键盘可达）。接入五条不可逆路径：存档覆盖、游戏中读档、返回主菜单、丢弃物件、规矩施放。
- **1d 三处文案与交互打架**：任务面板「搁置·点击追踪」死承诺改为纯状态词；HUD 亮 [G] 按 G 静默改为 openGuard 拒绝时出提示；触屏「铺子」死 chip 删除（shop 只由世界交互拉起）。
- **1e 键盘导航补齐**：MenuUI 五页（含设置滑条左右步进）与 InventoryUI 网格接 UIFocus——全站 13 面板键盘全覆盖，纯鼠标面板清零。

## 批2 · 令牌与层级收敛

- **2a 灰域收敛**：14 档近邻暖灰 → 5 档语义灰（body/bodyMuted/descText/hintMid/disabled），16 个旧名删除、全仓调用点迁移；`link`→琥珀族 goldDim；补 `lineHeight` 倍率令牌与 `letterSpacing.hint`（9 处硬编码收编）。
- **2b 册子正文分层（"文字没有重点"最实锤处）**：五本册子 buildDetail 从拍平字符串改为结构化块——释义/例句（引文声部）/来源（弱化）/批注（次声部）/集齐评语（分隔线后）各归其位；见闻录分类从行首前缀统一为分组表头。
- **2c 顶中通道仲裁**：`UITheme.topLanes` 单点车道表（场景名 10 / 引导 44 / 横幅 96 / toast 160），toast 车道移到横幅之下永不上侵；`z.banner` 档内优先级；**电影化静默**——过场中 toast/拾取回执一律压队、结束补冒（priority='system' 豁免）；toast 视觉件收敛为 `UIToast` 单实现。
- **2d-f**：存档槽两级排版（场景名+第N天 / 时间+时长副行）；地图六语义图例（样例复用绘制函数零第二份颜色）；任务/规矩空态中央指路块；页签带计数；检视框可选对象名标题（热区 label 接线）。

## 批3 · 两件新基建

- **3a RichContent v2（图文排版 + 可点词条共用地基）**：run 级版式引擎——块级（标题/正文/引文/图 inline·wide·full 三档/图注/分隔线；空行分段）+ 行内（[c:] 染色 run、中文逐字断行、行首行尾禁则、词级 span 几何 + 命中）。**纸页皮肤**（SKINS.paper/paperPage 亮底 + paperInk 墨字色板 + [c:] 纸面替换映射）上到四册右栏与成书内页；裸 `<` 降级地雷在 run 路径天然不存在。册面首开自动选中首条（右栏不再 60% 全黑）；插图异步到位不弹顶；成书上一章/下一章页脚导航；伪斜体清除。
- **3b 交互原语与滚动**：`UIListRow`（tap 激活+拖滚让路+消费标记+焦点铺光 90ms 淡入全内建）回收档案册/阅读器手搭行；UIScrollView 内容区拖拽滚动+惯性（触屏不再只能抓 4px 条）；输入路由收敛为三层模型（GameStateController=面板键路由 / UIFocus=面板内容导航 / 模态捕获=确认框），已记录待收编机制卡。
- **3c 人物簿头像**：CharacterEntry.portrait 字段四登记面同改（类型+渲染+编辑器下拉选择器+validator 资产对账），8 人实配 6 个立绘（calm 帧全部在盘核实）。

## 批4 · 线索系统（玩法需求清单新增 K7 节）

- `[clue:id]` 第三层文本标记（与 [tag:]/[c:] 并列，同一分词器；strip 路径剥净）；`clues.json` 注册表（含种子线索 houshan_sangeng，已标进见闻录城隍庙后山条目）；`ClueManager`（真相=flag `clue_<id>`，不自持久化；幂等采集；苔绿回执 toast）。
- 交互（方案A 静态册面先行）：词条苔绿、悬停琥珀下划线、点击闪金采集、已采转暗金常驻；对话面按 K7 二阶段规划暂只染色不可点。
- **线索簿**＝书架第七本（只列已采集、不给总数不留灰槽——存在即剧透，K7 拍板）。
- 登记面：collectClue 动作四件套 + json_lang 第五面（clues 宇宙）+ [clue:] 全库校验（未知 id=error 拦保存）+ `clue_` flag 前缀入 flag_registry（存档存续硬化，读档零告警）。

## 批5 · 打磨与平台

- 关场动画：UIWindow.fadeOutAndDestroy + 8 个面板 close 路径接入（重建路径保持瞬时）；行高亮 motion.fast 淡入。
- 场景名进场 4s 后淡出（不再永久与场景抢戏）；触屏不再显示键盘键帽（HUD 提示带/长按提示分平台话术）。
- ellipsize 双份收编 `styledText.ellipsizeStyledText`；Quest 自绘关闭键帽**保留**（经查它比 UIWindow 内建版多键盘焦点，回收反而退化——反向收编记录在案）。

## 明示未修（各有立项记录）

1. **systems 层 6 处 import ui**（三小游戏场景/检视场景/水面板）：UI 形代码放错层，硬改注入=三个小游戏回归风险，已记 inbox 立项单独搬迁。
2. **立绘 360 vs 定稿 240 注释矛盾**：未擅改运行时，待制作人对账（inbox 有记录）。
3. **对话面词条可点**（K7 二阶段，方案B 文本度量命中层）：按需求清单规划实施。
4. **主线叙事测试 4 条存量红**（HEAD 即红，`state_1` vs `initial`）与 anim_preview 两个空测试套件：非本轮引入，inbox 已报叙事域对账。
5. **DVC**：新增 6 枚图标在 `public/resources/runtime/images/ui/icons/`，需照惯例 `dvc add public/resources/runtime` 后随 chore(dvc) 提交。

## 验证门（终验）

| 门 | 结果 | 口径 |
|---|---|---|
| npx tsc --noEmit | **PASS** 零错误 | 绝对零 |
| npx vitest run | **PASS** 816 通过（+2 新增线索层测试），失败 4 文件全为存量基线（主线叙事×2 `state_1≠initial`、anim_preview×2 空套件；HEAD 干净检出同红，inbox 已报叙事域对账） | 存量基线外零失败 |
| validate-data | **PASS** 96 error / 149 warning，与开工前基线逐数相同（delta 0/0；存量为共享工作树其它会话的内容基线） | delta ≤ 0 |
| 素材存在性审计 --strict | **PASS** 833 媒体 + 165 文本字段 + 18 地图场景，0 issues | 绝对零 |
| git diff --check | **PASS** 干净 | 绝对零 |
| 编辑器 pytest（.tools/venv） | **PASS** 1424 通过；6 个失败全为 inbox 在案的 Windows 环境性存量 | 新失败零 |

### headless 实机行为验证（8 项 + 14 张全分辨率截图 `tmp/ui_review_2026-08-17/after/`）

HUD 入口条开关 / Esc 逐层回退七步链 / 册面纸页+自动选中 / 线索采集端到端（词条点击→flag→回执→线索簿带未读点）/ 丢弃确认框 / 存档槽两级排版 / 地图图例 / toast 车道 y=160 不压引导条——**全部 PASS**。

实机验证抓获并当场修复两个缺陷（修复后复验通过）：
1. **P0 桌面端启动崩溃**：HUD 构造器里入口条构建早于场景名文本创建，`layout()` 读未初始化字段——构建挪后 + layout 判空防御。此洞恰好证明了「行为改动必须真机验证」：tsc/vitest 全绿它照崩。
2. **P2 确认框 Esc 双消费**：确认框与 InputManager 同挂 window 同相竞争，Esc 一键连关框和底下面板——GameStateController 增 setKeySuppressor 钩子，模态在场整帧让路。

另两条实机观察（未修，已记录）：dev 裸启动 Dev Mode 面板默认盖屏（自动化取景需先关，属 dev 工作流）；`ws://127.0.0.1:5211` 调试桥断连重试无退避刷屏（dev-only，建议补退避）。
