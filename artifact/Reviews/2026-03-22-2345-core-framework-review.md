# 架构审查报告

**审查时间戳：** 2026-03-22 23:45

## 文档同步说明

- 逐项核对：目录结构、系统列表（含 EmoteBubbleManager 辅助模块说明）、事件清单（已无 `rule:used`）、UI 面板表、配置驱动清单、ActionExecutor 动作表、四个 core 提取模块描述。
- **全部一致，无需同步。**

## 问题列表

### 1. 耦合

- 未发现。

### 2. 过度设计

- 未发现。

### 3. 分层违反

- 未发现。

### 4. 生命周期与销毁

- [x] **位置**：`src/systems/EmoteBubbleManager.ts` L47。`showAndWait()` 中 `setTimeout(resolve, durationMs)` 未追踪，`destroy()` → `cleanup()` 无法取消该定时器。**已修复**：添加 `pendingTimers` Set 追踪定时器 ID，`cleanup()` 中统一 `clearTimeout` 并清空集合。

### 5. 数据驱动与配置驱动

- 未发现。

### 6. 统一动作执行

- 未发现。

### 7. 接口与约定

- 未发现。

### 8. 其他

- 未发现循环依赖、全局单例滥用或职责混杂问题。
- 事件命名与文档完全一致。
- DebugPanelUI 保留硬编码英文/中文文案属于调试工具，非玩家功能，不计入问题。

## 下一步

当前仅完成审查与报告，未做任何代码修改。如需修复请回复「开始修复」。
