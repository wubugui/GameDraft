# 主编辑器 对抗验证 + 收口修复报告 — 2026-07-14

> 承接 [主编辑器全面审查-2026-07-14.md](主编辑器全面审查-2026-07-14.md)(2 P0+19 P1+P2/P3 已修)与 [防再犯归纳](主编辑器-防再犯归纳-2026-07-14.md)。
> 本文记录:①对第一轮修复的**对抗验证**(6 组独立证伪);②对抗发现的**收口修复**(4 组);③最终验证。
> 方法铁律:验证组默认"没修好/护栏是假的",拿证据推翻才判 CONFIRMED;每个护栏用 **mutate→确认 FAIL→还原** 硬标准判"真咬"。

## 一、对抗验证结论(6 组)

**没有一个 P0/P1 修复被推翻——第一轮修复全部真实有效、抗住证伪。** 6 组独立复现原始触发点(含修复组没测的变体)、反向注入验护栏,结论:

| 组 | 覆盖 | 结论 |
|---|---|---|
| V1 场景 | P1-01/02/03/09 | 修复全 CONFIRMED;抓到 1 个假护栏(删除 spawn 护栏 setup 自我中和) |
| V2 任务图 | P0-1 + P1-14 | 修复全 CONFIRMED;P0-1 咬合真(改回 bug→真 QMouseEvent 测试 FAIL);抓到 1 个假护栏(多选拖动用 setPos) |
| V3 叙事 | P0-2 + P1-07/08/10 | 全 CONFIRMED;两处 parity 经注入验证真独立比对;发现 1 处轻微残余 fail-open |
| V4 登记面 parity | 全部镜像护栏 | 四大头号 parity 逐个 mutate 后 FAIL=真咬;但发现**两处同款镜像仍裸奔**(present 类型/tag 系统)+4 处精度盲区 |
| V5 保存数据核心 | P1-06/11/15/16/18/19 | 数据安全主张全部成立抗住对抗;发现 1 真隐患(降级 flush 双执行)+1 措辞出入 |
| V6 小游戏图对话 | P1-05/13/17+字节保真 | 全 CONFIRMED;11 小游戏+63 图对话文件亲测零 diff;发现 1 真崩溃(图对话 nodes 容器非 dict)+1 假护栏(自测绕开 load_path) |

**对抗一轮的净发现**:修复可信,但护栏体系"真但有缺口"——3 个假护栏(根因1"测在错误抽象层"重现)、2 处裸奔镜像(根因2 没堵干净)、1 真崩溃、1 真隐患、若干精度/措辞项。共 14 项收口。

## 二、收口修复(4 组 + 数据规范化)

### 根因2 合拢(FIX-1)——建成 6 个新 parity,全 mutate 自证真咬
| 护栏 | mutate 注入 | 结果 |
|---|---|---|
| 过场 present 三方 parity(CutsceneManager↔timeline↔validator) | 删 showTitle | FAIL ✓ |
| [tag:] 三方 parity(resolveText.ts↔ref_validator↔tag_catalog) | 删 scene tag | FAIL ✓ |
| CONTENT_ID_PARAMS **宇宙级**(升级:从"非裸框"到"宇宙正确") | giveItem.id 接成 rule 选择器 | FAIL ✓ |
| _PARAM_SCHEMAS↔manifest **参数级**(双向) | 加幻影参数 / 清 required | FAIL ✓ |
| ref-kind 三方(extract 软 warning 升硬门) | 未映射 kind | FAIL ✓ |
| SCOPED_PARAM_RULES 有效性 | scoped 收窄失效 | FAIL ✓ |

修复两个假承诺测试:characterRegistry overlay 插入序(fixture 改逆序才真区分 sorted)、set_expr 空转(换嵌套表达式才真咬)。**全项目手工镜像普查**完成:已有护栏的 6 处复核通过,新补 6 处;仍裸奔的 5 处附原因+收口建议(条件 6 叶需 TS 先导出常量、present 参数级需 manifest、cutscene blocklist 单文件不变式、showNotification 松枚举、runtime hasUnread 签名小疵转 runtime 组)。

### 假护栏改真(FIX-2)——3 个假护栏改成真能咬
- 删除 spawn 护栏:setup 改"选中→真实制造 pending→删",去掉修复→FAIL(entry 带旧坐标复活)。
- 多选拖动:改真 QMouseEvent(ctrl 追加选中),去掉 P0-1 修复→FAIL。
- flag 删除:断言引用计数文案,禁用扫描→FAIL。

### 图对话崩溃硬化(FIX-3)
`nodes` 容器非 dict / 顶层非 dict 两层守卫(graph_document 7 处 + load_path 单点归一);假自测改走真实 load_path 覆盖 4 种畸形。dialogue_graph 71 passed,59 张真实图逐字节门保持。

### 双执行+fail-safe+措辞(FIX-4)
- Save All 降级 flush 改 `inspect.signature` 探参(不再 catch-TypeError-retry),体内 TypeError 只调 1 次;去掉→FAIL(call count=2)。
- 叙事 flush 整体读不到时 fail-safe 返 False+可读原因(不再假成功)。
- P1-06 测试改名 `test_validator_warns_on_id_mismatch`+报告措辞更正(warning 是对的设计:stem 键已保护数据,不硬拦)。

### 数据规范化(M,主审自控)
signal_cues.json 用编辑器 canonical 写法规范化(15 个紧凑单行 action 展开,103→178 行),幂等、与 HEAD 语义完全相同(纯空白),消除"首次编辑器保存产生大块噪音 diff"。

## 三、最终验证(全绿)

| 门 | 结果 |
|---|---|
| 编辑器 Python(拆两半避 OdenGraphQt offscreen 段错误 flake) | A 367 + B 335 = **702 passed**(比修复前 686 多 16 个新护栏测试) |
| 图对话套件 | **71 passed + 23 subtests**(含 59 例逐字节往返门) |
| 叙事 TS typecheck / vitest | 通过 / **114 passed** |
| validate-data | **0 error**, 58 warning(既有 plane 归属) |
| 素材审计 --strict | **0 issues** |
| 运行时 src/ 本会话改动 | **0**(工作区 4 处 src 改动为 7-13 分支既有) |
| 新增 parity/护栏测试 | 本次收口 4 个新 parity 文件 + 多处假护栏改真;全 mutate 自证真咬 |

## 四、根因治理状态

| 根因 | 第一轮 | 对抗+收口后 |
|---|---|---|
| 1 护栏测在错误抽象层 | 修了 P0-1 假测试 | 又抓出 3 个假护栏(删除 spawn/多选拖动/图对话自测),全改真;确立"护栏从最外层入口进" |
| 2 手工镜像无语义 parity | 补了 5 处 | 普查全项目,再补 6 处 + 修 2 假承诺;剩 5 处裸奔有据可查+收口路线 |
| 3 引用生命周期防护不均 | flag/quest/plane 补齐 | 对抗确认真咬 |
| 4 fail-open 惯性 | ErrorBoundary/超时/导航 | 又补 2 处(降级 flush 双执行、叙事 flush 整体读不到) |
| 5 Save All 连坐 | 降级不连坐 | 对抗确认;双执行隐患收口 |
| 6 外部修改零防护 | detect_external_changes | 对抗确认全边界 |

**净结论**:所有修复经独立证伪站住;护栏体系从"真但有缺口"收口到"真且系统"——根因1/2 这次是连着"全项目普查"一起治的,不再是单点补。仍裸奔的 5 处镜像已登记原因与低成本收口路线(多数需 runtime 侧先导出权威常量),非当前可达缺陷。
