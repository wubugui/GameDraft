
## 三条视图轴（位面 / 时段 / 过场）  (2)
- [1] **high** 新画布完全没有三条视图轴的入口：位面视图 / 时段视图 / 过场编辑视图三个下拉一个都没有，整套功能在 UI 上不可达
      v1: tools/editor/editors/scene_editor.py:11400-11439 建三个 FilterableTypeCombo —— 「过场编辑视图」(11401-11410)、「位面视图」(11412-11422)、「时段视图」(11424-11439)，各自带长 tooltip
      v2: tools/editor/editors/scene_v2/page.py:326-338 只有一个程序化入口 `set_view_axes(axes)`；全仓 grep `set_view_axes` 只命中 tools/editor/tests/test_scene_v2_overlays.py
      impact: 三条轴是「画布上这个实体现在看不看得见」的唯一控制面，在新画布里整体缺失。策划无法按位面/时段预览排位，等于新画布始终显示「全部实体叠在一起」；跨页跳转承诺的「打开该位面的位面视图」静默失效。判定层（shared/scene_view_filters.py）和落显隐层（view.set_presence_filter）都写好了且实测正确，缺的纯粹是接线与控件
      probe_run: True
- [2] **high** 「仅过场」的缺省判定与老画布和运行时相反：新画布把只该在过场里存在的实体当普通实体画出来，可点可拖可改
      v1: tools/editor/editors/scene_editor.py:225-226 `_entity_is_cutscene_only(ent) = _entity_has_cutscene_binding(ent) and ent.get("cutsceneOnly", True) is n
      v2: tools/editor/shared/scene_view_filters.py:83-85 `entity_is_cutscene_only` 写的是 `d.get("cutsceneOnly") is True` —— 缺省判成 False（= 共享实体，永远显示），与 v1 和 runtim
      impact: 新画布对同一份场景 JSON 显示出比老画布多的实体，且多出来的正是「只在过场里存在」的那些。策划照着这块骗人的画布排位、改坐标，改动会落到真实数据上但在正常游戏里看不到效果；反过来也会误以为这些实体在普通场景里存在而据此规划走位/遮挡。修 shared 的这一个默认值同时也是过场轴唯一可用的前提。
      probe_run: True

## 视口与背景（缩放/平移/fit/scene rect/背景加载与世界尺寸/视口辅助物）  (6)
- [3] **high** 打开任何场景，新画布的初始缩放恒定算错——fit 跑在新建 view 还没被 Qt 布局之前，场景显示成正确尺寸的 6%
      v1: tools/editor/editors/scene_editor.py:3588-3611 `fit_all()`：先 `_perform_fit_all()`，再用 `QTimer.singleShot(0/40/120/240)` 重试，320ms 后才关掉 `_auto_fit_after_
      v2: tools/editor/editors/scene_v2/page.py:164-198 `load_scene()`：第 190 行 `self._view = SceneView(...)` + `self._canvas_layout.addWidget(self._view)` 之后，第 
      impact: 每次打开/切换场景，1600x900 的场景被画成视口里一个几十像素的小点（正确缩放的 6.3%，约 1/16）。而且新画布没有「适配」按钮也没有快捷键（见 gap #2），唯一的补救是 Ctrl+滚轮按 1.15 倍一格一格放大约 20 格，每开一个场景都要来一遍。老画布不存在这个问题。
      probe_run: True
- [4] **high** 新画布没有任何缩放/适配 UI：v1 的 +、−、「适配」三个按钮一个都没有，视口跑偏后无法一键回到全览
      v1: tools/editor/editors/scene_editor.py:11351-11368 建了一条独立的 zoom 工具栏：`+`（放大画布视图）、`−`（缩小画布视图）、`适配`（将整个场景适配到画布视口）；槽在 14188-14196（`_on_canvas_zoom_in/out/fi
      v2: tools/editor/editors/scene_v2/page.py:301-324 `_install_tools()`：`self._toolbar.clear()` 之后只 `addAction(tool.display_name)` 逐个加工具，没有任何缩放/适配动作。`SceneVi
      impact: 缩放/平移一旦跑偏（尤其叠加 gap #1 的错误初始缩放），只能靠滚轮慢慢摇回来；老画布点一下「适配」即可。另外触控板用户没有 Ctrl+滚轮时连放大缩小都够不着（v1 有 +/− 按钮兜底）。
      probe_run: True
- [5] **medium** 中键拖动平移画布在新画布上完全无效（v1 全编辑器统一的平移手势）
      v1: tools/editor/editors/scene_editor.py:3756-3760 `mousePressEvent` 里 `if event.button() == MiddleButton: self._middle_panning = True; setCursor(ClosedHa
      v2: `grep -rn MiddleButton tools/editor/editors/scene_v2/` 无任何命中。view.py:90 `setDragMode(NoDrag)`；view.py:542-549 `mousePressEvent` 把所有按键转给 ToolManager，而 
      impact: 放大之后要平移只剩「无修饰滚轮」（纵向）和拖滚动条，横向平移必须 Shift/横向滚轮或拖底部滚动条。习惯了老画布中键拖的策划会以为画布卡死。
      probe_run: True
- [6] **high** 在属性面板换/导入背景图（或改世界尺寸）之后，新画布的背景与场景矩形不刷新——v1 的 scene_background_changed 信号在 v2 没接线
      v1: tools/editor/editors/scene_editor.py:11587-11588 `self._props.scene_background_changed.connect(self._on_scene_background_changed)`；14885-14905 `_on_sc
      v2: tools/editor/editors/scene_v2/page.py 全文没有 `scene_background_changed`（grep 只在 v1 命中）。`_refresh_background()`（page.py:203-224）只在 `load_scene()` 第 188 行
      impact: 画布仍显示旧背景（或「本场景无背景图」占位），而磁盘和数据其实已经换了。策划会以为导入失败而反复重导；即使换回正确图，sceneRect 也停在旧世界尺寸，适配/滚动范围都是错的。必须切到别的场景再切回来才恢复。老画布是即时刷新的。
      probe_run: True
- [7] **medium** 每次从别的编辑器页切回新画布，缩放和平移都被重置（v1 明确用 reset_view=False 保住视口）
      v1: tools/editor/editors/scene_editor.py:14644-14664 `reload_from_model()` → `self._load_scene(scene_id, reset_view=False)`；12588 `if reset_view: self._ca
      v2: tools/editor/editors/scene_v2/page.py:486-500 `reload_from_model()` 直接 `self.load_scene(sid)`；`load_scene()`（page.py:164-198）会销毁旧 view、新建 SceneView、并在
      impact: 逐个摆位时每查一次别的页就丢一次视口位置，要重新找回刚才在编辑的那块区域。老画布切回来是原样。（附带：view/Document 被整个重建，撤销栈也一起没了，但那属于撤销域。）
      probe_run: True
- [8] **medium** 「显示 NPC 比例参考框」这个默认开启的尺度参照物在新画布里整个消失
      v1: tools/editor/editors/scene_editor.py:3458-3513 `set_npc_reference_visible` / `rebuild_npc_reference`：在世界左上、右下各画一个与角色动画 JSON 的 worldWidth×worldHeight 同
      v2: `grep -rn "npc_reference\|reference" tools/editor/editors/scene_v2/*.py` 无命中（探针 viewport_probe_main.py [G] 输出 "(none)"）；view.py 的 `_PART_ITEM_FACTORY`
      impact: 给新场景定 worldWidth/worldHeight、或判断某个热点交互半径「按人身高算大概多少」时，失去了画布上唯一的世界单位实物比例尺，只能靠数字盲估。
      probe_run: True

## 实体图元与 part 完整性（PART_TABLE 逐 part 核对：hotspot/npc/zone/spawn/group）  (10)
- [9] **high** npc 的 sprite part 画的是整张图集，不是其中一帧 —— 每个 NPC 在新画布上是一片缩微小人网格
      v1: tools/editor/editors/scene_editor.py:497-521 `_SceneNpcAnimRuntime.draw_at()` 先 `_crop_atlas_cell(self.atlas, self.cols, self.rows, idx, cell_w=…, cel
      v2: tools/editor/editors/scene_v2/page.py:271-272 `_npc_sprite_metrics` 返回的 url 是 `str(sheet_path)`，即**整张 atlas.png**；view.py:258 `item.set_pixmap(self._t
      impact: 新画布上每个 NPC 都不是一个人，而是一坨密密麻麻的缩微小人。策划无法用它对位、判断人物大小、判断遮挡前后关系；老画布这里是会走动的单帧精灵。全库 28 个场景、几百个 NPC 全中。
      probe_run: True
- [10] **high** 内容 part（npc:sprite / hotspot:display）不乘透视系数，画布上的大小是错的
      v1: tools/editor/editors/scene_editor.py:513 `eff = self.inst_scale * (self.persp if …)` —— NPC 精灵的有效缩放 = 实例 scale × 透视系数；scene_editor.py:2747-2748 `pf = 
      v2: tools/editor/editors/scene_v2/view.py:280-287（npc 分支）与 :300-307（hotspot 分支）`_content_spec` 只取 `float(ent.get("scale", 1.0) or 1.0)`；全 scene_v2 目录 grep
      impact: 透视场景里所有远处角色/热点贴图在新画布上都偏大（近处偏小），策划照着摆位就会摆错；内容层 z 排序的输入（脚底 quad 尺寸）也跟着偏，前后遮挡关系可能整体翻。
      probe_run: True
- [11] **high** 把手（hotspot/npc/spawn 的 handle part）没有实体名标签，也没有 tooltip
      v1: tools/editor/editors/scene_editor.py:600-615 `_DraggableCircle.__init__` 建了一个 `QGraphicsTextItem` 子项做标签（`ItemIgnoresTransformations` 保持屏幕恒定字号，`_apply_
      v2: tools/editor/editors/scene_v2/entity_items.py:96-113 `HandleItem.paint()` 只有 `drawEllipse` 两笔（交互半径圈 + 圆点），没有任何 drawText；全 scene_v2 目录 grep `drawText|Q
      impact: 「把玩家送到哪个出生点」「这个圆点是哪个热点」这类日常操作在新画布上没法直接看，必须回左边实体树逐个点选比对；老画布是抬眼就能读。
      probe_run: True
- [12] **medium** hotspot 的 handle 不再按 type 分色，六类热点在画布上长得一模一样
      v1: tools/editor/editors/scene_editor.py:135-142 `_HOTSPOT_COLORS` 给 inspect/act_spot/pickup/transition/npc/encounter 六种类型各一个颜色；scene_editor.py:2716-2717 
      v2: tools/editor/editors/scene_v2/entity_items.py:28-33 `KIND_COLORS` 每族只有一个颜色；entity_items.py:64-65 `HandleItem.__init__` 用 `KIND_COLORS.get(ref.kind, …)
      impact: 策划一眼分不出「这是个传送点还是个可查看点还是个遭遇点」，改错对象的概率明显上升；老画布靠颜色就能扫一遍全场。
      probe_run: True
- [13] **medium** zone 的 polygon part 不再区分 zoneKind=depth_floor，深度地面和普通触发区同色
      v1: tools/editor/editors/scene_editor.py:284-287 `_zone_canvas_color(zone)`：`zoneKind == "depth_floor"` 返回 `_ZONE_COLOR_DEPTH_FLOOR`（蓝，QColor(80,160,255,7
      v2: tools/editor/editors/scene_v2/view.py:61 `("zone", "polygon"): PolygonItem`，entity_items.py:213-216 `PolygonItem.__init__` 用 `KIND_COLORS.get(ref.kind
      impact: 深度地面（决定角色踩地深度，改错直接影响角色遮挡/缩放）与普通触发 zone 在画布上无法区分，叠在一起时更容易拖错、删错。
      probe_run: True
- [14] **medium** handle 的交互半径圈少乘实例 scale 与透视系数（且缺省值 0≠50），画出来的范围不是运行时范围
      v1: tools/editor/editors/scene_editor.py:2718-2719 `ir = float(hs.get("interactionRange", 50) or 0) * entity_scale_of(hs) * self.persp_factor(hs, "hotspot
      v2: tools/editor/editors/scene_v2/view.py:234 `item.set_interaction_range(float(ent.get("interactionRange", 0) or 0))` —— 既没乘 `entity_scale_of`，也没乘透视系数，缺省
      impact: 策划照着虚线圈调「玩家走到多近能交互」，在透视场景里被系统性误导（最多 2.2 倍）；缩放过的实体同样偏。
      probe_run: True
- [15] **medium** hotspot/npc 的 ghost part（运行时命中面幽灵轮廓）在 v2 里完全不存在
      v1: tools/editor/editors/scene_editor.py:2595-2630 `_sync_collision_persp_ghost()`：`pf != 1.0 且多边形 ≥3 点`时，把 authored 多边形绕锚点 ×pf 画成只读虚线（与运行时 `anchorCollisi
      v2: `grep -rn ghost tools/editor/editors/scene_v2/` **零命中**；view.py:53-63 `_PART_ITEM_FACTORY` 里没有 `("hotspot","ghost")` / `("npc","ghost")` 两项（view.py:50
      impact: 在透视场景里给 NPC/热点画走位阻挡或命中面时，画布上看到的多边形不是运行时生效的那个（可差 2 倍以上），而且没有任何提示；老画布用一圈虚线明确告诉你「真正生效的是这一圈」。
      probe_run: True
- [16] **medium** group 的 box part 包围盒只算锚点和 zone 顶点，不算展示图/精灵占位，也不外扩留白 —— 框套不住组员
      v1: tools/editor/editors/scene_editor.py:13050-13090 `_entity_canvas_bbox()`：hotspot 取 `displayImage.worldWidth/Height × scale × 透视系数`、npc 取精灵运行时的 `world_
      v2: tools/editor/editors/scene_v2/tools_overlays.py:96-120 `group_bounds()` 只收集两种点：`ent["x"], ent["y"]` 与 `ent["polygon"]` 的顶点；不看 displayImage、不看精灵尺寸、不乘 s
      impact: 分组框画不住它所框的东西，看起来像框错了成员；框边命中带也贴着成员，靠边的成员会被组框吃掉点击。
      probe_run: True
- [17] **medium** group 的 box part 从不绘制组标题，画布上分不清哪个框是哪个组
      v1: tools/editor/editors/scene_editor.py:2010-2019 `_SceneGroupBox.__init__` 建了一个 `QGraphicsTextItem` 标题子项（`ItemIgnoresTransformations` 保持屏幕恒定字号），2086-210
      v2: tools/editor/editors/scene_v2/overlays.py:56-60 `GroupBoxItem.set_geometry()` 存下 `self._title`，但 overlays.py:98-111 的 `paint()` 只有 `drawRect` + `drawE
      impact: 两个组的框叠在一起时无法分辨谁是谁、也看不到成员数；老画布是 Figma frame 式的「框 + 名字」，扫一眼就知道。
      probe_run: True
- [18] **medium** 没有成员的分组，box part 在 v2 里完全不可见也点不中
      v1: tools/editor/editors/scene_editor.py:2024-2050 `_SceneGroupBox.set_geometry(rect=None, anchor, title)` 走 `_empty=True` 分支：把图元摆到 anchor 上、框不画但**标题照画**；
      v2: tools/editor/editors/scene_v2/overlays.py:56-60 `set_geometry(None, title)` → `self._rect = QRectF()`（null）；overlays.py:99-100 `paint()` 开头 `if self._
      impact: 刚建好还没塞成员的组在新画布上凭空消失，用户以为没建成功而重复新建；老画布会留一个把手 + 标题在那儿，还能拖着先摆好位置。
      probe_run: True

## 缩放/旋转 gizmo（v1 _TransformGizmo / transform_gizmo_live+committed ↔ v2 tools_transform.TransformTool / overlays.TransformGizmoItem）  (6)
- [19] **high** 在手柄上按一下不拖就松手，实体的 scale/rotation 就被改写，场景标脏还多一条撤销记录
      v1: tools/editor/editors/scene_editor.py:804-816 _TransformGizmo.mouseReleaseEvent：`if self.scale_v == self._press_scale and self.rot_deg == self._press_r
      v2: tools/editor/editors/scene_v2/tools_transform.py:147-162 mouse_released：只要 press 时抓到了手柄就无条件把 (scale, rotation) 组成 vals 推命令，没有任何「没动过」的判据；round(scale,4)
      impact: 用户什么都没做（手柄按下即松），场景被标脏、JSON 里的数值被静默改写、撤销栈平白多一格；批量点检一遍场景就能污染一片实体，且 diff 出来的全是「我没改过的东西」。
      probe_run: True
- [20] **high** 交互半径虚线圈不乘实例 scale，缩放后圈纹丝不动，画的半径和运行时实际可交互距离对不上
      v1: tools/editor/editors/scene_editor.py:2718-2719（add_hotspot）与 2929-2930（add_npc）：`ir = float(...interactionRange...) * entity_scale_of(hs) * self.persp
      v2: tools/editor/editors/scene_v2/view.py:234 `item.set_interaction_range(float(ent.get('interactionRange', 0) or 0))` —— 全仓只有这一处调用，raw 值直接进图元，没有乘 entity 
      impact: 策划照着画布上的虚线圈调 interactionRange，实际游戏里玩家在圈外三倍远的地方就能触发（或反过来以为够不着而把半径调大一倍）——画布对玩法参数直接撒谎，且缩放越大偏得越多。
      probe_run: True
- [21] **medium** 只拖旋转手柄也会把没碰过的 scale 一起改写（int 漂成 float、精度被截断），只拖缩放手柄则把 rotation 归一化（-30 变成 330）
      v1: tools/editor/editors/scene_editor.py:8556-8575 _write_transform_fields_live 的 _put：`if isinstance(old,(int,float)) and float(old)==v: return  # 值等价：保原
      v2: tools/editor/editors/scene_v2/tools_transform.py:155-159：两个键永远一起写，scale 无条件 round(...,4)、rotation 无条件 round(rot % 360.0, 3)；commands.py:252-263 的 _sam
      impact: 「我只转了一下」却在 diff 里看到 scale 变了；负角写法被单方面改成正角，属性面板里再看是 330 而不是自己填的 -30；整数缩放全场景漂成小数，黄金往返/数值表示保真契约这一侧直接失守。
      probe_run: True
- [22] **medium** 出生点（spawnPoint）也能被 gizmo 缩放/旋转，往只有 x/y 的结构件里写出 schema 外的 scale/rotation
      v1: tools/editor/editors/scene_editor.py:14094-14099 _sync_transform_gizmo：`if len(refs) != 1 or refs[0][0] not in ('hotspot','npc'): self._canvas.hide_tr
      v2: tools/editor/editors/scene_v2/tools_transform.py:58-66 _anchor 只要求实体 dict 里有 'x' 键，不筛 kind；document.py:125-126 让 EntityRef('spawn', ...) 能取到 spawnPoin
      impact: 用户以为在调出生点位置，实际给出生点写进了运行时根本不读的 scale/rotation 字段，存盘后长期躺在 JSON 里；老画布打开同一场景时这两个键既不显示也不可编辑，只能手改文本删掉。
      probe_run: True
- [23] **medium** 手势进行中只有展示图/精灵跟着转，碰撞面、交互圈、属性面板数值框都不动，松手才整体跳到位
      v1: tools/editor/editors/scene_editor.py:14144-14154 _on_gizmo_transform_live 每帧做三件事：_write_transform_fields_live 写 staging → sync_transform_widgets(8649-
      v2: tools/editor/editors/scene_v2/view.py:489-514 set_transform_preview：`for part in ('display','sprite')` —— 预览只改这两个内容图元的几何，collision / ghost / handle(交互
      impact: 转/缩的时候画面自相矛盾：贴图转了、碰撞面和交互圈留在原地，用户没法边拖边对齐碰撞面与美术，只能松手看一眼、不满意再来一次；同时看不到 scale/rotation 的实时数值，拖到几度几倍全靠猜。
      probe_run: True
- [24] **medium** gizmo 变成模式化：单选实体不再自动出手柄，必须切到「缩放旋转」工具，而该模式下点实体既不能选也不能移，工具栏还看不出自己在哪个模式
      v1: tools/editor/editors/scene_editor.py:14094-14119 _sync_transform_gizmo 在选择变化/拖动结束等 7 个调用点（12834/13848/14088/14170/14558/14849/15337）被调用，单选 hotspot/npc
      v2: tools/editor/editors/scene_v2/view.py:529-531 gizmo 位置来自「当前工具」的 gizmo_positions()，只有 TransformTool 有这个方法；tools_transform.py:106-131 mouse_pressed 命中不到
      impact: v1 里「选中→直接转」变成「切工具→拖→再切回去」，转第二个实体要来回切两次；进了变换模式又没选中任何实体时，画面与选择模式一模一样，点谁都没反应，用户会以为画布卡死；工具栏勾选态与真实模式对不上，无从判断当前在哪个模式。
      probe_run: True

## 选择（点选/加选/框选/叠放循环/树↔画布联动/选中态下游）  (9)
- [25] **high** 选中出生点(spawn)时属性面板完全不切页：出生点在新画布没法编辑，而且面板还停在上一个实体上，在上面打的字被静默丢弃
      v1: tools/editor/editors/scene_editor.py:14264-14276 — `_on_item_selected` 的 `elif kind == "spawn":` 分支调 `props.load_spawn_props(scene_use, eid)`，右侧切到 `_s
      v2: tools/editor/editors/scene_v2/panel_bridge.py:130-134 `_PANEL_BINDING` 只有 hotspot/npc/zone，没有 spawn（也没有 group/scene）；panel_bridge.py:199-207 `sync_fro
      impact: 出生点在新画布里只能拖坐标，改名/设默认/改朝向等全部入口消失；更坏的是面板显示的是别的实体，用户在上面做的编辑无提示地丢失，切回去才发现改动没了。
      probe_run: True
- [26] **high** 多选（≥2 实体）没有多选页：右侧仍显示最后一个单实体的表单，且此时面板上的任何编辑被静默丢弃，v1 的批量「指派分组/复制/删除」按钮整条消失
      v1: tools/editor/editors/scene_editor.py:14203-14210 `_on_item_selected` 里 `if len(self._canvas_selected_entity_refs()) > 1:` → `self._props.show_multi_se
      v2: tools/editor/editors/scene_v2/panel_bridge.py:199-202 `sync_from_selection`：`if len(sel) != 1: self._loaded = None; return` —— 多选时直接返回、不切页；panel_bridg
      impact: 用户看不出自己处在多选状态（面板撒谎），在上面做的编辑无声消失；v1 的批量指派分组/批量复制/批量删除按钮入口没了（Delete/Ctrl+D 快捷键还在，但按钮找不到）。
      probe_run: True
- [27] **high** 点画布空白取消选择后不回场景属性页：场景属性（世界尺寸/滤镜/镜头/光照）一旦选过任何实体就再也打不开，除非切走场景再切回来
      v1: tools/editor/editors/scene_editor.py:14278-14292 `_on_item_deselected`：先 `_undo_flush_pending_as_command()` 提交未应用编辑，再 `self._props.load_scene_props(sc
      v2: tools/editor/editors/scene_v2/page.py:183 是全 `scene_v2/` 唯一一次 `load_scene_props`，只在 `load_scene()` 里调；page.py:427-436 `_on_doc_changed` 收到 `SelectionC
      impact: 场景级属性的唯一入口丢失（要绕一圈切场景）；同时面板显示的对象与实际选择脱节，编辑静默丢失。
      probe_run: True
- [28] **high** 分组在新画布里选不中也编辑不了：实体树没有分组节点，画布点亮组框也不切属性面板，group 的 id/标签/成员列表全无入口
      v1: tools/editor/editors/scene_editor.py:12683-12688 `_section()` 给分组节点写 `UserRole=("group", gid)`，12704-12707 按类型模式下还建「场景分组（N）」一节逐个列出；scene_editor.py:128
      v2: tools/editor/editors/scene_v2/page.py:386-396 `refresh_entity_tree` 只建 hotspot/npc/zone/spawn 四类，没有 group；tools_overlays.py:157-165 `GroupBoxTool.sele
      impact: 分组只剩"整体拖动"一个能力；建组/改组名/看谁在组里在新画布上完全做不到，必须切回老画布。
      probe_run: True
- [29] **medium** 分组选中态粘滞：点空白不熄灯，之后按方向键会把整组实体挪走（用户以为什么都没选）
      v1: tools/editor/editors/scene_editor.py:14293 `_on_item_deselected` 末尾 `self._canvas.set_selected_group(None)` —— 点空白既清实体选择也清组选中；scene_editor.py:3865-388
      v2: tools/editor/editors/scene_v2/tools_overlays.py:167-172 `GroupBoxTool.mouse_pressed`：`gid, box = self._box_at(scene_pos); if gid is None: return False
      impact: 以为没选中任何东西，一按方向键（很多人用方向键滚视图的肌肉记忆）就整组坐标被改并标脏；改动会真进模型和撤销栈。
      probe_run: True
- [30] **medium** 树↔画布只同步高亮、不做定位：树里点一个实体画布不滚过去，画布上点一个实体树也不滚到那一行
      v1: tools/editor/editors/scene_editor.py:12893-12896 树单选 → `self._on_item_selected(kind, eid)` **加** `self._focus_canvas_on_entity(kind, eid)`；scene_edito
      v2: tools/editor/editors/scene_v2/page.py:416-429 `_on_tree_selection_changed` 只 `self._doc.set_selection(refs)`，没有 centerOn；page.py:398-414 `_sync_tree_s
      impact: 实体多的场景里，树基本失去"定位"功能：点一行只能看见树自己变蓝，找不到东西在画布哪儿；反过来在画布上点一个实体，树里对应行可能在滚动区外，看不到高亮。
      probe_run: True
- [31] **medium** 加选修饰键从 Ctrl 换成了 Shift：老习惯的 Ctrl+点选不再加选，反而把已有的多选整个清掉
      v1: tools/editor/editors/scene_editor.py:3762-3768 `if event.button()==Left and (modifiers & ControlModifier):` 注释原文「Ctrl+点选 = Qt 原生加选/减选（多选路径）」，走 QGraphi
      v2: tools/editor/editors/scene_v2/tools_builtin.py:99-105 `SelectTool.mouse_pressed` 只认 `Qt.KeyboardModifier.ShiftModifier` 做加/减选，其余修饰键一律走 `self._doc.set_
      impact: 从老画布过来的人第一时间选不出多选；而且 Ctrl+点在 v2 是"静默丢掉已选"，配合上一条（多选没有多选页）就更难自查。
      probe_run: True
- [32] **high** 「移动」工具下无法改选也无法取消选择：在任意位置（包括空白背景、或另一个实体上）按下拖动，挪走的都是之前选中的那个实体
      v1: tools/editor/editors/scene_editor.py:589-591 `_DraggableCircle` 带 `ItemIsMovable | ItemIsSelectable`，scene_editor.py:2446 `setDragMode(QGraphicsView.D
      v2: tools/editor/editors/scene_v2/tools_builtin.py:196-221 `MoveTool.mouse_pressed` 完全不做命中判定：只要 `self._doc.selection` 非空就把**当前选择**记进 `_start` 开始拖，`scene_p
      impact: 改错对象且看不见：被拖的实体可能在视口外，用户只看到"我拖的这个没动"，实际另一个实体的坐标已经被改并写进模型。
      probe_run: True
- [33] **low** 工具栏按钮的勾选态和当前工具对不上：启动时「选择」是活的但按钮不亮，点过两个工具后两个按钮都亮着
      v1: 老画布没有工具栏（选择/移动是同一手势），无对应物；对照物是 v2 自己的语义 —— tools.py:230-245 `ToolManager.select` 保证同一时刻恰好一个工具活着，工具栏理应反映它。
      v2: tools/editor/editors/scene_v2/page.py:317-323 `for tool in view.tools.tools: act=self._toolbar.addAction(...); act.setCheckable(True); act.triggered.c
      impact: 看不出自己现在处在哪个工具，配合上一条（移动工具下点空白会挪实体）容易在不知情的情况下改坏坐标。
      probe_run: True

## 透视深度轴 + 透视缩放（v1 _PerspAxisItem / persp_factor ↔ v2 overlays.PerspectiveAxisItem / tools_overlays.PerspectiveAxisTool / view._content_spec / sorting.py）  (7)
- [34] **high** v2 画布完全不乘透视系数：NPC 精灵 / 热点贴图按原始尺寸画，与运行时差到 2.2 倍（预览撒谎）
      v1: tools/editor/editors/scene_editor.py:2588-2593 `persp_factor()` = `entity_perspective_factor(self._persp_cfg, ent, kind, ...)`；:2748-2749 `pf = self.p
      v2: tools/editor/editors/scene_v2/view.py:262-308 `_content_spec()`：hotspot 走 :305 `float(ent.get("scale",1.0) or 1.0)`、npc 走 :284 同样一行，两处都只有实例 scale，没有任何
      impact: 策划在新画布上摆 NPC 站位、调贴图 worldWidth/Height、排「谁站在谁前面」时，看到的尺寸与游戏里差最多 2.2 倍。照画布摆好的构图进游戏就是散的，而且没有任何报错——这正是重建要消灭的那一类「编辑器骗人」，老画布专门为此写了注释「防预览撒谎」。
      probe_run: True
- [35] **high** 属性面板「透视缩放」段的所有编辑（近/远端缩放、中途点、affectsSpeed、启用/关闭）在新画布里静默丢失
      v1: tools/editor/editors/scene_editor.py:5340-5418 建出整段透视控件；:6029-6060+ `_flush_persp_into()` 把控件写进 `_staging_scene`；:14618-14626 `flush_to_model()` → `_a
      v2: tools/editor/editors/scene_v2/panel_bridge.py:130-135 `_PANEL_BINDING` 只有 hotspot/npc/zone 三族，没有 scene；:281-285 `commit_panel_edits()` 里 `binding = _P
      impact: 用户在面板上把远端缩放调了半天、加了中途点、关掉了 affectsSpeed，面板还显示「有未应用编辑」，但存盘后一个字都没进 JSON，而且 `confirm_close()` 恒 True，切页/关窗连一句提示都没有。数据静默丢失，用户只有回游戏里跑一遍才会发现白改。
      probe_run: True
- [36] **high** 没配过透视的场景，在新画布里根本没有任何入口启用透视
      v1: tools/editor/editors/scene_editor.py:5909-5921 `_on_persp_widgets_changed()`：勾上「启用透视缩放」而 `_sc_persp_axis` 为空时**自动 seed 一条默认竖直轴**（近端在下、远端在上），随后经 `_flus
      v2: 两条路都堵死：(a) 面板路见上一条（panel_bridge.py:130 `_PANEL_BINDING` 无 scene）；(b) 画布路——tools/editor/editors/scene_v2/tools_overlays.py:44-53 `PerspectiveAxisTool.m
      impact: 新画布只能改已有的透视轴端点位置，不能给场景新建透视配置。策划要给一个新场景加近大远小，必须切回老画布（或手改 JSON）——新画布在这个功能上是只读的一半。
      probe_run: True
- [37] **medium** 内容层前后次序漏乘透视系数：旋转 + 参与透视的实体在新画布里的遮挡关系与运行时相反
      v1: tools/editor/editors/scene_editor.py:11872-11882（热点）`pf = self._canvas.persp_factor(hs,"hotspot")` → `s = entity_scale_of(hs) * pf` → `foot = sort_foo
      v2: tools/editor/editors/scene_v2/sorting.py:56 `s = entity_scale_of(ent)` 与 :68 同一行——只有实例 scale，没有 pf；文件顶部 :18 也只 import 了 `entity_scale_of`。
      impact: 策划在新画布上排「谁挡谁」时看到的层级与游戏里相反。触发条件是「实体有 rotation 且参与透视」（无旋转时 sort_foot_y_of 返回 None、两侧同值，所以只在旋转体上暴雷）——正因为条件窄，撞上时更难归因。
      probe_run: True
- [38] **medium** 运行时命中面「幽灵轮廓」丢失：参与透视的实体，画布上看不到真实点击范围
      v1: tools/editor/editors/scene_editor.py:2595-2631 `_sync_collision_persp_ghost()`：pf≠1 且有碰撞多边形时，把 authored 多边形绕锚点 ×pf 画成只读虚线（注释：与运行时 anchorCollisionPolyg
      v2: tools/editor/editors/scene_v2/ 全包 grep 不到 ghost / extraScale / 任何按 pf 缩放多边形的代码（`grep -rn "entity_perspective_factor|perspective_scale_at"` 零命中）。view.p
      impact: 参与透视的实体，作者画的碰撞面与游戏里真正能点到的范围差一个 pf（远端可差一半）。老画布同时画 authored（可编辑）+ 幽灵（真实命中）两套，新画布只剩前一套——策划照着画的命中面在游戏里点不到，且画布上没有任何提示。
      probe_run: True
- [39] **medium** 深度轴 overlay 退化：没有指向远端的箭头、没有近/远端缩放数值标签、没有中途点等值线
      v1: tools/editor/editors/scene_editor.py:1795-1801 类注释即写明「近→far 箭头 + 两端可拖手柄 + 垂直于轴的等缩放等值线（near/far/中途点各一条虚线）」；:1869-1898 `paint()` 依次画箭头两笔（指向 far）、`_draw_
      v2: tools/editor/editors/scene_v2/overlays.py:182-195 `PerspectiveAxisItem.paint()` 全文只有三笔：`drawLine(near,far)` + 近端一个橙色实心圆 + 远端一个蓝色实心圆。类里根本没有 near_scale 
      impact: 调透视轴时失去全部现场读数。近远端缩放到底是多少、中途点卡在轴的哪个位置，都得回面板对着数字猜，而面板那些数字在新画布里又存不下去（见第 2 条）——两条叠起来，新画布的透视编辑基本只剩「拖两个端点坐标」。
      probe_run: True
- [40] **low** Esc 取消轴拖动后，深度轴 overlay 停在假位置不回位
      v1: tools/editor/editors/scene_editor.py:1911-1940 `_PerspAxisItem` 只有 press/move/release 三个事件，没有取消分支——老画布这条手势本身不存在「取消后留脏」的形态（拖到哪就提交到哪）。对照 v2 的取消契约：新画布把「E
      v2: tools/editor/editors/scene_v2/tools_overlays.py:88-93 `cancel_gesture()` 只清 `_which` / `_start` 就 `return True`，既没有把 overlay 调回模型值（没调 `self._item.set_
      impact: 取消一次拖动后，画布上显示的深度轴与实际生效的轴对不上，用户会照着这根假轴继续摆实体。数据没坏，但画面在骗人，直到下一次任何编辑触发刷新才回位。
      probe_run: True

## 分组（group）  (11)
- [41] **high** 单成员（或零成员）的分组，在新画布上整个消失，也点不中——真实场景 雾津街头 的「主线_吃饭点」就是这一例
      v1: scene_editor.py:13050-13090 `_entity_canvas_bbox`：热点/NPC 没有展示图时兜底 `r = max(self._canvas.handle_radius, 8.0)`，给出一个 2r 的方框（13089）；scene_editor.py:13112 
      v2: tools_overlays.py:96-127 `group_bounds` 只收集成员的 x/y 与 polygon 顶点，单个点成员 → `QRectF(x, y, 0, 0)`；overlays.py:99-100 `paint` 里 `if self._rect.isNull(): ret
      impact: 这个组在画布上既看不见也选不中，整组位移/微移全部够不到；成员被删到只剩一个时，原本还在的框会当场消失，用户会以为分组丢了。老画布这两种情况都画得出、拖得动。
      probe_run: True
- [42] **high** 分组属性面板在新画布里完全够不到：组 id、label、整组显影 conditions、成员列表、精确位移都没法看也没法改
      v1: scene_editor.py:10827 `_build_group_panel` / 10999 `load_group_props`：组 id、label、conditions（整组显影条件）、成员列表（10870，双击可定位）、只读几何行；scene_editor.py:10886 `_bu
      v2: panel_bridge.py:131-134 `_PANEL_BINDING` 只有 hotspot/npc/zone；panel_bridge.py:200-208 选择里没有 group 这一族就直接 `self._loaded = None` 返回。全仓 `grep -rn "load_gr
      impact: 该场景两个组身上都配了 conditions（整组显影条件，是玩法数据）。在新画布里既看不到也改不了，组改名、改 label、看成员清单、按数值精确位移这些全部做不了——只能回老画布。
      probe_run: True
- [43] **high** 新画布没有任何创建/删除分组、给实体指派或移出分组的入口
      v1: scene_editor.py:11501 「新增组」按钮 → 13892 `_add_scene_group` 写 entityGroups 条目；scene_editor.py:15868 `_delete_scene_group`（带入站引用阻断 + 成员 group 清理），入口在实体树右键
      v2: page.py 全文没有任何 entityGroups 的写入（只有 page.py:353 读）；全仓 `grep -rn "QMenu|contextMenuEvent" tools/editor/editors/scene_v2/` = 0 命中，树与画布都没有右键菜单；page.py:380
      impact: 在新画布里分组是只读的——只能移动已经存在的组，不能建、不能删、不能往里加人或把人踢出去。做内容时必须切回老画布。
      probe_run: True
- [44] **high** 「分组」工具里选中过一个组之后，这个选中态永远不清除；此后按方向键动的是整组，而不是当前选中的那个实体
      v1: scene_editor.py:12824-12827：画布选中了真实体就 `self._canvas.set_selected_group(None)`（注释原文「选中了真实体 = 离开分组：组框熄灯」）；scene_editor.py:3863-3881 方向键微移整组的前提是 `self._s
      v2: tools_overlays.py:156-159 `select_group` 只在 `mouse_pressed` 命中组框时被调（:171-177），点空白处 `_box_at` 返回 (None, None) → :169 直接 `return False`，`_selected_gid` 
      impact: 用户以为在微调一个 NPC，实际把整支 13 人的送葬队伍挪了 1 个单位，画面上不容易看出来；连按几次就是一片静默的坏坐标，而且撤销记录写的是「整组微移」，对不上用户的操作意图。
      probe_run: True
- [45] **medium** 分组框上没有名字：一个场景有多个组时，画布上是几个一模一样的虚线框，分不出哪个是哪个，也不能点标题选中
      v1: scene_editor.py:2007 组框自带 `QGraphicsTextItem` 标题（`ItemIgnoresTransformations`，屏幕恒定字号），scene_editor.py:13195-13200 标题文本是 `[组] {gid}（{label}） ×{成员数}`；sc
      v2: overlays.py:55-59 `set_geometry` 把 title 存进 `self._title` 之后**再没有任何地方用过**；overlays.py:98-109 `paint` 只画矩形和把手圆点（探针确认 paint 源码里既无 `_title` 也无 `drawText`
      impact: 多组场景里没法一眼分辨/定位某个组，也没有成员数这个唯一辨识信息；老画布里最好按的那个点击靶（标题）在新画布上不存在，只剩下几像素宽的框边线可点。
      probe_run: True
- [46] **medium** 组锚点（editor.anchor）整套没了：不能 Alt+拖把手改把手位置、不能重置把手，JSON 里已有的 anchor 也被忽略
      v1: scene_editor.py:2280-2300 press 时 `Alt + 命中把手` 进 `anchor` 模式；2330-2336 release 发 `group_anchor_committed`；scene_editor.py:13717-13740 `_on_group_ancho
      v2: overlays.py:81-90 `handle_center` 纯派生（框左上角外侧），从不读实体数据；tools_overlays.py:166-181 `mouse_pressed` 里 `hit_handle` 与 `hit_edge` 走**同一个分支**，没有 Alt 分支、没有 an
      impact: 老画布里摆好的把手位置在新画布上失效（把手跑到另一个地方），而且新画布既改不了也重置不了；老画布注释里写明把手位置是为了躲开成员密集区才反复调过三轮的，派生位置压住成员时用户没有任何救济手段。
      probe_run: True
- [47] **medium** 工具栏里的「整组位移」是个死按钮：切过去之后拖组框、点实体、按方向键全都没反应
      v1: 老画布没有模式，组框在正常编辑态下随时可拖（scene_editor.py:2244-2350 `_SceneGroupBox` 自己处理 press/move/release）；面板上的精确位移入口是 scene_editor.py:10921 的「应用位移」按钮，走 13691 `_on_gro
      v2: tools_transform.py:286-350 `GroupMoveTool` **没有覆盖 `mouse_pressed`**，只有一个需要外部调用的 `begin(gid, scene_pos)`（:317）；全仓 grep `group_tool.begin` / `.begin(` 只
      impact: 工具栏上有一个名字最像「我想干的事」的按钮，点进去整个画布就死了——拖不动组、选不了实体、什么都不发生，用户只能靠猜切回「选择」或「分组」。
      probe_run: True
- [48] **medium** 分组包围盒只用成员的脚点算，不含精灵/展示图尺寸也不留白：框线从成员身体中间穿过，框不住任何人
      v1: scene_editor.py:13050-13090 `_entity_canvas_bbox`：NPC 用场景精灵的真实世界尺寸（`rt.world_w/world_h`，底中锚点 `QRectF(x-w/2, y-h, w, h)`），热点用 displayImage 的 worldWidth
      v2: tools_overlays.py:96-127 `group_bounds` 只吃 `ent["x"]/ent["y"]` 与 `polygon` 顶点，完全不问渲染尺寸，也不做任何外扩；overlays.py:66-79 `_pad` 的命中带还是**跨在框线两侧**（outer/inner 各
      impact: 框看起来没把成员框住，用户会以为选错了组或者组里少了人；框边线又正好压在最外侧成员的脚下，在「分组」工具里想点那几个成员的落点全被组框吃掉。
      probe_run: True
- [49] **medium** 兼容标签组（成员带 group 字符串、场景里没有 entityGroups 条目）在新画布上没有框，等于这个组不存在
      v1: project_model.py:1439-1470 `scene_group_ids_for_scene` 明确把「旧场景仅在成员上保存 group 字符串」的标签追加为兼容分组；scene_editor.py:13192 组框就是照这份名录建的，所以这类组在老画布上照样有框、能选能拖，而且 sc
      v2: page.py:352-362 `refresh_group_boxes` 只遍历 `sc.get("entityGroups")`，成员身上的 group 标签一个都不看（而 tools_transform.py:191-204 `group_member_refs` 反过来只认成员标签——两边口
      impact: 这类场景在新画布里看不到任何分组，用户会以为分组数据丢了；组存在但整组位移/微移一个入口都没有（组框是唯一入口）。
      probe_run: True
- [50] **medium** 组框只有切到「分组」工具才碰得到；默认「选择」工具下点组框边线，点中的是底下的实体
      v1: 老画布无模式：`_SceneGroupBox` 是场景里的图元，自己处理鼠标（scene_editor.py:2244 起），正常编辑态下随时可以点框边选组、拖组、方向键微移；scene_editor.py:2265-2266 还专门在 press 里先 `clear_selection()` 再 
      v2: tools.py:204-270 `ToolManager` 同一时刻**恰好一个**工具活着，输入只转发给 `self._current`；page.py:315-324 `GroupBoxTool` 只是十个并列工具之一，page.py:324 默认激活的是 `select_tool`。反过来，
      impact: 「点框边选组」这个老画布里随手可做的动作，在新画布里必须先找到并切换工具；而切过去之后又完全不能选实体，编排一组 NPC 时要在两个工具之间来回切。
      probe_run: True
- [51] **low** 没有「显示场景分组框」开关，虚线框和把手无法关掉
      v1: scene_editor.py:11389-11398 `_chk_group_boxes` 复选框（默认勾选）→ 13254 `_on_group_boxes_toggled` → 2997 `SceneCanvas.set_group_boxes_visible`；tooltip 写明「取消勾选
      v2: 全仓 `grep -rn "group_boxes_visible|set_group_boxes_visible" tools/editor/editors/scene_v2/` = 0 命中；view.py:324-339 `sync_group_boxes` 建出来的 GroupBoxItem
      impact: 对着背景图对位时关不掉分组框的干扰；老画布提供的这条出路在新画布上没有。
      probe_run: True

## 移动与拖拽（MoveTool / set_move_preview / panel_bridge / 整组位移 / 画布平移）  (11)
- [52] **high** 选中一个实体后切到「移动」工具，在画布任意空白处按下并拖动，会把那个实体整个拖走
      v1: tools/editor/editors/scene_editor.py:589 —— 把手图元带 ItemIsMovable，位移由 Qt 派发给**被按住的那个图元**；scene_editor.py:3944-3971 的 release 也是按 `press_pos.get(id(it))`
      v2: tools/editor/editors/scene_v2/tools_builtin.py:196-215 —— `MoveTool.mouse_pressed` 只判断 `button==Left and self._doc.selection`，**完全没有命中判定**：不问落点下有没有图元、
      impact: 误拖：手一抖就把上一个选中的实体挪到几百单位外，而且视线在鼠标这边、根本看不见远处那个实体在动，很可能存盘后才发现。同时「点空白取消选中」在移动工具下也不可能（press 恒被吃掉）。
      probe_run: True
- [53] **high** 工具栏上的「整组位移」按钮是死的：选中它以后画布对鼠标完全无反应
      v1: tools/editor/editors/scene_editor.py:3782-3800 / 3919-3927 —— v1 没有模式工具，组框的选中与整组拖动就在画布主手势里，按下组框边线即可拖；`_translate_group_members`(:13305) 由该路径直接驱动。
      v2: tools/editor/editors/scene_v2/tools_transform.py:286-357 —— `GroupMoveTool` 只实现了 `mouse_moved/mouse_released`，**没有 `mouse_pressed`**，手势唯一入口是 `begin()`
      impact: 一个名字最像「我要整组挪」的按钮把画布变成死的；用户会以为编辑器卡了。真正的整组拖动藏在名叫「分组」的另一个按钮后面。
      probe_run: True
- [54] **medium** 拖 NPC 时巡逻路线跟着一起动，松手又弹回原处（预览挪了、数据没挪）
      v1: tools/editor/editors/scene_editor.py:14498-14511 —— 单实体拖 NPC 的 live 分支只 pop 巡逻预览态、重画碰撞面、同步 x/y 数值框，**从不碰 patrol overlay**；release 的 :14572-14586 同样不写 
      v2: tools/editor/editors/scene_v2/view.py:474-489 `set_move_preview` 对 `items_of(ref)` 的**每一个 part** 贴预览位移，而 patrol 是 NPC 的 part 之一（scene_canvas_model.py:
      impact: 同一个「挪这个 NPC」的意图，鼠标拖和方向键给出两种相反结果；而鼠标那条还在拖动全程用画面骗人（看着路线跟来了，松手才发现没跟）。用户很容易据此以为路线已经跟着走了。
      probe_run: True
- [55] **medium** 整组位移无视分组的「不带巡逻路线」开关（editor.movePatrol=false），照样把路线一起挪走
      v1: tools/editor/editors/scene_editor.py:13327 `move_patrol = self._group_move_patrol(sc, gid)` + :13334-13339 `if kind == "npc" and move_patrol:` —— 只有开关
      v2: tools/editor/editors/scene_v2/tools_transform.py:220-260 `translate_entities` —— 只要实体有 `patrol.route` 就**无条件**整条平移；:214-218 `translate_group` 直接转调它，全程
      impact: 用户明确关掉的开关被静默忽略，巡逻路线被改脏；而且路线偏移往往要进游戏跑一遍才看得出来。
      probe_run: True
- [56] **medium** 中键拖拽平移画布没有了
      v1: tools/editor/editors/scene_editor.py:3756-3760（中键按下 → `_middle_panning=True` + ClosedHandCursor）、:3820-3837（移动时改两条滚动条）、:3907-3911（松开复位）。方案书 artifact/D
      v2: tools/editor/editors/scene_v2/view.py:536-560 —— `mousePressEvent/mouseMoveEvent` 只把事件转给当前工具，没有任何中键分支；view.py:90 `setDragMode(NoDrag)`；全仓 grep `Middle
      impact: 大场景导航手感直接退化；这是策划每天都在用的动作。
      probe_run: True
- [57] **medium** 工具栏勾不掉：点过的工具按钮全部保持按下态，且启动时当前工具那个是没勾的
      v1: v1 没有模式工具栏（拖/选是同一手势，scene_editor.py:589 + :3938-3989），因此不存在「看不出当前是哪个工具」这个问题 —— 这条属于 v2 自己坏掉的。
      v2: tools/editor/editors/scene_v2/page.py:317-322 —— 循环里 `act.setCheckable(True)` 之后只接了 `triggered → tools.select(t)`，**没有 QActionGroup、没有互斥、也没有在 tool_cha
      impact: 用户没法从界面判断现在是什么工具 —— 而在 v2 里「现在是什么工具」决定了按下鼠标会发生什么（见前两条：移动工具会甩实体、整组位移工具让画布装死），认错工具的代价很高。
      probe_run: True
- [58] **medium** 拖动一个热点穿过另一个热点时，前后遮挡关系不跟着变，松手才突然翻过来
      v1: tools/editor/editors/scene_editor.py:14495 —— live 拖动分支里每一帧都调 `_resort_canvas_content_z()`（注释写明「热点没有动画 runtime，走不到 tick 那条重排；拖动改了脚底 y 就得重排」）。
      v2: tools/editor/editors/scene_v2/tools_builtin.py:216-230 —— 手势中只更新 `self._offset`，不发任何文档事件；page.py:436-447 `_on_doc_changed → resort_content_z()` 只有命令落地
      impact: 拖动过程中看到的层叠关系是错的，用户要靠松手后那一跳才知道最终效果，摆图要反复试。
      probe_run: True
- [59] **low** 拖动中右侧属性面板的 x/y 数值框不跟着走，松手才跳到新值
      v1: tools/editor/editors/scene_editor.py:668-681（图元 `itemChange` 每帧发 `item_position_live`）→ :14482-14496 hotspot 分支 `self._props.sync_hotspot_xy_widgets(e
      v2: tools/editor/editors/scene_v2/panel_bridge.py:206-215 —— 面板只在收到 `EntitiesChanged` 时 `sync_from_selection()`，而该事件只由命令的 `_apply` 发出（commands.py:76-83），即
      impact: 想拖到某个精确坐标时没有实时读数，只能松手看一眼、不对再拖一次。
      probe_run: True
- [60] **low** 纯水平（或纯垂直）拖动会把没动的那一轴的整数坐标写成小数
      v1: tools/editor/editors/scene_editor.py:14562-14572 `_on_item_moved_impl` 用 `keep = self._props._keep_num`(:6380-6388「未改动的数值按原始 int/float 表示回写（1376 不漂成 1
      v2: tools/editor/editors/scene_v2/tools_builtin.py:239-242 —— `values.append({"x": round(sx+dx,1), "y": round(sy+dy,1)})`，两轴无条件重算成 float，没有任何表示保真；对照同仓 too
      impact: 拖一下就在 JSON 里留下与本次操作无关的 `100 → 100.0` 改动，git diff 变噪音；本仓的数值往返保真契约（commands.py:249-256 自己也写了这一条）在鼠标拖这条路径上是破的。
      probe_run: True
- [61] **low** 整组位移 / 方向键微移把成员坐标的小数位截断到 1 位
      v1: tools/editor/editors/scene_editor.py:13284-13303 `_shift_point_dict`：`px = max(1, _decimals_of(ox), _decimals_of(dx))` 后 `round(float(ox)+dx, px)` —— 
      v2: tools/editor/editors/scene_v2/tools_transform.py:262-281 `_shift`：只守住「零位移不碰」和「int 仍是 int」两条，其余一律 `round(out, 1)`。
      impact: 整组挪一下，组里所有多位小数坐标被静默抹掉小数位（1308.14 → 1308.1），画面上看不出来，但数据被改了且不可逆（撤销能回，存盘后不能）。
      probe_run: True
- [62] **medium** 【域外顺带确认】同一场景被重载一次（主窗程序化切页 / 跳转 / Task 编排后）之后，内容层 z 全部塌成 0，前后遮挡关系整体失效
      v1: tools/editor/editors/scene_editor.py:11914 `_resort_canvas_content_z` 在每条重载/刷新路径上都被显式调用（:12005、:12372、:12598、:13412、:14142、:14470…），没有跨次缓存。
      v2: tools/editor/editors/scene_v2/sorting.py:78-92 `assign_content_z` 的脏检查键是 `(tie, round(z,4), ref_key)` —— **不含图元身份**；page.py:184-190 `load_scene()` 会重建
      impact: 应该被挡住的贴图跑到前面来，摆好的遮挡关系看起来全乱了，而数据其实没变 —— 用户会去改数据「修」一个根本不存在的数据问题。
      probe_run: True

## 内容层前后次序（z 排序）  (4)
- [63] **high** 新画布走过 reload_from_model() 之后，全场内容图元的 z 全变成 0，前后次序整套失效（老画布不会）
      v1: tools/editor/editors/scene_editor.py:14644 `reload_from_model()` → `_load_scene(scene_id, reset_view=False)`。v1 是在**同一块画布**上原地重建：拆场景那一刻内容集合先变空，`_resor
      v2: tools/editor/editors/scene_v2/page.py:486 `reload_from_model()` → :164 `load_scene()`：**新建** SceneDocument + 新建 SceneView（全部图元是全新对象，DisplayImageItem /
      impact: 策划在新画布里排好的前后遮挡关系，只要离开该页再回来就整套消失，画面退化成 JSON 数组顺序；而且没有任何提示、图元还在、拖一下不相干的东西也不一定救得回来（除非某次编辑让排序键真的变了）。这正是重建要消灭的「编辑器骗人」——看着对、跑起来不对，且一声不吭。
      probe_run: True
- [64] **medium** 画布手势（拖动 / gizmo 旋转缩放）进行中不重排，前后关系要松手才跳一下
      v1: tools/editor/editors/scene_editor.py:14495 `_on_item_position_live()` 里拖动每一帧写完 staging 就 `self._resort_canvas_content_z()`（注释原文：「热点没有动画 runtime，走不到 ti
      v2: tools/editor/editors/scene_v2/tools_builtin.py:167 MoveTool 明写「手势期间不写数据 —— 只记偏移，release 时一条命令落地」，:224 `mouse_moved` 只更新 `self._offset`；view.py:474 `se
      impact: 「把这个人挪到树后面」这类调层动作在新画布里失去所见即所得：拖动过程中看到的层级是错的，只能靠松手试错反复调。实体密集的场景（雾津街头 30+ 项）里，一次调层要来回松手好几遍。
      probe_run: True
- [65] **medium** 排序脚底 y 漏乘透视系数：配了 perspectiveScale 的场景里旋转过的实体，新画布算出的层级与运行时/老画布都不同
      v1: tools/editor/editors/scene_editor.py:11871-11873 热点侧 `pf = self._canvas.persp_factor(hs, "hotspot")` → `s = entity_scale_of(hs) * pf` → `foot = sort_f
      v2: tools/editor/editors/scene_v2/sorting.py:63-65 热点侧 `s = entity_scale_of(ent)` / `foot = sort_foot_y_of(ent, ww * s, hh * s)`，:73-74 NPC 侧 `s = entity_
      impact: 编辑器预览撒谎且不报错：策划照着新画布排好的前后关系，进游戏是另一个结果。触发面窄（要同时有 perspectiveScale 与旋转，当前 28 个场景里 rotation 用得还很少），但一旦踩上就属于最难查的一类 —— 两个画布都不报错、结果不同。
      probe_run: True
- [66] **medium** 「画布预览巡逻」复选框在新画布里点了完全没反应，连带每拍重排也整套没有
      v1: 面板控件在 scene_editor.py:9317-9319 定义（QCheckBox「画布预览巡逻（不写回 x,y）」→ `npc_patrol_preview_changed` 信号，:4577）；SceneEditor 在 :11597 `self._props.npc_patrol_pre
      v2: tools/editor/editors/scene_v2/panel_bridge.py:148-149 —— 桥**只**连了 `panel.changed` 与 `document.changed`，`npc_patrol_preview_changed` 一根线都没接。scene_v2 全目
      impact: 控件在新画布里照常显示、照常可勾，勾了没反应 —— 用户会以为是场景配错了。同时失去唯一一种「看动起来的前后关系」的手段：巡逻路线穿过遮挡物时到底会不会被挡住，在新画布里只能靠脑补脚底 y。
      probe_run: True

## 多边形/折线编辑（Zone 多边形、hotspot/npc 碰撞面、NPC 巡逻路线、光环境曲线）  (6)
- [67] **high** 光曲线上插入/删除一个控制点，会把它后面每一个控制点的 env 光照关键帧整体错位一格（静默改光）
      v1: tools/editor/editors/scene_editor.py:1743 插点是 `self._points.insert(best_i + 1, {"x": mx, "y": my, "env": copy.deepcopy(a["env"])})` —— 往列表里塞一个新 dict，其
      v2: tools/editor/editors/scene_v2/tools_builtin.py:323-339 `_write_values` 的 lightcurve 分支：`old = light_curve_points(ent)` 拿的是**旧**点列，然后 `for i, (x, y) in
      impact: 作者只想在路径中间加一个控制点，结果整条路径后半段的打光关键帧全部前移一格、末帧被复制。编辑器里没有任何提示，画面变化要下一次进游戏走那条路才看得出来；而这条曲线正是「玩家位置插值光照」的唯一数据源。
      probe_run: True
- [68] **high** 属性面板里整块光环境曲线编辑（关键帧编辑器 / 添加点 / 删除 / 上移下移）在新画布上完全写不进模型，切页关窗静默丢弃
      v1: tools/editor/editors/scene_editor.py:6999 `commit_scene_staging_to_source()` 把 `_staging_scene` 的非列表字段整份提交回模型，由 :14725（`_commit_pending_scene_edits`，c
      v2: tools/editor/editors/scene_v2/panel_bridge.py:130-134 `_PANEL_BINDING` 只有 hotspot / npc / zone，**没有 scene**；:199-222 `sync_from_selection` 对场景级永远把 `_l
      impact: 光曲线的 env 关键帧只能在这个面板里编，画布没有第二个入口 —— 等于新画布里打光关键帧根本不能编辑。而且没有光曲线的场景在新画布里连一条都建不出来（画布双击插点要求先命中已有边线，view.py:221 又规定点列为空就不建图元）。作者以为改好了，切页就没了，且不会弹任何「有未保存修改」提示。
      probe_run: True
- [69] **high** 光曲线控制点在画布上根本看不见（无顶点手柄、也没有 v1 那套光照 gizmo），却仍能被误拖误删
      v1: tools/editor/editors/scene_editor.py:1580 `_paint_light_gizmo` 对**每一个**控制点恒画一套可视化：主光方向箭头+主光色圆盘、环境光色环、影迹（长度随仰角、暗度随 darkness）、脚下接触阴影椭圆（随 contactSize）、编号
      v2: tools/editor/editors/scene_v2/entity_items.py:209-218：顶点手柄 `if not self._selected: return`，只有选中态才画点。而 view.py:409-412 `_apply_selection` 的选择集来自 `docum
      impact: 两头都吃亏：想编光曲线的看不到控制点在哪、只能盲拖那条线；不想编的又会在编别的多边形时把它拖脏（tools_builtin.py:342-366 的 `_target_parts` 把光曲线做成「永远在列」的候选，命中半径 10 屏幕像素，缩小视图后折合很大一片世界范围）。配合第一条 env 错位，误删一个看不见的控制点还会连带把后面所有关键帧移位。
      probe_run: True
- [70] **medium** 属性面板的「画布预览巡逻（不写回 x,y）」勾选框在新画布上是死控件
      v1: tools/editor/editors/scene_editor.py:9317-9320 建勾选框并接 `_on_npc_patrol_preview_toggled`（:9510）→ 发 `npc_patrol_preview_changed`；:11597-11598 SceneEditor
      v2: `grep -rn "npc_patrol_preview_changed" tools/editor/editors/scene_v2/` 零命中。tools/editor/editors/scene_v2/page.py 对共享面板只有一处引用（:183 `load_scene_props`），
      impact: 调巡逻路线时唯一能「看它走一遍」的手段没了，只能靠脑补路点顺序对不对。控件还在、还能勾上，所以用户会以为是路线数据配错了而去改数据。
      probe_run: True
- [71] **medium** 鼠标停在某个顶点上按 Delete：v1 删那一个顶点，v2 把整个实体删掉
      v1: tools/editor/editors/scene_editor.py:15610 `_on_delete_key_shortcut` 先试 `_try_delete_zone_hovered_vertex()`（:15603），命中就调 :1064 `try_delete_hovered_ver
      v2: tools/editor/editors/scene_v2/tools.py:108-109：`if key in (Qt.Key.Key_Delete, Qt.Key.Key_Backspace): return self._delete_selection()` —— 基类统一处理，Polygo
      impact: 用惯老画布的人以为在删一个顶点，实际整个 Zone / 热点没了。Ctrl+Z 能回来，但要先意识到发生了什么；顶点又没有悬停高亮，「我到底停在哪个点上」也看不出来。
      probe_run: True
- [72] **low** 「锁定 Zone / 碰撞多边形点选」这个视图开关在新画布里没有
      v1: tools/editor/editors/scene_editor.py:11379-11387 左栏勾选框「锁定 Zone / 碰撞多边形点选」，接 :14181 `_on_block_zone_pick_toggled` → :2520 `SceneCanvas.set_zone_pick_fr
      v2: `grep -rn "pick_frozen|frozen" tools/editor/editors/scene_v2/` 零命中；page.py:317-322 的工具栏只放工具按钮，整个 v2 页没有任何视图开关控件。
      impact: 大面积 Zone / 碰撞面盖住其它实体时，选中下层实体要反复点、每次还得先经过 Zone，且拖到一半容易把 Zone 顶点带走。属于能绕过但很烦的一类，不是数据损坏。
      probe_run: False

## NPC 精灵预览与动画运行时（v1 _SceneNpcAnimRuntime / npc_scene_anim_refresh_requested / npc_patrol_preview_changed → v2 content_items.SpritePreviewItem + view.set_sprite_metrics_provider + page._npc_sprite_metrics）  (5)
- [73] **high** v2 把整张图集（全部帧）压进 NPC 的世界框里画，每个 NPC 都变成一张缩微的贴图接触表
      v1: tools/editor/editors/scene_editor.py:497 —— `_SceneNpcAnimRuntime.draw_at` 里 `pm = _crop_atlas_cell(self.atlas, self.cols, self.rows, idx, cell_w=...,
      v2: tools/editor/editors/scene_v2/page.py:244-274 `_npc_sprite_metrics` 返回的第三项是 `str(sheet_path)`，即**整张 atlas.png 的路径**；page.py:230-241 `_load_texture` 原样
      impact: 新画布上没有一个 NPC 长得对。策划没法凭画布判断这个角色是谁、朝哪边、占多大地方、有没有摆歪；摆位、比例、遮挡关系全部只能靠猜。这是新画布 NPC 预览最直接的、开场就能看见的坏。
      probe_run: True
- [74] **high** v2 的精灵完全不动 —— 没有任何动画驱动，refresh_frame 是死代码
      v1: tools/editor/editors/scene_editor.py:11239-11243 建了 8ms 的 `QTimer`（PreciseTimer）接到 `_tick_scene_npc_anims`；12322-12374 每拍推进帧游标、拉 staging 坐标、重求透视；362-5
      v2: `tools/editor/editors/scene_v2/` 全目录 grep 不到 `QTimer` / `QElapsedTimer` / `startTimer`（只有 changes.py:8 的注释提了老画布的 singleShot）。`content_items.py:175 ref
      impact: 策划再也没法在画布上确认「这个 NPC 配的动画到底能不能播、播的是哪段、帧率对不对、循环有没有断」。以前一眼能看出的「图集切错了 / frames 写反了 / 这状态根本没帧」现在必须开游戏才知道。
      probe_run: True
- [75] **high** 巡逻预览在 v2 整个没了：面板上那个「画布预览巡逻」复选框还在、还能勾，勾了什么都不发生
      v1: 面板侧 tools/editor/editors/scene_editor.py:9317-9320 建 `_npc_patrol_preview` 复选框并接 `_on_npc_patrol_preview_toggled`（9510-9516）发 `npc_patrol_preview_chan
      v2: v2 复用的正是同一个 `ScenePropertyPanel`（page.py:119-120 `from ..scene_editor import ScenePropertyPanel`），所以复选框照样显示；但 `panel_bridge.py:148-149` 只 connect 了 `p
      impact: 一个看得见、点得动、但完全无效的控件——比缺功能更糟：策划会以为「勾了预览、它不动 = 我的路线/速度配错了」，去改一堆本来没问题的数据。真正需要的「路线走不走得通、速度合不合适、走路动画对不对」也验证不了。
      probe_run: True
- [76] **medium** v2 完全无视 initialAnimState 与 initialAnimPlayback（仓库里 107 / 13 个 NPC 在用）
      v1: tools/editor/editors/scene_editor.py:12195-12208：`want = npc['initialAnimState']` → 命中则用它，否则回退 `idle`，再否则取第一个状态；取该状态的 `frames` / `frameRate` / `loop` 
      v2: `tools/editor/editors/scene_v2/` 全目录 grep 不到 `initialAnimState` / `initialAnimPlayback` / `states` / `frames` / `holdFrame` / `startFrame` / `referenc
      impact: 这两个字段的**唯一**可视化校验入口就是画布预览。在新画布上改它们等于盲改：状态名拼错、这个状态在 anim.json 里不存在、holdFrame 超出帧数——全都没有任何反馈，只能进游戏才发现。
      probe_run: True
- [77] **medium** v2 不给 NPC 精灵施加场景透视系数，画布上的 NPC 比运行时该有的尺寸大一大截
      v1: tools/editor/editors/scene_editor.py:12263 建 runtime 时 `rt.persp = self._canvas.persp_factor(pos0, 'npc', nx, ny)`，12369（普通）/12354（巡逻）每拍按脚底点重求；`draw_a
      v2: `entity_perspective_factor` 全仓只被 `scene_editor.py:69,2593` 和它的单测引用，`tools/editor/editors/scene_v2/` 下 grep 不到。view.py:270-288 `_content_spec` 返回的 scal
      impact: 在配了近大远小的场景里，策划照着新画布调 NPC 的位置与 scale，进游戏一看全是另一个尺寸；「这个人站在这里合不合身」这个判断在新画布上是错的。老画布特意做了这一层就是为了不撒谎。
      probe_run: True

## 光环境 / 打光（lightEnv、lightEnvCurve、统一光影 lighting 灯位、画布放灯、运行时双向同步）  (6)
- [78] **high** v2 里「统一光影 lighting（灯位）」整段面板全是摆设：加灯/删灯/换灯型/改任何参数/改玩家阴影绑定，一个字节都写不进模型，Save All 后全丢，还不弹任何提示
      v1: tools/editor/editors/scene_editor.py:6390 `_flush_scene_widgets_into()` 在末尾调 `self._writeback_scene_lights(sc)`（:6467），把面板工作副本 `_sc_lighting` 连同 `play
      v2: tools/editor/editors/scene_v2/panel_bridge.py:130-134 `_PANEL_BINDING` 只有 hotspot/npc/zone 三族，**没有 scene**；:186 `sync_from_selection()` 取不到 binding 就 
      impact: 打光工作流在新画布上是完全残的：摆一晚上灯，存盘后场景 JSON 一个字节没变；因为模型压根没入脏，主窗口既不会在 Save All 时写这个场景，也不会在关窗/切工程时问「要保存吗」，属于零提示的静默全损。同一段面板里的 `playerShadowBindings`（玩家阴影绑定）一并丢。
      probe_run: True
- [79] **high** v2 里「光环境曲线 lightEnvCurve」的关键帧编辑器与增删/排序按钮同样不落库：改主光方位角/仰角/强度/颜色/阴影暗度、点「添加点」「删除」「上移」「下移」，全部存不下
      v1: tools/editor/editors/scene_editor.py:5541-5550 建「添加点/上移/下移/删除」四个按钮，:5555 `self._lc_env_editor = _LightEnvKeyframeEditor()`（关键帧编辑器本体在 :4380）。数据入口 `_on_
      v2: 与上一条同根：tools/editor/editors/scene_v2/panel_bridge.py:130-134 `_PANEL_BINDING` 无 scene 项，`_on_panel_changed` 对场景面板恒早退。v2 只在**画布**一侧接了光曲线（view.py:71 `_S
      impact: 新画布上只能把光曲线的控制点**拖到别处**，不能改任何一帧的光本身。想调一处光就必须切回老画布，等于「打光只能在 v1 里做」。而且面板改完 v1 会亮红色「● 未应用」提示，v2 页没有任何提示，作者以为改好了。
      probe_run: True
- [80] **high** v2 里「在画布上定位选中的灯」按钮点亮后，在画布上点击毫无反应——按钮还在、还能按下去，就是不放灯
      v1: tools/editor/editors/scene_editor.py:5635 建按钮「在画布上定位选中的灯」（setCheckable），:6680 `_on_sl_place_toggled` 发 `light_place_mode_changed`(:4573)；:11626 `self.
      v2: tools/editor/editors/scene_v2/ 全目录 grep `light` 只命中 lightcurve 相关（light_curve.py / tools_builtin.py:323,364 / view.py:36,71,221,369）——`light_place_mod
      impact: 摆灯唯一顺手的入口没了。灯位是 3D 伪世界坐标（x/y/z），面板表单里根本没有 x/y 输入框——`pos` 只能靠「画布点一下取地面深度」得到。所以在 v2 页上，新加的灯永远停在场景中心的缺省位置，作者没有任何办法把它挪到想要的地方。
      probe_run: True
- [81] **high** v2 画布上给光曲线插点或删点，会把所有关键帧的 env 整体错位一格——后一帧的光被前一帧覆盖，最后一帧的光直接消失
      v1: tools/editor/editors/scene_editor.py:1758 `contextMenuEvent` 删点是 `del self._points[vi]`——整个点（含 env）作为一个单元被摘掉，其余各点的 env 原地不动；:1743 双击插点是 `self._points.
      v2: tools/editor/editors/scene_v2/tools_builtin.py:323-339 `_write_values` 的 lightcurve 分支按**位置下标**重新配对 env：`src = old[i] if i < len(old) …`（:330）。可 `worl
      impact: 这是 v2 独有的数据损坏，且完全静默：画布上折线的形状是对的，面板关键帧表也不会刷新，作者要等到进游戏走到那一段、发现光不对，才可能察觉。而 v2 页又改不了关键帧（见上一条），撞上之后连修都修不了，只能切回老画布重调。
      probe_run: True
- [82] **medium** v2 页照样参与与游戏的灯位双向实时同步，但只会把面板里的灯发给游戏；游戏里 F3 摆好的灯拉回来只进面板不进模型，弹窗还写着「记得 Save All 才落盘」而 Save All 什么都不写
      v1: tools/editor/main_window.py:2638 `_lighting_sync_panel()` 用 `getattr(ed, "_props", None)` + `hasattr(panel, "sync_lighting_snapshot")` 鸭子识别当前页；:2707 `
      v2: tools/editor/editors/scene_v2/page.py:121 `self._props = ScenePropertyPanel(model)` —— v2 页也有 `_props` 且就是同一个类，所以主窗口那套鸭子识别对它同样成立、同步照跑；但落库那一半被 panel_br
      impact: 「在游戏里摆灯、在编辑器里落盘」这条主推的打光工作流在新画布上是断的，而且断在最坏的地方：界面全程给正反馈（表变了、状态行显示同步中、弹窗明说「按 Save All 就好」），只有落盘那一步是空的。作者按提示做完，成果照样丢。
      probe_run: True
- [83] **medium** v2 画布上的光环境曲线退化成一条普通青色折线：没有主光方向箭头、影迹长度/暗度、环境光色环、接触阴影椭圆，也没有 az/el/I/dk 数值读数；面板选中的关键帧在画布上也不高亮
      v1: tools/editor/editors/scene_editor.py:1570-1580 `_LightCurvePolyline.paint()` 对每个控制点调 `_paint_light_gizmo()`(:1580)：按 env 画主光来向箭头与颜色、按 `cot(elevation)`
      v2: tools/editor/editors/scene_v2/view.py:71 `_SCENE_PARTS = {"lightcurve": PolylineItem}`，而 entity_items.py:229 `PolylineItem` 就是巡逻路线用的同一个类，:233 缺省色 `QCo
      impact: 光曲线在画布上的存在意义就是「一眼看出这一段的光从哪来、影子多长多黑」——v2 把这层信息整个抹掉了，作者只能靠盯右侧表格里的数字反推。加上关键帧表选中与画布不联动，曲线一多就分不清屏幕上哪个点对应表里哪一行，等于失去了在画布上定位关键帧的能力。
      probe_run: True

## 新建 / 删除 / 复制实体，右键菜单，快捷键  (13)
- [84] **high** 画布右键菜单在 v2 整体不存在：「在此添加 Hotspot/NPC/Zone/命名出生点」和分组框右键三项全部没有
      v1: tools/editor/editors/scene_editor.py:3714-3745 SceneCanvas.contextMenuEvent：先让顶点 item 处理删顶点，命中分组框走 _exec_group_context_menu，否则在落点弹四项「在此添加 Hotspot / NP
      v2: tools/editor/editors/scene_v2/ 全包 grep QMenu = 0 次（探针统计）；SceneView 未覆写 contextMenuEvent（探针：SceneView.contextMenuEvent is QGraphicsView.contextMenuEven
      impact: 老画布最常用的建实体方式（在想要的位置右键就地新建）在新画布消失；对着分组框右键也没有「选中全部成员 / 删除该分组」入口
      probe_run: True
- [85] **high** 命名出生点（spawnPoints）在 v2 完全不能新建 / 删除 / 复制，但它照样显示在实体树和画布上
      v1: scene_editor.py:3738 右键「在此添加命名出生点」→ 15573 _add_spawn_at；11269 工具栏 +Add Entity 里的「Spawn Point」；15825-15866 _delete_selected_impl 里 spawn 与三类实体同路删除（只挡 d
      v2: tools/editor/editors/scene_v2/commands_structure.py:18 LIST_KEY 只有 hotspot/npc/zone；tools_structure.py:106-118 create_entity_at 对 kind 不在 LIST_KEY 直接 
      impact: 出生点看得见、拖得动，却删不掉也建不了；要加一个新的传送落点只能回老画布或手改 JSON
      probe_run: True
- [86] **high** 选中分组框后按 Delete，会静默删掉「之前选中的那个实体」而不是分组；v2 根本没有删除分组的路径
      v1: scene_editor.py:15810-15818 _delete_selected_impl 里 group ref 单独分流到 _delete_scene_group；15867-15915 _delete_scene_group 先扫入站引用、有引用就阻断，再弹确认框说明会把 N 个成员移
      v2: tools/editor/editors/scene_v2/tools_overlays.py:213-225 GroupBoxTool.key_pressed 只处理方向键，其余（含 Delete）交回 tools.py:108 的基类 _delete_selection()，而它删的是 docu
      impact: 想删一个分组，结果分组还在、另一个无关实体被无声删掉；用户不会立刻发现，Ctrl+Z 也要先意识到删错了东西
      probe_run: True
- [87] **high** Ctrl+D 复制不剥离 cutsceneIds / cutsceneOnly：副本变成「仅过场实体」，正常游戏里永远不显示，且没有任何提示
      v1: tools/editor/shared/entity_refactor.py:1604-1605 duplicate_entity 里 `stripped = dup.pop('cutsceneIds')` + `dup.pop('cutsceneOnly')`，理由写在 :1574-1577；sc
      v2: tools/editor/editors/scene_v2/tools_structure.py:147 `clone = copy.deepcopy(src)`，随后只改 id 与 x/y/polygon，没有任何字段剥离与提示（:131-175 全部）
      impact: 在有过场绑定的场景里复制实体，副本在画布上看得见、在游戏里却永远不出现（cutsceneOnly 语义），排查时完全没有线索
      probe_run: True
- [88] **medium** 属性面板底部的「从场景删除」按钮（热区/NPC/Zone/出生点四个面板都有）在 v2 点了没反应
      v1: scene_editor.py:4982-4991 _append_entity_delete_footer 建按钮并 clicked -> delete_current_entity_requested.emit（调用点 7822/9363/10435/10883/11124，出生点面板那个是 _
      v2: tools/editor/editors/scene_v2/page.py 复用同一个 ScenePropertyPanel（page.py:120-122），但全文件只在 :183 用过 self._props；panel_bridge.py 只连了 panel.changed（:148）。全仓 
      impact: 一个显眼的删除按钮成了死按钮，用户会以为编辑器卡住或数据被锁；出生点面板的删除按钮同理（那还是出生点唯一可见的删除入口）
      probe_run: True
- [89] **medium** 焦点在左侧实体树时 Delete / Ctrl+D 全部失效，而实体树又没有右键菜单——在树里选中的实体删不掉也复制不了
      v1: scene_editor.py:11675-11680 Delete/Backspace 用 QShortcut + WidgetWithChildrenShortcut 挂在整页上（树、面板、画布任一处聚焦都生效）；11304-11311 Ctrl+D 的 QAction 同样 addAction
      v2: 快捷键只在 tools/editor/editors/scene_v2/view.py:580 的 SceneView.keyPressEvent 里分发（→ tools.py:98-118）；page.py 里 self._tree 只接了 itemSelectionChanged（page.py
      impact: 用户习惯在左树里点名字选实体再按 Delete；在新画布里这条路完全没反应，右键也不弹菜单，必须先把焦点点回画布才行
      probe_run: True
- [90] **medium** 属性面板的「在画布上定位选中的灯」在 v2 断线：按钮按下去、模式也进了，点画布却永远落不了灯
      v1: scene_editor.py:2392 light_place_requested 信号、3086-3088 set_light_place_mode、3751-3754 mousePressEvent 最前面拦截并 emit；11625-11626 canvas.light_place_requ
      v2: tools/editor/editors/scene_v2/ 全仓 grep light_place / place_selected_light_at / set_light_place_mode = 0 次（探针统计）；SceneView 没有 set_light_place_mode 属性
      impact: 摆灯只能手填坐标；用户会反复点画布并以为深度图有问题（老画布在这里会给「本场景没有深度图」这类明确提示，新画布连提示都没有）
      probe_run: True
- [91] **medium** 「安全删除（引用报告）」等重构入口在 v2 整体消失：删实体前看不到有多少处引用会悬垂
      v1: scene_editor.py:11302-11330 工具栏「重构」菜单：复制实体（Ctrl+D）/ 迁移到场景… / 重命名 id… / 安全删除（引用报告）… / 转为 NPC… / 撤销上次重构；15645-15716 _refactor_selected 会先全项目扫引用、弹预览对话框，删
      v2: tools/editor/editors/scene_v2/page.py:317-322 工具栏只 addAction 各个 tool 的 display_name，没有任何重构/删除菜单；scene_v2 全仓没有 import entity_refactor（唯一的删除实现是 tools_st
      impact: 删掉一个被引用的实体后毫无提示，悬垂引用要等到跑 Validate Data 才发现；改名 / 跨场景迁移在新画布也彻底没有入口
      probe_run: True
- [92] **medium** 新建 / 复制的 id 取号不查 npc↔hotspot 共用命名空间，能造出编辑器自己禁止的同名数据
      v1: scene_editor.py:14987-14991 _ENTITY_ID_NAMESPACES 规定 hotspot 与 npc 共用一个 id 命名空间；15461-15476 取号时用 _namespace_entity_ids 扫两张表；14994-15006 _entity_id_con
      v2: tools/editor/editors/scene_v2/tools_structure.py:32-37 existing_ids 只读 LIST_KEY[kind] 那一张表；:40-64 unique_entity_id 因此看不见另一张表里的同名 id
      impact: 复制/新建后场景里出现一个热区和一个 NPC 同 id，属性面板自己的撞名闸禁止用户手输这种数据；后续按 id 解析的 emote/演出目标会指错对象
      probe_run: True
- [93] **medium** 复制 NPC 时 patrol.route 不跟着偏移，副本的巡逻路线仍钉在原实体那条路上
      v1: entity_refactor.py:1608-1616 duplicate_entity 里 x/y、polygon、collisionPolygon、patrol.route 一起按同一 offset 平移（注释明确说 collisionPolygonLocal 是局部系才不动）
      v2: tools/editor/editors/scene_v2/tools_structure.py:152-162 只平移 clone['x']、clone['y'] 和 clone['polygon']，没有碰 patrol.route
      impact: 复制一个巡逻 NPC，副本一进游戏就往原路线跑；画布上两条巡逻折线完全重叠，要一个个点重拖
      probe_run: True
- [94] **medium** 工具栏上三个新建工具全都叫「新建」，且工具勾选态从不同步——分不清哪个建热区、哪个建 NPC、当前又在哪个工具里
      v1: scene_editor.py:11265-11278 「+ Add Entity」下拉菜单四项分别写明 Hotspot / NPC / Zone / Spawn Point，点一次建一个，不进入持续模式
      v2: tools/editor/editors/scene_v2/tools_structure.py:88 CreateTool.display_name = "新建"（类属性，不随 kind 变）；page.py:309-312 注册三个 CreateTool；page.py:318-322 用 to
      impact: 只能靠猜或试点来挑要建的实体类型（建错了还得撤销）；工具栏也无法反映当前处于哪个工具，处在「新建」模式时每点一下画布就多一个实体
      probe_run: True
- [95] **low** 删除实体没有二次确认，Delete 一按即删且无任何提示
      v1: scene_editor.py:15835-15847 删除前按数量拼出提示语并走 confirm.confirm_delete(self, prompt)，用户取消就不删；默认出生点还会单独弹「默认出生点不可删除」
      v2: tools/editor/editors/scene_v2/tools.py:108-109 Delete/Backspace 直接调 _delete_selection()；tools_structure.py:120-129 delete_selected 里没有任何确认或提示
      impact: 误触 Delete（尤其在焦点不明确时）会直接删掉当前选择；虽然能 Ctrl+Z，但用户没有被告知刚删了什么，配合上面「分组模式下删错对象」那条更难察觉
      probe_run: True
- [96] **low** 新建实体的默认字段与 v1 不一致：热区少 label/data，NPC 的 name 变成 id，Zone 默认框尺寸与落位都不同
      v1: scene_editor.py:15496-15511 新热区 {id:new_hotspot_0, type:inspect, label:'', x, y, interactionRange:50, data:{text:''}}；15526-15536 新 NPC name='New NPC'
      v2: tools/editor/editors/scene_v2/tools_structure.py:25-29 _DEFAULTS 只有 type/interactionRange；:66-82 _new_entity 里 npc 的 name 直接取 id，zone 用以落点为中心的 80×80 方
      impact: 从新画布建的热区在数据里少了 label/data 两个 v1 一直会写的键，NPC 名字直接是 id（老画布是 'New NPC'，一眼能看出没改过名）；新建 Zone 的大小/位置与老画布不同，习惯了尺寸的人要重新拖
      probe_run: True

## 撤销 / 重做 / 脏标记 / 保存（scene_v2 vs scene_editor v1）  (6)
- [97] **high** 新画布的"场景级"属性面板编辑全部静默丢失：不入模型、不入撤销栈、不标脏、关窗零提示
      v1: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_editor.py:14618 `flush_to_model()` 在 `is_pending_dirty()` 时调 `_apply_
      v2: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_v2\page.py:183 `load_scene()` 里 `self._props.load_scene_props(...)` —
      impact: 在新画布上改场景名/世界尺寸/BGM/滤镜/日夜/出口锚点/走跑速/环境音/onEnter/光环境曲线/lighting/出生点属性——存盘后全部没有，Ctrl+Z 里也没有这一步，而且因为模型压根没被标脏，关窗时连"是否保存"都不会弹。左上角 v1 的红色"● 未应用"提示在 v2 里也没接线，用户得不到任何信号。
      probe_run: True
- [98] **high** 新画布面板改实体 id 没有撞名/空 id 闸：能写出两个同 id 的实体、或 id 为空的实体
      v1: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_editor.py:15008 `_preflight_entity_id_commit()` 在**任何** staging 写回 so
      v2: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_v2\panel_bridge.py:253 `commit_panel_edits()` 把 `diff_fields(staged, 
      impact: 运行时 getNpcById / 实体寻址是 first-wins，画布图元按 kind:id 建键会互相覆盖，动作参数与对话 owner 全部串台；空 id 的实体则彻底不可寻址。用户在新画布随手改个 id 就能把场景数据改坏，改坏时没有任何提示，要等 validate-data 才发现。
      probe_run: True
- [99] **high** 跨页：一边改的数据会被另一边的陈旧 staging 静默回灌覆盖（两个方向都会），且对方撤销栈已被清空
      v1: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_editor.py:6999 `commit_scene_staging_to_source()` 把 `_staging_scene` 
      v2: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_v2\document.py:212 `notice_external_scene_write()` 收到"别人改了这个场景"后**只 c
      impact: 两个画布并存时，在一边做的编辑会被另一边"什么都没碰过"的面板悄悄覆盖回旧值，画面上看不出来，没有任何提示。对方的撤销栈已被清空，只能靠在"作案"那一侧按 Ctrl+Z 连带救回——而那会同时丢掉刚做的那次编辑，且用户根本不知道要去撤。直接 Save All 就把回退后的数据落盘了。
      probe_run: True
- [100] **medium** 新画布属性面板的撤销粒度过粗：换过选中实体再改回来的两次编辑会被并成一条，撤销一次直接退回最初值
      v1: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_editor.py:14740 `_undo_flush_pending_as_command()` —— 每一条"离开当前编辑"的路径（
      v2: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_v2\panel_bridge.py:295 `mergeable=self._gesture_open`，:302 首次 push 成功
      impact: 用户以为撤销一步只退一步，实际一次退掉整个编辑会话；中间那个想留下的值既撤不到、redo 也只能跳到最后一次，只能手打回去。选择切换在 v1 是天然的撤销分界，在 v2 不再是。
      probe_run: True
- [101] **low** 新画布工具栏没有撤销/重做按钮（v1 有，且带可用态）
      v1: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_editor.py:11332-11347：工具栏上有"撤销"/"重做"两个 QToolButton，接 `_editor_undo`/`
      v2: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_v2\page.py:301 `_install_tools()` 里 `self._toolbar.clear()` 之后只为每个工具加
      impact: 从旧画布切过来的用户找不到撤销按钮，也失去了"当前还能不能撤"的视觉反馈，只能靠记住快捷键。
      probe_run: True
- [102] **medium** 新画布的 Ctrl+Z 不再交回文本框：在属性面板输入框里打字后按 Ctrl+Z，撤掉的是画布上一步编辑而不是刚打的字
      v1: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_editor.py:14767 `_focused_text_widget()` + :14777 `_editor_undo()` 头两
      v2: E:\GameDev\GameDraft\.claude\worktrees\canvas-rebuild\tools\editor\editors\scene_v2\page.py:502-508 `editor_undo`/`editor_redo` 各两行，直接 `self._doc.undo
      impact: 在属性面板里打字改错想退一格，结果整段回到原值或画布上别的东西被退回去，且这一步在文本框里不可逆（文本框自己的撤销历史没被用上）。
      probe_run: False

## 属性面板 ↔ 画布双向联动（scene_v2/panel_bridge.py + page.py 对 ScenePropertyPanel 的接线）  (11)
- [103] **high** 场景级属性面板（世界尺寸/BGM/滤镜/日夜/出口锚点/相机/行走跑动速度/深度容差/透视缩放/环境音/onEnter/光环境曲线/统一光影灯位）在 v2 里改了等于没改，模型一个字节都不变，也不标脏、不提示、关窗不拦
      v1: tools/editor/editors/scene_editor.py:14706 `_commit_pending_scene_edits` 是离开路径的统一提交口；14725 调 `props.commit_scene_staging_to_source()`（定义在 6999），把 `_st
      v2: tools/editor/editors/scene_v2/panel_bridge.py:130 `_PANEL_BINDING` 只有 hotspot/npc/zone 三族，没有 scene；panel_bridge.py:248 `_on_panel_changed` 在 `self._lo
      impact: 策划在新画布上做的所有场景级配置（含统一光影的整张灯表、光环境曲线、透视缩放、日夜开关、世界尺寸、BGM/滤镜/环境音/onEnter/出口锚点）全部无声蒸发。因为 model.is_dirty 保持 False，Save All 不会写、关窗/切工程也不会弹「有未保存改动」，用户没有任何一次被提醒的机会——只有下次打开场景发现全没了。
      probe_run: True
- [104] **high** 在面板里改实体 id 没有撞名闸也没有空值闸，直接写进模型：可以造出两个同 id 的热点/NPC/区域，或者 id 为空字符串的实体
      v1: tools/editor/editors/scene_editor.py:15008 `_preflight_entity_id_commit`：提交前逐个比对 `entity_staging_pairs()`，空 id 或同命名空间撞名一律弹 QMessageBox 并 `props.revert
      v2: tools/editor/editors/scene_v2/panel_bridge.py:253 `commit_panel_edits` 直接把 diff_fields 的结果（含 id）交给 build_change_fields_command；303 行只做了一件事——把 `self._l
      impact: 一次手滑改重名就把场景 JSON 写坏，且当场毫无反馈。之后运行时按 id 取实体是 first-wins（另一个永远取不到）、编辑器按 id 首匹配定位与删除（改属性/删除会作用到错的那一个），只有事后跑 validate-data 才发现。v1 是弹窗拦下并退回 id。
      probe_run: True
- [105] **high** 在面板里改实体 id 之后，该实体从画布上消失、左侧实体树仍显示旧 id、Document 选择悬垂，而且这次改名 Ctrl+Z 撤不回来
      v1: tools/editor/editors/scene_editor.py:14229-14232 一线：id 改动只进 `_staging_hotspot`，真正落库要走 14706 的提交链，提交后编辑器统一走 `_load_scene` 重投影（14834），画布图元与实体树按新 id 整体重建
      v2: tools/editor/editors/scene_v2/panel_bridge.py:303 改完 id 只更新桥自己的 `_loaded`，既不改 `document.selection` 也不通知页重建树；page.py:430 `_on_doc_changed` 只在 EntitiesA
      impact: 用户在面板里给热点改个名，画布上那个点当场不见了，只能切场景再切回来才看得见；这期间树还在显示旧名字，点树上的旧节点选不中任何东西。想反悔按 Ctrl+Z 也回不去——名字永久变了，而同一次编辑里的其它字段却被撤掉，等于撤销撤了一半。
      probe_run: True
- [106] **high** 属性面板底部的「从场景删除」按钮在新画布上是死按钮，点了什么都不发生
      v1: tools/editor/editors/scene_editor.py:4982-4991 `_append_entity_delete_footer` 给 hotspot(7822)/npc(9363)/zone(10435)/group(10883)/spawn(11124) 五个面板各挂一个
      v2: tools/editor/editors/scene_v2/page.py 全文没有一处 `delete_current_entity_requested`（全仓 grep 只命中 scene_editor.py 自己）。page.py:444 有 `delete_selected()`，但只留给快
      impact: 面板上明摆着的删除按钮点不动，用户会以为编辑器卡了或者这个实体删不掉，反复点几次也没反应，也没有任何错误提示。
      probe_run: True
- [107] **high** 取消选择 / 框选多个 / 选中出生点 / 选中分组之后，属性面板不切换，仍停在上一次那个实体的完整表单上，而此时在这个表单里做的任何编辑都被静默丢弃
      v1: tools/editor/editors/scene_editor.py:14285-14292 `_on_item_deselected`：点画布空白先 `_undo_flush_pending_as_command()` 提交，再 `load_scene_props(sc, clear_pend
      v2: tools/editor/editors/scene_v2/panel_bridge.py:199-217 `sync_from_selection`：`len(sel) != 1` 或 `_PANEL_BINDING.get(ref.kind) is None`（spawn/group 都不在表里
      impact: 面板和画布对不上号：用户以为自己在编辑当前选中的东西（或者刚点了别处），实际在一张已经跟选择脱钩、甚至属于已删实体的表单上打字，改完切走一看什么都没变，而且从头到尾没有一句提示。最阴的是删除之后那一版——面板还完整显示着已经不存在的实体。
      probe_run: True
- [108] **medium** 出生点与分组在新画布上根本没有属性面板可用：选中它们不会加载 load_spawn_props / load_group_props，分组面板的四个按钮与多选面板的三个批量按钮全部不可达
      v1: tools/editor/editors/scene_editor.py:11127 `load_spawn_props`（改出生点名字/坐标/朝向）与 10999 `load_group_props`（改分组 id/label/成员三列表）都是完整实现的面板页；11604-11611 把分组面板的
      v2: tools/editor/editors/scene_v2/panel_bridge.py:130 `_PANEL_BINDING` 只登记 hotspot/npc/zone；全仓 grep：scene_v2 目录里没有 `load_spawn_props`、`load_group_props`、`
      impact: 在新画布上无法给出生点改名、无法精确输入出生点坐标（只能在画布上拖）；无法改分组 id/label、无法增删分组成员、无法整组平移到指定坐标、无法「选中本组全部成员」或「重置锚点」；多选之后也没有批量指派分组/批量复制/批量删除的入口。这些在老画布里都是现成的面板功能。
      probe_run: True
- [109] **medium** 统一光影的「在画布上定位灯」整条链路在新画布上不存在：面板那个定位开关按下去没有任何效果，画布点击也永远不会落灯
      v1: tools/editor/editors/scene_editor.py:6680-6682 `_on_sl_place_toggled` → `light_place_mode_changed.emit(on)`；11626 `self._props.light_place_mode_change
      v2: 全仓 grep `light_place` / `set_light_place_mode` / `place_selected_light_at` 在 tools/editor/editors/scene_v2/ 下 0 命中；scene_editor.py:11626 与 11961 是仅有的两
      impact: 新画布上摆灯只能手填 pos 数值，最常用的「点哪儿放哪儿」没了；再叠加上面那条（场景面板编辑根本不落库），实际结果是新画布上完全没法给场景加灯或调灯。
      probe_run: True
- [110] **medium** 在属性面板里导入/更换背景图之后，新画布不刷新背景，也不按新图更新世界尺寸——画布还停在旧背景/空背景与旧世界矩形上
      v1: tools/editor/editors/scene_editor.py:6216-6239 背景导入把 backgrounds[0].image 与（首次导入时）worldWidth/worldHeight 同时写进 `_staging_scene` **和 `_source_scene`（就是模
      v2: tools/editor/editors/scene_v2/page.py:203 `_refresh_background` 的唯一调用点是 page.py:188（`load_scene` 里），没有任何别的触发；全仓 grep 不到 `scene_background_changed` 在 s
      impact: 换完背景画布毫无反应，用户会以为导入失败又导一遍；首次导入时世界尺寸已按图片写进模型，画布却还按旧尺寸摆实体，此时在画布上拖东西定位是照着错的世界矩形拖的。必须切走场景再切回来才对得上。
      probe_run: True
- [111] **medium** 透视缩放（perspectiveScale）在新画布上既没有 live 预览也存不下来：面板改完画布上的基准线与各实体预览都不动
      v1: tools/editor/editors/scene_editor.py:5909-5921 `_on_persp_widgets_changed` 在首次启用时自动生成默认竖直轴，然后 `perspective_preview_changed.emit(self._persp_cfg_previe
      v2: 全仓 grep `perspective_preview_changed` 在 scene_v2 下 0 命中。page.py:365 `refresh_perspective_axis` 只从 `doc.scene()['perspectiveScale']` 读**模型**里已有的近/远端；而面
      impact: 新画布上没法给场景配透视缩放：既看不到实时预览，也存不下配置，连画布上那根本该出现的近/远端轴都不会出现（工具栏里的「透视轴」工具因此没有可拖的东西）。
      probe_run: True
- [112] **medium** 面板里的出生点选取对话框直写场景数据后，新画布的 Document 完全不知情：目标场景恰好是当前场景时，新建/拖动出的出生点在画布和实体树上都看不见
      v1: tools/editor/editors/scene_editor.py:8320 `TargetSpawnPickerDialog(self._model, sid, ...)` —— 该对话框内「新建命名出生点」与拖图钉都是**直写 model.scenes[sid]**（4019 的 `_bt
      v2: tools/editor/editors/scene_v2/document.py:212 `SceneDocument.notice_external_scene_write` **是实现了的**，但全仓 grep 该方法在 v2 侧只有 document.py:209 那一处自广播调用；`sce
      impact: 用户明明刚建好一个出生点，回到画布却找不到它，会误以为没建成而重复新建，结果 spawnPoints 里堆出一串 spawn_1/spawn_2。v1 至少还会因为清栈把「这次改动栈外发生过」这件事表达出来，v2 连这一层都没有。
      probe_run: True
- [113] **low** NPC 面板的「预览巡逻走位」复选框在新画布上是死控件，勾不勾都没有任何变化
      v1: tools/editor/editors/scene_editor.py:9510-9516 `_on_npc_patrol_preview_toggled` 只做一件事——`npc_patrol_preview_changed.emit(id, checked)`（连 `_emit_props_c
      v2: 全仓 grep `npc_patrol_preview_changed` / `_patrol_preview_ids` / `_patrol_preview_advance` 在 tools/editor/editors/scene_v2/ 下 0 命中；v2 的 NPC 精灵是 content_
      impact: 想在编辑器里看一眼巡逻路线走起来对不对，勾了没反应，只能靠静态折线自己脑补，或者切回老画布。属于少一个可视化功能，不动数据。
      probe_run: True

## 画布之外但牵涉画布的地方：主窗口鸭子协议钩子、跨页刷新/跳转、属性面板接线、撞名闸/重构、左栏与视图轴 UI  (10)
- [114] **high** 新画布的场景级属性编辑（世界尺寸/名称/BGM/滤镜/日夜/exitAnchors/onEnter/灯表…）全部静默丢弃，而 flush_to_model / commit_pending_on_leave / confirm_close 三个门控钩子都恒返回 True，连保存提示都不弹
      v1: tools/editor/editors/scene_editor.py:14618 flush_to_model 在 _props.is_pending_dirty() 时调 _apply_props() 真提交；:14628 commit_pending_on_leave 走 _undo_flu
      v2: tools/editor/editors/scene_v2/page.py:465/474/478 三个钩子注释写明「本页没有 staging 层」故恒为 True；但 :121 直接复用了有 staging 的 ScenePropertyPanel，:184 load_scene 里还主动 loa
      impact: 整类场景级配置在新画布上根本存不进去，且因为三个钩子都说「我没有未提交编辑」，关窗/换工程/Save All 都不会提示，用户看到的是「我明明改了并保存了，重开全没了」。主窗口的光照实时同步（main_window.py:2651 靠 getattr(ed,"_props") 认页，v2 也吃这条）从游戏拉回来的灯同样只落在面板 staging 里，永远写不
      probe_run: True
- [115] **high** 新画布没有实体 id 撞名闸：在属性面板把 id 改成已存在的 id 会直接产生两个同 id 实体，改成空串也照写
      v1: tools/editor/editors/scene_editor.py:15008 _preflight_entity_id_commit —— 提交前遍历 entity_staging_pairs，空 id 或同命名空间撞名一律弹窗 + revert_entity_id 退回原 id（:1499
      v2: grep 全 tools/editor/editors/scene_v2/ 无 _preflight_entity_id_commit / _entity_id_conflict / entity_refactor / conflict 任何一处。id 走 panel_bridge.py:283 的
      impact: 同 id 实体在运行时 getNpcById 是 first-wins、画布图元按 kind:id 建键会互相覆盖、按 id 的动作引用直接串台；空 id 实体则彻底无法寻址。用户只是在 id 框里打了个字，数据就坏了，且没有任何提示，要等 Validate Data 才发现。
      probe_run: True
- [116] **high** 属性面板的 20 条「面板 → 画布/编辑器」信号在新画布一条都没接：面板底部「从场景删除」按钮点了没反应，「在画布上定位灯」开关、换背景图刷新、透视预览、巡逻 overlay、分组三按钮、未应用红字提示全部失灵
      v1: tools/editor/editors/scene_editor.py:11579-11629 逐条接线 interaction_range_changed / zone_polygon_changed / hotspot_collision_polygon_changed / npc_colli
      v2: tools/editor/editors/scene_v2/page.py:121 只 self._props = ScenePropertyPanel(model) 后直接 addWidget，全文件再无一处 self._props.<signal>.connect；panel_bridge.py
      impact: 右侧面板上一整排按钮/开关在新画布上是死的，点了毫无反馈也无报错，用户只会以为编辑器卡了。其中 scene_directly_written 没接还额外意味着：坐标点选器一类对话框直写别的场景之后，v2 的撤销栈不会做穿越防护，Ctrl+Z 有机会把那次直写错误回滚。
      probe_run: True
- [117] **high** 新画布的属性面板不会随选择切页：选中「出生点」仍显示上一个热点的表单（且改了不写任何数据），多选和取消选择同样卡在旧表单上
      v1: tools/editor/editors/scene_editor.py:4550 起的 ScenePropertyPanel 是 QStackedWidget，备有 _empty / _multi_panel（:4560 多选页「已选 N 个实体」）/ _scene_panel / _hotspo
      v2: tools/editor/editors/scene_v2/panel_bridge.py:130 _PANEL_BINDING 只有 hotspot/npc/zone；:200 sync_from_selection 对其它 kind、对 len(sel)!=1 一律只把 self._loaded
      impact: 出生点在新画布上完全没法改属性（改名、增删 spawnPoints、改坐标数值）——树里点得到、画布上看得见，右侧却是别的实体的表单。更危险的是这个表单看着是活的：用户以为在编辑出生点，实际敲进去的字既不写出生点也不写那个热点（_loaded 已是 None），一次静默空转。
      probe_run: True
- [118] **high** 新画布每被程序化跳转/导航后退一次就把撤销栈整个清空，Ctrl+Z 从此撤不动
      v1: tools/editor/main_window.py:1342 _show_stack_page → :1352 _refresh_scene_page_on_activate → :1369 对场景页调 reload_from_model()。v1 的实现 scene_editor.py:146
      v2: tools/editor/editors/scene_v2/page.py:486 reload_from_model → :493 load_scene(sid) → :179 每次都 self._doc = SceneDocument(...) 重建文档，撤销栈随文档一起换新（旧栈连同历史丢弃）
      impact: 撤销历史在用户完全没意识到的时机被清空，误操作再也退不回去；v1 在同样路径上是保住的，属于实打实的倒退。
      probe_run: True
- [119] **high** 新画布缺 reload_refs_from_model 钩子：别处新建的物品/滤镜/遭遇/BGM/立绘/动画包在它的属性面板下拉里永远看不见，要整个重开编辑器
      v1: tools/editor/editors/scene_editor.py:11687 SceneEditor.reload_refs_from_model → 转发 _props.reload_refs_from_model() 并刷新位面视图/时段视图下拉；面板侧 :4591 先 self._mo
      v2: tools/editor/editors/scene_v2/page.py 全文件无 reload_refs_from_model（探针 probe1 实测 hasattr(SceneEditorV2,'reload_refs_from_model') = False）。主窗口的兜底扫描（main_
      impact: 「要重启编辑器才看得到」这一族老 bug 在新画布上原样复活；用户配不上刚建的引用，只能重开工程。
      probe_run: True
- [120] **medium** 实体重构菜单整块缺失（迁移到场景 / 重命名 id / 安全删除带引用报告 / 转为 NPC / 撤销上次重构），新画布上改 id 或删实体不再有任何引用跟随与预警
      v1: tools/editor/editors/scene_editor.py:11303-11320 工具栏「重构」QToolButton：复制实体（Ctrl+D）、迁移到场景…、重命名 id…、安全删除（引用报告）…、转为 NPC（纯展示热点）…、撤销上次重构；:15667 起走 shared/ent
      v2: grep 全 tools/editor/editors/scene_v2/ 无 entity_refactor / RenameEntityDialog / scan_entity_usages 任何一处；page.py:301 _install_tools 建的工具栏只放 ToolManager 
      impact: 改名/迁移/删除这三件最容易搞坏引用的事，在新画布上从「先扫描预览再机械改写」退化成「裸改，出了事等 Validate Data」。
      probe_run: False
- [121] **medium** 三个视图轴下拉（过场编辑视图 / 位面视图 / 时段视图）在新画布上不存在，set_view_axes 全仓零调用者；activate_plane_view 也没有，位面页 Tab3 那种带位面的跳转在 v2 上无从落地
      v1: tools/editor/editors/scene_editor.py:11407 _combo_cutscene_ctx（过场编辑视图，控制 cutsceneOnly 实体加不加载）、:11419 _combo_plane_view（位面视图）、:11436 _combo_phase_view（
      v2: tools/editor/editors/scene_v2/page.py:326 set_view_axes 存在但全仓 grep 只有它自己的定义和 :77 的默认 ViewAxes()，没有任何 UI 或外部调用者（grep -rn "set_view_axes" tools/ 仅两处，都在 
      impact: 多位面/多时段/带过场专属实体的场景在新画布上没法分层查看，实体互相叠死，点选与摆位都靠猜。
      probe_run: True
- [122] **medium** 新画布左栏是裸列表 + 裸树：没有新建场景、没有场景搜索、实体树没有过滤框 / 按分组视图 / 新增组 / 右键菜单
      v1: tools/editor/editors/scene_editor.py:11463 _scene_search（按 id/名称过滤场景）、:11466 _btn_new_scene「+ 新建场景」、:11485 _tree_mode（按类型 / 按分组）、:11489 _tree_filter「过
      v2: tools/editor/editors/scene_v2/page.py:88-101 左栏只有一个 QListWidget（场景）+ 一个 QTreeWidget（实体），树在 :380 refresh_entity_tree 里写死四个分类（热点/NPC/区域/出生点），无过滤、无分组视图、无
      impact: 新画布无法独立完成日常工作，用户必须在两个 Scene 页之间来回切；而每次切回新画布页又会撞上撤销栈被清空那条。
      probe_run: False
- [123] **low** 跨页跳转落点方法在 v2 全缺（select_scene_by_id / select_npc_by_id / select_hotspot_by_id / select_zone_by_id），scene_page_registry.NAV_TARGET 一旦切到 "v2" 就变成「切了页但不定位」的静默失败；而且全局搜索的场景级落点绕过了 registry，硬写标签 "Scene"
      v1: tools/editor/editors/scene_editor.py:15983-15992 定义了 select_npc_by_id / select_hotspot_by_id / select_zone_by_id / select_scene_by_id 四个跳转落点；main_wind
      v2: 探针 outside_canvas_probe1.py 实测 SceneEditorV2 这四个方法全部 hasattr = False（activate_plane_view 同）。另外 main_window.py:3197 的场景级搜索落点写的是 self._nav_hit_generic("
      impact: registry 承诺的「改一个常量整体切换」实际上是坏的：切过去之后跨页跳转全线静默失效，且场景级与实体级落点会分裂到两个不同的页。
      probe_run: True


# 已核实的判词（55 条里的）
- REAL [high] 新画布页没有位面/时段/过场三条视图轴的任何 UI 入口，过滤只能靠程序化调 set_view_axes；连带后果是 v1 默认隐藏的 cutsceneOnly 实体在 v2 上一直显示（跨页跳转那半条今天不成立，NAV_TARGET 仍是
      why: 三条证伪路线全部试过且失败。(1) 换名/换位置：全仓 grep `set_view_axes` 只命中 tools/editor/tests/test_scene_v2_overlays.py 与 test_scene_v2_page.py，无任何 UI 调用；grep `set_plane_filter`/`set_phase_filter`/`activate_plane_view` 在 s
      fix: 在 tools/editor/editors/scene_v2/page.py 的右栏工具栏旁补三个 FilterableTypeCombo（候选分别取模型位面表、game_config.dayNight.phases、过场清单，照 scene_editor.py:11695-11790 那三个 _refill_* 的口径），typeCommitted 后组装 ViewAxes 调已有的 set_
- REAL [medium] 共享过场判定 entity_is_cutscene_only 的缺省与 v1/运行时相反（缺 cutsceneOnly 键被判成共享实体），加上 v2 默认不装过滤器，新画布把仅过场实体当普通实体全部画出、可选可拖可改
      why: 五条证伪方向逐个走完都没能推翻。(1) 全仓 grep：scene_v2 目录里除 panel_bridge.py/changes.py 提到字段名外没有任何过场判定，`set_view_axes` 只有测试在调，没有生产调用方，v2 没有等价实现。(2) v1 确实有：scene_editor.py:225-226 缺省判为仅过场，:12568-12572 装载期用它决定「建不建图元」，:117
      fix: 改 tools/editor/shared/scene_view_filters.py:58-60，把 `d.get("cutsceneOnly") is True` 改成与 v1(scene_editor.py:226)/runtime(src/data/types.ts:1517) 同口径的「有绑定且 cutsceneOnly is not False」；同步修正 tools/editor/t
- REAL [medium] 新画布 SceneView 完全不处理鼠标中键：v1（以及 map/quest/坐标点选器）统一的「中键拖动平移画布」手势在 v2 上失效，放大后只能靠滚轮/拖滚动条平移
      why: 四个证伪方向逐条查完，都没能推翻，判定成立。

1) v2 是否换个地方实现了：`grep -rn "MiddleButton" tools/editor/ --include=*.py` 命中 map_editor.py:70/80/95、quest_editor.py:64/110、scene_editor.py:3756/3826/3907、shared/move_entity_map_pi
      fix: 在 `tools/editor/editors/scene_v2/view.py` 的 `mousePressEvent/mouseMoveEvent/mouseReleaseEvent`（542-570 行附近）里，在转发给 ToolManager **之前**加一段中键分支，照抄 `tools/editor/editors/scene_editor.py:3756-3760 / 3825-38
- REAL [low] 新画布缺少「适配」(fit) 按钮：v1 工具栏上一键回到全览的入口在 v2 没有对应物，只能靠 Ctrl+滚轮缩回去（+/− 两个按钮的缺失不构成回归，Ctrl+滚轮缩放在两版一模一样）
      why: 核心事实成立，但严重度与影响被夸大。亲自跑了探针（scratchpad/refute_zoomfit_probe.py，offscreen 构造真实 SceneEditorV2 + SceneEditor）：
1) v2 侧确认无入口：page 的全部 QAction 只有工具名 ['分组','整组位移','新建','移动','编辑多边形','缩放旋转','选择','透视轴']（其中「缩放旋转」是
      fix: 在 tools/editor/editors/scene_v2/page.py 的 `_install_tools()`（301-324，注意 317 行每次 load_scene 都 `self._toolbar.clear()`，动作必须在 clear 之后重加，或另起一条不被 clear 的 zoom 工具栏）末尾加 `addSeparator()` + 三个动作，分别接 `view.zoo
- REAL [high] 新画布每次装载场景都在 view 还没被布局（viewport 98x28）时就 fit 了一次且永不重试，22 个真实场景全部停在正确缩放的 4~5.5%；而 zoom_by 的 0.02 下限守卫连放大都拦住，用户没有任何补救手段
      why: 独立复现成功，且比原报告更严重。(1) v2 全仓 grep 确认没有替代实现：`tools/editor/editors/scene_v2/` 下 `fit_scene|fitInView|resizeEvent|showEvent|singleShot` 只命中 page.py:192 与 view.py:458-462，工具栏（page.py:301-320）只注册工具、无「适配」动作，vi
      fix: 两处都要改：一是 tools/editor/editors/scene_v2/page.py:192 的 fit_scene 必须等布局落定——最小做法是在 SceneView（tools/editor/editors/scene_v2/view.py:458 附近）加一个 `_pending_fit` 标志 + `resizeEvent` 覆写，标志置起时每次 resize 重跑 fit_sce
- REAL [high] 新画布(scene_v2)没接属性面板的 scene_background_changed：点「导入/更换背景图」后 PNG 已落盘、backgrounds 与 worldWidth/Height 已写进模型，但画布背景图元与 sceneR
      why: 五个证伪方向逐个走了一遍，全部没打掉。(1) 全仓 grep：`scene_background_changed` 只在 tools/editor/editors/scene_editor.py 命中(4581 声明 / 6239 emit / 11587 connect / 11588 槽 / 14885 实现)，scene_v2/ 整个包零命中；page.py:203 `_refresh_ba
      fix: 在 tools/editor/editors/scene_v2/page.py 的 `__init__`(面板在那里创建、页级共享，不能放 load_scene 里，否则每次切场景叠加一条重复连接)把 `self._props.scene_background_changed` 接到一个新槽：槽内先判 `self._doc/self._view` 非空，再调 `self._refresh_back
- REAL [low] v1 默认开启的「NPC 比例参考框」（左上/右下两个 worldWidth×worldHeight 矩形 + 'NPC 参考 …wu' 标签）及其开关在新画布里完全没有实现，画布上失去唯一的世界单位实物比例尺
      why: 证伪失败，四个方向逐一排除。(1) 换名字/换位置：全仓 grep `npc_ref|reference|参考|ruler|grid|网格` 在 tools/editor/editors/scene_v2/ 只命中 renderer.py:29 与 tools.py:9 两条无关注释；page.py:40-43 只从 anim_atlas_preview 引了 resolved_anim_worl
      fix: 在 tools/editor/editors/scene_v2/overlays.py 加一个场景级覆盖物（照 PerspectiveAxisItem/组框那条通道，z 取内容之下、底图之上），由 tools/editor/editors/scene_v2/page.py 在 load_scene / refresh_scene_geometry 里调 tools/editor/shared/an
- REAL [high] 新画布把 NPC 的整张精灵图集画进了单个人物 quad（没有切帧、也没有动画驱动），每个 NPC 显示成一片缩微小人网格
      why: 我按五条证伪方向逐个查，全部没能推翻它，并且自己跑了独立探针复现。

1) 「v2 其实有，只是换了地方」——不成立。全仓 grep `crop_atlas_cell|_crop_atlas_cell|source_rect|src_rect|frame_rect`，切帧函数 `crop_atlas_cell` 定义在 `tools/editor/shared/anim_atlas_preview
      fix: 最小修复在贴图供给侧，不用动 view/items 的几何：让 `tools/editor/editors/scene_v2/page.py` 的 `_npc_sprite_metrics` 除 (w,h,url) 外再带上 cols/rows/cellWidth/cellHeight 与初始状态的首帧序号，然后在喂给 `SceneView` 的贴图路径上调用现成的 `tools/editor/s
- REAL [high] v2 画布全线不乘透视系数：3 个配了 perspectiveScale 的场景里 58 个 NPC 精灵被画成最多 2.22 倍大（热点展示图/交互圈/排序键同一处漏，热点侧因缺省不参与暂未触发）
      why: 证伪失败，五个方向逐个查过都不成立。(1) 全仓 grep：scene_v2 目录下 entity_perspective_factor 零引用，persp 只命中透视轴覆盖物（page.py:365-377、tools_overlays.py:27-85），没有换名换位；注入的 _npc_sprite_metrics（page.py:244）返回的是动画包原始世界尺寸，也没预乘系数。(2) v1
      fix: 在 tools/editor/editors/scene_v2/view.py 的 _content_spec（:280-287 npc 分支、:300-307 hotspot 分支）把返回的 scale 乘上 entity_perspective_factor(self._doc.scene().get("perspectiveScale"), ent, kind)（helper 在 tools
- REFUTED [low] 不成立：日常切页（点导航树）v2 视口原样保留；唯一重置视口的 reload_from_model 路径上，v1 因 _refresh_scene_list 未屏蔽信号而触发 fit_all，同样重置——两者行为一致，无回归
      why: 证伪成立，两个独立角度都不通过。

【角度4：复现步骤跑不出来】报告写的是「每次切到场景页都会调 reload_from_model」，这是错的。tools/editor/main_window.py:1352 `_refresh_scene_page_on_activate` 的**唯一**调用点是 1342 行、位于 `_show_stack_page` 内。用户手点左侧导航树走的是另一条路：
      fix: 
- REAL [low] 新画布把所有 zone 都画成同一种黄色，depth_floor 区不再像老画布那样显示为蓝色，画布上无法分辨深度地面与普通触发区（全库 7 个 depth_floor，其中 2 个场景与普通 zone 混放）
      why: Could not refute it; probe reproduces it on real scenes. v1 evidence verified by calling the function directly: scene_editor.py:284-287 _zone_canvas_color returns _ZONE_COLOR_DEPTH_FLOOR #50a0ff,a=72 
      fix: 在 tools/editor/editors/scene_v2/view.py 的 _sync_part 里给 ("zone","polygon") 传按 zoneKind 取的颜色（把 scene_editor.py:284-287 的 _zone_canvas_color 提到 shared/ 供两套画布共用），并给 entity_items.py 的 _PointsItem 补一个 set_
- REAL [medium] v2 画布的把手（hotspot/npc/spawn）既不画实体名标签也不设 tooltip，同族实体在画布上只能靠位置猜；v1 的 _DraggableCircle 一直带屏幕恒定字号的带底标签 + "{kind}: {id}" tool
      why: 三条证据链都亲自核过，结论是这条**成立**，不是误报。

1) v1 确实有，且无开关、恒常显示：tools/editor/editors/scene_editor.py:601-615 `_DraggableCircle.__init__` 无条件建 `QGraphicsTextItem` 子项（`ItemIgnoresTransformations` 保持屏幕恒定字号，`_apply_lab
      fix: 两处最小改动：(1) tools/editor/editors/scene_v2/entity_items.py 的 HandleItem —— 该 item 已经是 ItemIgnoresTransformations（本地坐标即屏幕像素），在 paint() 里圆点旁 drawText 一段文本，并把文本尺寸算进 boundingRect()；给它加一个 set_label(text) 存字段
- REAL [medium] v2 把所有 hotspot 的把手都画成同一个蓝色 #3c8cff，六种 type 在画布上无任何区分，且类型改动后也不重上色（v1 有六色分色 + 改类型即时重上色）
      why: 五个证伪方向全部落空，探针实跑复现。(1)全仓 grep `HOTSPOT_COLORS|hotspot_color|type_color|TYPE_COLORS` 只命中 scene_editor.py(v1)；scene_v2/ 内 `"type"` 字面量仅出现在 tools_structure.py:26（新建热点的默认值），与颜色无关；`_color` 只在 entity_items.p
      fix: 在 tools/editor/editors/scene_v2/entity_items.py 里加一张 HOTSPOT_TYPE_COLORS（六个值直接沿用 scene_editor.py:135-142，免得策划换页重新认色），并给 HandleItem 补一个 set_color(QColor)（改 self._color 后 self.update()）；然后在 tools/editor
- REAL [medium] v2 的交互半径虚线圈只按 authored 数值画，漏乘实例 scale 与透视系数，与运行时判定半径（Hotspot/Npc.effectiveInteractionRange）不一致；真实场景里 38 个 NPC 被画错，最大偏差 2
      why: 尝试了五个证伪方向，全部失败。(1) 全仓 grep：scene_v2/ 里 entity_perspective_factor 一次都没出现，entity_scale_of 只在 sorting.py:56,68 做 z 排序，handle 半径确实只有 view.py:234 一处、只传原始数值，entity_items.py:87 的 _range_px 只乘视图缩放。(2) v1 没读错：
      fix: 在 tools/editor/editors/scene_v2/view.py:234 改成与 scene_editor.py:2718-2719/2929-2930 同式：float(ent.get("interactionRange", 0) or 0) * entity_scale_of(ent) * entity_perspective_factor(self._doc.scene().g
- REAL [low] v2 不画运行时命中面的只读虚线轮廓（v1 的 collision ghost）：透视场景里给 NPC 画碰撞多边形时，画布只显示 authored 多边形，没有任何提示说明运行时生效的是绕锚点 ×pf 后的那一圈
      why: 五个证伪方向逐个查完，四个都没能推翻它，只有「用户是否察觉」这一条把严重度压下来。

(1) 换名字/换位置：不成立。`grep -rn ghost tools/editor/editors/scene_v2/` 零命中；`entity_perspective_factor` / `persp_factor` 在整个 scene_v2/ 目录零引用。我自己跑的探针 refute_ghost_v2p
      fix: 最小修复：在 tools/editor/editors/scene_v2/view.py 的 _PART_ITEM_FACTORY 补 ("hotspot","ghost") / ("npc","ghost") 两项（复用 PolygonItem 或加一个不吃鼠标、DashLine 的只读子类），在 _sync_entity 里按 tools/editor/shared/entity_transf
- REAL [medium] v2 分组框只画虚线矩形和把手，组名/成员数/tooltip 全部不画（`GroupBoxItem._title` 是死字段），画布上无法识别某个框属于哪个组
      why: 亲自跑了探针（scratchpad/refute_grouptitle_probe.py，sh scripts/py.sh -B 执行），三条独立证据都指向"成立"：(1) 用记录型 painter 代理调 `GroupBoxItem.paint()`，调用序列是 ['setRenderHint','setPen','setBrush','drawRect','setBrush','drawEll
      fix: tools/editor/editors/scene_v2/overlays.py:98-111 `GroupBoxItem.paint()` 里补一次 drawText：字号用屏幕恒定口径（`px / self._scale`，与 `handle_center()` 里 `HANDLE_PICK_PX / self._scale` 同源），文字摆在 `self._rect.top()` 之上、把
- REAL [low] v2 的分组框只按成员锚点和 zone 顶点取包围盒，不含展示图/精灵的落地占位，框线从组员贴图身上切过去（城门口一组 6 个 NPC 全部头部露在框外，最多 119 世界单位）——纯视觉错位，不会吃掉点击
      why: 证伪未成立，但影响描述要改。(1) 全仓 grep：group_bounds(tools_overlays.py:96-120) 是 v2 唯一的组框几何来源，page.py:346-363 原样交给 view.sync_group_boxes(view.py:324)，GroupBoxItem.set_geometry(overlays.py:113) 原样存、paint 原样画（boundin
      fix: 改 group_bounds 的取点口径：tools/editor/editors/scene_v2/tools_overlays.py:96-120 现在只收 ent["x"]/["y"] 与 polygon 顶点，应改成对每个成员收「实际画出来的落地矩形」的四角——即 view._content_spec（tools/editor/editors/scene_v2/view.py:262-30
- REAL [high] 新画布的交互半径虚线圈按 authored 原值画，既不乘实例 scale 也不乘透视系数，与 v1/运行时的 effectiveInteractionRange 口径不一致；真实场景里 39 个 NPC 的圈画错，最大偏到 2.2 倍
      why: 证伪失败，问题成立，且比报告描述的更广。

1) v2 侧确认：全仓只有 tools/editor/editors/scene_v2/view.py:234 一处 `item.set_interaction_range(float(ent.get("interactionRange", 0) or 0))`，raw 值直接进图元；entity_items.py:81-95 的 `_range_px
      fix: tools/editor/editors/scene_v2/view.py:234 把 `float(ent.get("interactionRange", 0) or 0)` 换成 `raw * entity_scale_of(ent) * entity_perspective_factor(self._doc.scene().get("perspectiveScale"), ent, ref.
- REAL [medium] 新画布的「缩放旋转」gizmo 不按实体类型设防，出生点（默认与命名）选中后也出手柄，拖一下就往只有 x/y 的 spawnPoint / spawnPoints[name] 里写进 schema 外的 scale/rotation，画面零
      why: 亲自跑了页面级探针（真 SceneEditorV2 + 真 ProjectModel + 真工具栏 action + ToolManager 事件），全链路复现：画布点出生点圆点 → selection=EntityRef('spawn','default')；点工具栏「缩放旋转」→ view.transform_gizmo.isVisible()=True；经 view.tools 拖方手柄 →
      fix: 在 tools/editor/editors/scene_v2/tools_transform.py 的 TransformTool 里加一个统一的 kind 白名单（等价于 v1 scene_editor.py:14094-14096 的 refs[0][0] not in ('hotspot','npc')）：给 _anchor(ref)（58-66 行）开头补一句 `if ref.kind 
- REFUTED [low] 新画布拖缩放手柄会把用户手填的负角 rotation 单方面归一化（-30 → 330，同一个角、仅显示与 diff 观感变化）；同报告里的"精度被截断"方向反了（v1 只留 2 位小数、v2 留 4 位，v2 更好），"int scale
      why: 亲自跑了探针（scratchpad/refute_gizmo_transform_repr.py + refute_gizmo_scan_scenes.py，两边同一手势对拍），三条харm 里两条是错的，剩下一条够不上 medium。

前提先确认对：v1 的画布 gizmo 提交路径确实走报告引的那个函数（scene_editor.py:14156-14164 `_on_gizmo_trans
      fix: 若要顺手收掉残余：tools_transform.py:155-159 提交前对"本次手势没碰过的那个键"沿用同文件 262 行 `_shift` 那套"值等价则原值返回、保留原始表示"的写法——旋转手势里 scale 直接回填 `ent.get("scale")` 原值、缩放手势里 rotation 回填原值，别再 round/取模；只对真正被拖的那个键做 round。归一化本身也可以只在跨 ±
- REAL [medium] v2 把缩放/旋转做成了独占工具模式：单选实体不再自动出手柄（只有 TransformTool 有 gizmo_positions），而该模式下画布点击既不选也不移（mouse_pressed 命中不到手柄就 return False，无回
      why: 五条证伪路径全部失败，实测复现与报告逐条一致。

A) 「v2 其实有、只是换了地方」——不成立。全仓 grep `gizmo_positions` 只有 tools_transform.py:88 一处定义、view.py:530 一处取用；探针枚举全部 10 个已注册工具，只有 transform 有该属性（select/move/polygon/group_move/create×3/per
      fix: 两处最小修：(1) tools/editor/editors/scene_v2/page.py:317-322 `_install_tools` 里把这批 action 放进一个 exclusive QActionGroup，并接 `view.tools.tool_changed` → 对应 action.setChecked(True)（初始 select 时也要同步一次），顺带把三个 Crea
- REAL [medium] v2 缩放旋转工具在手柄上「按下即松手」仍会提交一条命令：当实体 rotation 落在 [0,360) 之外（如 -30，属性面板 spinbox 明确允许 -360~360）时，会被静默改写成 330.0，场景标脏并多一条撤销记录；v1
      why: 机制成立，但报告者的证据与影响估计有相当一部分是错的，严重度应从 high 降到 medium。

【成立的部分】
- v1 侧读得对：scene_editor.py:809-813 确有 `if self.scale_v == self._press_scale and self.rot_deg == self._press_rot: event.accept(); return`，零变化不发 
      fix: 在 tools/editor/editors/scene_v2/tools_transform.py 的 mouse_released（147-162）补 v1 那道源头闸：press 时已存了 _start_scale/_start_rot，release 时若 self._preview == (self._start_scale, self._start_rot)（即整个手势一次 mouse
- REAL [high] 新画布选中出生点时属性面板不切页：出生点只能拖、无法改名/精确改坐标/删除，且面板仍停在上一个实体的表单上，在那上面打的字既不入命令也不写模型、关窗切页无任何提示地丢失
      why: 亲自跑探针 C:/Users/weiruanrinima/AppData/Local/Temp/claude/E--GameDev-GameDraft/25473be9-c526-4118-98a6-2fbaeb17ced1/scratchpad/refute_spawn_panel.py，同一份数据同时构造 v1 SceneEditor 与 v2 SceneEditorV2，四条证伪路线全部失败
      fix: 最小修复在 tools/editor/editors/scene_v2/panel_bridge.py：(a) sync_from_selection 在查不到 binding（含 spawn、多选、无选中）时不能只把 _loaded 置 None 就返回，必须把面板显式切走——多选调 panel.show_multi_selection(n)，其余回落 panel.load_scene_prop
- REFUTED [low] v2 对分组几乎没有任何管理面（树里不列组、组框从不画标题、不读 editor.anchor、没有新建/指派/删除组的入口）；「空组不可见」只是这一大片空白里的一个角落，不是独立的功能回归
      why: 机械事实我复现了，但报告的定性站不住。

【v1 侧属实】探针 refute_empty_group_v1.py 实跑 SceneEditor + 空组 {"id":"newgrp"}：_empty=True、anchor=(400,711)、shape() 非空且包含把手、title_item 文本 '[组] newgrp（just-created） ×0' 可见、paint 产出 423 字节
      fix: 真要收，别单修空组：先在 tools/editor/editors/scene_v2/overlays.py 的 GroupBoxItem.paint 里把已经存着的 _title 真正画出来（照 v1 用 ItemIgnoresTransformations 的子 QGraphicsTextItem），再在 page.py 的实体树里加一个「分组」顶层分类并把 GroupBoxTool.sele
- REAL [high] v2 多选（≥2 实体）时右侧属性面板不切页：仍停在最后一个单实体表单上，此时面板上的任何编辑既不入模型也不入 undo，且切回单选时控件被刷回旧值、编辑不可恢复；v1 的多选页（已选 N 个 + 指派分组/复制/删除三个批量按钮）在 v2
      why: 五个证伪方向逐个查完，全部没能推翻。(1) 换名字/换位置：scene_v2/ 全目录 grep `show_multi_selection`、`_multi_panel`、`assign_group`、`指派分组` 均零命中；v2 确实复用了同一个 ScenePropertyPanel（page.py:120-122），所以 _multi_panel 对象是存在的，但没有任何代码切到它，且探针实
      fix: 最小修复在 tools/editor/editors/scene_v2/panel_bridge.py 的 sync_from_selection（199-202 行）：len(sel) > 1 时不要只把 _loaded 置 None 就返回，改为调用面板既有的 self._panel.show_multi_selection(len(sel))（该方法已存在于 tools/editor/edi
- REAL [medium] 新画布缩放/旋转手势中只有展示图/精灵实时跟转，同一实体的碰撞面多边形和属性面板的 scale/rotation 数值框要到松手才更新
      why: 证伪失败——两个探针都实测复现。v1 侧确认成立：scene_editor.py:800 gizmo 每次 mouseMoveEvent 都 emit transform_gizmo_live，:14144-14154 每帧跑 _write_transform_fields_live(:8556) → sync_transform_widgets(:8649-8668，blockSignals+s
      fix: 在 tools/editor/editors/scene_v2/view.py:489 的 set_transform_preview 里，除 display/sprite 外一并用预览值重算 collision 图元：拿实体 dict 做浅拷贝把 scale/rotation 覆盖成预览值，喂给已有的 _collision_world_points(view.py:381)，再 item.set
- REAL [high] 新画布里场景级属性（worldWidth/worldHeight/滤镜/镜头/光照/bgm/出口/昼夜）既回不去也改不动：点画布空白清空选择后属性面板仍停在上一个实体的表单（v1 会回场景面板），而即便场景面板可见，改动也全部落在面板 st
      why: 三个探针（scratchpad/refute_sceneprops_v1.py、refute_sceneprops_v2.py、refute_sceneprops_edit.py）都跑通，五条证伪路线全部失败。(1) v1 真的有：scene_editor.py:3977 空白处 release 发 item_deselected → :14278-14292 _on_item_deselecte
      fix: 在 tools/editor/editors/scene_v2/panel_bridge.py 给 _PANEL_BINDING 加 "scene": ("_staging_scene", "load_scene_props") 一条，并让 sync_from_selection 在 len(sel) != 1 时回落到 EntityRef("scene", doc.scene_id)（docum
- REAL [high] 
      why: 亲自跑探针复现，五个证伪方向全部走不通。

1) 「v2 换了名字/位置」——不成立。grep -rn "load_group_props" tools/editor/editors/scene_v2/ 零命中；scene_v2/ 里没有任何 contextMenu/customContextMenu；工具栏动作实测只有 ['选择','移动','编辑多边形','缩放旋转','排序位置','新建',
      fix: 
- REAL [low] v2 工具栏按钮的勾选态与真实当前工具完全脱钩：初始「选择」活着但无按钮按下，切工具后旧按钮不弹起（可累积多个同时按下），重复点当前工具反而把它自己弹起来
      why: 证伪四个方向全部失败。(1) 全仓 grep：`QActionGroup|setExclusive` 在 tools/editor 下只命中 tools.py:207 的一句注释（说"动作组只负责 UI 的按钮状态"——而这个动作组根本没被创建）；scene_v2/ 里 `setChecked` 零命中；`tool_changed` 唯一消费者是 view.py:122 的 refresh_ges
      fix: tools/editor/editors/scene_v2/page.py:317-322 `_install_tools`：建 `QActionGroup(self)` 并 `setExclusive(True)`，把每个 act `addAction` 进去（或自留 tool→QAction 映射）；再把 `view.tools.tool_changed` 连到一个回刷槽，按 current 
- REAL [medium] v2 树↔画布只同步选中态、不做视口/滚动定位：树里点一行画布不 centerOn（v1 scene_editor.py:12896 有），画布上选中也不把树滚到那一行（v1 scene_editor.py:12852 scrollToIt
      why: 未能证伪，四个方向逐个查过都不成立。(1) 全仓 grep：scene_v2/ 下 centerOn/ensureVisible/scrollToItem 只有 page.py:460 一处，且在只供跨页跳转的 select_entity() 里；view.py:165 的 SelectionChanged 只走 _apply_selection（仅切图元选中态），panel_bridge.py:
      fix: 在 tools/editor/editors/scene_v2/page.py 两处补：(1) `_on_tree_selection_changed`（416-429）在 `set_selection` 后，单选时抽出 `select_entity`（450-462）里那三行 `item = self._view.item_for(ref, "handle"); self._view.cente
- REAL [high] v2 画布的实体预览不乘透视系数：NPC 精灵（及显式开启透视的热点贴图）按原始世界尺寸画，在 3 个配了 perspectiveScale 的真实场景里与运行时最多差 2.22 倍，且没有任何提示
      why: 尽力证伪失败，五个方向全部走不通，这条成立。

1) 「v2 换了名字/换了位置」→ 不成立。`grep -rn "persp" tools/editor/editors/scene_v2/` 全包只有 6 处、全部是透视轴 overlay 本身（view.py:110-112/313/342 的 PerspectiveAxisItem、page.py:365-377 读 `perspective
      fix: 在 tools/editor/editors/scene_v2/view.py:262-308 `_content_spec` 里把返回的 scale 改成 `entity_scale_of(ent) * entity_perspective_factor(cfg, ent, kind)`（复用 tools/editor/shared/entity_transform_math.py 的现成函数，
- REAL [high] 「移动」工具的 mouse_pressed 完全不做命中判定：该模式下按在任何位置（空白或别的实体）都直接拖走当前选择，既改不了选也取消不掉选择，空白处误拖会静默改坐标并标脏
      why: 亲自跑探针（经 SceneView.mousePressEvent/MoveEvent/ReleaseEvent 真事件派发，脚本在 scratchpad/refute_movetool_hittest.py），四步全部复现：选择工具点中 h1 后切「移动」工具，(B) 在 n1 身上按下拖 40px → n1 纹丝不动、h1 从 (100,100) 变成 (140,100)，undo+1、场景标
      fix: 改 tools/editor/editors/scene_v2/tools_builtin.py:196-221 的 MoveTool.mouse_pressed：先用基类已有的白名单命中 self.hits_at(scene_pos, self._view.entity_items())（与 SelectTool._hits 同一条，tools_builtin.py:57-62 / tools.
- REAL [high] 新画布里整个「场景级」属性面板（透视缩放只是其中一段，还包括场景名/世界宽高/行走速度等）的编辑一律不进模型：PanelBridge 只绑了 hotspot/npc/zone，scene 页没有任何提交路径，且 flush_to_model
      why: 五个证伪方向逐条试过，全部没能推翻。(1) 全仓 grep：scene_v2 里唯一的 perspectiveScale 写入是 tools_overlays.py:80-86 PerspectiveAxisTool.mouse_released，只提交画布拖出来的 near/far 端点 x/y；panel_bridge.py:130-135 的 _PANEL_BINDING 确实只有 hots
      fix: 在 tools/editor/editors/scene_v2/panel_bridge.py 给 _PANEL_BINDING 补一条 scene 分支（staging 属性 `_staging_scene` / 载入方法 `load_scene_props`，提交前调 `save_scene_props()` 或 `flush_active_panel_widgets_to_staging(o
- REAL [high] 新画布对场景透视配置是"只能改、不能建也不能删"：没配过 perspectiveScale 的场景无法启用透视，已配的也取消不掉——面板勾选框只写进 staging，画布透视轴工具在无配置时拒绝起手
      why: 亲自跑探针复现，报告成立且比报告说的还多一条。探针（scratchpad/refute_persp_entry.py，v2 SceneEditorV2 + 无 perspectiveScale 的场景）实测：勾选面板「启用透视缩放」后 panel._sc_persp_axis 确实 seed 出 {'near':{'x':400.0,'y':552.0},'far':{'x':400.0,'y':
      fix: 两条路二选一（推荐同时）：(a) 根治——在 tools/editor/editors/scene_v2/panel_bridge.py:130 的 _PANEL_BINDING 里补一条 "scene": ("_staging_scene", "load_scene_props")，并在 sync_from_selection / commit_panel_edits 里为 EntityRef(
- REFUTED [low] 画布加选修饰键由 Ctrl(v1) 改为 Shift(v2)，功能未丢失、且 v2 常驻状态栏写明「Shift 加选」，仅是并存期两页快捷键不一致
      why: 机械事实成立但结论不成立。我用真实 widget 事件跑了 v1（SceneCanvas.viewport() 收 QMouseEvent）：点 h1 →('hotspot:h1',)，Ctrl+点 n1 →('hotspot:h1','npc:n1')，Shift+点 n1 →('npc:n1',)，Ctrl 再点 n1 →('hotspot:h1',)；v2 直连 SelectTool：Ctr
      fix: 若要消除并存期不一致：在 tools/editor/editors/scene_v2/tools_builtin.py 的 SelectTool.mouse_pressed(:82,:97) 与 mouse_released(:129) 把判定从 `modifiers & ShiftModifier` 放宽为 `modifiers & (ShiftModifier | ControlModifie
- REAL [medium] v2 分组工具的组选中态没有任何熄灭路径：点空白、点中别的实体、按 Esc、切走再切回都清不掉，之后方向键一律微移整组（连刚点中的那个实体都动不了）；v1 在这两种情况下都会 set_selected_group(None)
      why: 证伪失败，这条成立，且比原报告描述的更广。我自己跑了两个探针（scratchpad/refute_group_sticky_probe.py、refute_group_sticky_probe2.py，走真实 SceneEditorV2 + ToolManager 派发 + view.keyPressEvent）：

1) 原复现成立：分组工具点组框边线选中 g1 → 点空白 (760,30)（G
      fix: 改 tools/editor/editors/scene_v2/tools_overlays.py 的 GroupBoxTool 两处：(1) mouse_pressed 里 _box_at 落空时先 self.select_group("") 再 return False（保持事件继续下沉到基类选择路径），对齐 v1 scene_editor.py:14293；(2) 订阅 document.c
- REAL [low] v2 透视轴工具的 cancel_gesture 不把 overlay 复位：Esc 取消端点拖动后，画布上的深度轴一直停在拖动位置（数据是对的），直到下一次任意文档变更才回位
      why: 证伪失败，这条成立。(1) 全仓确认 v2 没有别的复位通道：`refresh_perspective_axis` 只在 page.py:196 装载 与 page.py:437 `_on_doc_changed` 里被调，`sync_perspective_axis`(view.py:342) 无其它调用点；最像的候选 `view.keyPressEvent`(view.py:580) 之后的 
      fix: tools/editor/editors/scene_v2/tools_overlays.py:88-93 `PerspectiveAxisTool.cancel_gesture()` 里，在清 `_which`/`_start` 之前用 `self._start` 里那份 cfg 把 overlay 调回去：`self._item.set_axis(QPointF(start['near']['
- REAL [low] v2 透视深度轴 overlay 只画一根线加两个同样大小的实心圆：没有指向远端的箭头、没有 near/far 的 ×缩放文字标签、没有垂直于轴的等缩放虚线（near/far/midStop 各一条），端点近大远小的区分也没了——而 v2 
      why: 证伪失败，这条成立。(1) 全仓 grep：scene_v2/ 与 shared/ 下 near_scale|far_scale|mid_stops|midStops|近 × 零命中；v2 全目录 drawText 只有 content_items.py:70 的备注文本，无 QGraphicsTextItem/addText——功能没有换名字或搬去别处。(2) v1 侧读得没错：scene_ed
      fix: 在 tools/editor/editors/scene_v2/overlays.py 的 PerspectiveAxisItem 上补 near_scale/far_scale/mid_stops 三个字段（由 page.py:365-378 从 perspectiveScale 取出、经 view.py:342 sync_perspective_axis 传下），paint() 照搬 scen
- REAL [low] v2 未实现 `ghost` part：在 3 个配了 perspectiveScale 的场景里，给参与透视的实体画碰撞面时，v1 会额外画一圈只读虚线示意运行时真实命中范围，v2 什么都不画（当前项目数据里 0 个实体命中该条件，需策划
      why: 结论：成立，但报告的复现步骤与影响方向都是错的，严重度应从 medium 降到 low。

【成立部分 — 亲自跑了 3 个探针验证】
1) v2 确实没有。`tools/editor/editors/scene_v2/view.py:53` 的 `_PART_ITEM_FACTORY` 只有 handle/display/collision/patrol/sprite/polygon 六种，注释
      fix: `tools/editor/editors/scene_v2/view.py:53` 的 `_PART_ITEM_FACTORY` 补 `("hotspot","ghost")` / `("npc","ghost")` → 一个只读虚线 PolygonItem 变体（`items.py`，`setAcceptedMouseButtons(NoButton)`，Z 与 collision 同层）；`
- REAL [high] 新画布 scene_v2 全程没有施加场景透视系数（perspectiveScale）：实体贴图/精灵按未缩放尺寸绘制（3 个真实场景 58 个 NPC 现已失真，如 storyteller_zhang 应 289.1 高、实际画 180）
      why: 证伪失败——五个方向逐个查过，全部不成立，我自己跑的探针复现出与报告一字不差的数字。

(1) 换名字/换位置：全仓 grep `entity_perspective_factor` / `perspective_scale_at`，只命中 `tools/editor/editors/scene_editor.py`(v1) 与 `tools/editor/tests/test_perspecti
      fix: 把 v1 已有的 `tools/editor/shared/entity_transform_math.py::entity_perspective_factor` 接进 scene_v2，一次接三处，别只补排序：(1) `scene_v2/view.py::_content_spec`(:262-300) 返回的 scale 改成 `实例 scale × pf`，NPC/热点两支都要——cfg 
- REAL [high] 新画布里分组是纯只读的：没有新建/删除分组、没有指派/移出成员的入口，选中组框也不弹分组属性页（label / conditions 都改不了）
      why: 证伪失败，这条成立，而且比原描述更严重。五个方向逐个查过：

【方向1：v2 换了名字/位置？】全仓 grep 无果。tools/editor/editors/scene_v2/ 里 entityGroups 只出现一次，是 page.py:353 的**读**（refresh_group_boxes 建包围盒）。tools_structure.py 的 LIST_KEY 只有 hotspot/n
      fix: 最小修复集中在 tools/editor/editors/scene_v2/：(1) 新增一个 commands_group.py，写 AddGroupCommand / RemoveGroupCommand（删除时同步清成员 group 键，并保留 v1 scene_editor.py:15868 的入站引用阻断，用 _scene_group_inbound_references 同一口径）与 
- REAL [medium] v2 的分组包围盒退化成零尺寸（单成员点实体）或 None（零成员）时，组框既不绘制也不可命中，该组在画布上再也选不中、拖不动——把两人组的成员改剩一个即可复现，v1 对这两种情况都画得出、点得中
      why: 四条证伪方向逐个跑过，都没能把它推翻。(1) v2 没有别处实现：全仓 grep，`select_group` 的唯一调用点是 `tools_overlays.py:175` 的组框命中，`overlays.py:99/114/122` 三个方法一律 `isNull -> 不画/不命中`，组选中态没有第二入口。(2) v1 确实有：跑真 `SceneEditor` 探针，单成员组 rect=QRe
      fix: 两处一起改：`tools/editor/editors/scene_v2/tools_overlays.py:96-118` 的 `group_bounds` 给退化结果一个最小世界尺寸（照 v1 `scene_editor.py:13088-13089` 的 `r = max(handle_radius, 8.0)` 兜底，把零尺寸包围盒撑成 2r 方框）；`tools/editor/edito
- REAL [medium] v2 分组框完全没有名字标识：GroupBoxItem 收下 title 后从不绘制，画布上只有匿名虚线框，也没有 tooltip，v1 那个「点标题选中组」的点击靶随之消失
      why: 四条证伪路线全部走不通，亲自跑了探针。

(1) 换名字/换位置？全仓 grep：`_title` 在 scene_v2 里只出现在 overlays.py:40（初始化）、55-58（set_geometry 存下来）、view.py:325-335（透传），此后再无引用。探针用 inspect.getsource(GroupBoxItem.paint) 确认 paint 源码里既无 drawT
      fix: 在 tools/editor/editors/scene_v2/overlays.py 的 GroupBoxItem 里把已存下的 `self._title` 画出来：paint 中在框左上角外侧以屏幕恒定字号绘制（painter.save() 后按 self._scale 换算，或照 v1 挂一个带 ItemIgnoresTransformations 的子 QGraphicsTextItem）
- REAL [high] 新画布能画出并选中分组框，却没有任何通往分组属性面板的路：组 id / label / 整组显影 conditions / 成员列表 / 数值精确位移与三个组级按钮全部看不到也改不了（conditions 是玩法数据，且 v2 里能看见的组
      why: 五个证伪方向逐个跑完，全部失败，原报告成立且我的探针独立复现了它。(1)「v2 换地方有」——不成立：scene_v2 全目录 grep load_group_props / _group_panel = 0 命中（探针内 subprocess 实跑）；panel_bridge.py:130-134 _PANEL_BINDING 只有 hotspot/npc/zone，:200-208 其它 ki
      fix: 两处都要补，缺一条编辑仍会静默丢失。(1) 载入侧：tools_overlays.py:156-159 GroupBoxTool.select_group 加一个 gid 信号（或由 page.py 直接在选中回调里）→ page.py 调 self._props.load_group_props(self._doc.scene(), gid)，并把面板的 group_translate_requ
- REFUTED [low] v2 分组把手是纯派生位置、忽略 editor.anchor（数据不丢，且当前 0 个场景用了它）；真正的缺口是 v2 选中分组后完全没有编辑面（无属性面板、无右键菜单）
      why: 代码读得没错，探针也复现了（scratchpad/refute_group_anchor.py 跑真实 test_room_b.json：注入 editor.anchor={700,50} 后 v2 handle_center 仍是 (868.5,211.4)，Alt+拖把手把 NPC 从 916.4,308.8 挪到 956.4,333.8）。但影响不成立：(1) 全仓 grep '"ancho
      fix: 若要收：真正该补的是 tools/editor/editors/scene_v2/page.py + panel_bridge.py 的分组编辑面（选中组时装载 ScenePropertyPanel 的 group 页，接上 group_label/delete/select_members），anchor 只是其中一个字段；顺带把 tools_overlays.py:123 那句「拖把手挪锚点」
- REAL [medium] 「分组」工具的组选中态无法清除（点空白、按 Esc、切工具都不清，只有选另一个组才换）；组选中期间方向键恒作用于整组，而 Delete 却作用于实体选择，同一工具里两套「当前选中」打架
      why: 机制成立，我自己跑探针复现了；但报告的复现步骤有一步是错的。

【成立的部分】tools_overlays.py:156-159 的 select_group 全仓只有两个调用点：tools_overlays.py:171-174（命中组框时）和测试。page.py:430-433 处理 SelectionChanged 时完全不碰分组工具。GroupBoxTool 既不覆写 activate 也
      fix: 两处任选其一或都做：(1) tools/editor/editors/scene_v2/page.py 的 _on_doc_changed 在收到 SelectionChanged 且 refs 非空时调 self.group_box_tool.select_group("")，对齐 v1 scene_editor.py:12824-12827 的语义；(2) tools/editor/edito
- REAL [low] 工具栏的「整组位移」是个半接线的冗余工具：选中它后鼠标完全失效（拖组框、点实体都没反应），方向键则退化成移动单个已选实体、把组拆散——而它宣称的功能其实由旁边的「分组」工具完整实现
      why: 证伪失败，问题成立，但原描述有两处错。亲自跑了两个探针（scratchpad/refute_groupmove_deadbutton.py、refute_groupmove_arrowcase.py，走真实 QMouseEvent/QKeyEvent 打进 SceneEditorV2 的真实工具栏动作）。

成立的部分：点工具栏「整组位移」后 view.tools.current.tool_id 
      fix: 最小修复：在 tools/editor/editors/scene_v2/page.py:308 停止把 GroupMoveTool 注册进 view.tools（工具栏由 _install_tools 遍历 view.tools.tools 生成，注册即上工具栏），因为 tools_overlays.py:123 的 GroupBoxTool（display_name「分组」）已经实现同一手势—
- REAL [medium] v2「移动」工具按下时不做任何命中判定：只要有选中集，按在空白处或按在另一个实体上拖动，被拖走的都是当前选中的实体（v1 由 ItemIsMovable 保证只有被按住的图元会动）
      why: 证伪失败，这条成立，但描述里有两处夸大需要修正。

逐条查完五个证伪方向：
1) 全仓 grep：`MoveTool` 只有 tools_builtin.py:167 一处实现，页面在 page.py:305 注册、page.py:317-322 给每个已注册工具加一个可勾选工具栏按钮（"移动"），默认 `select`（page.py:323）。命中判定没有换地方——`SelectTool` 用
      fix: 在 tools/editor/editors/scene_v2/tools_builtin.py:196 的 `MoveTool.mouse_pressed` 开头补一道与 SelectTool 同源的命中闸：用 `self.hits_at(scene_pos, self._view.entity_items())`（或 `entities_at`）取落点候选，只有当候选里存在 `it.ref i
- REAL [medium] 分组框包围盒只取成员脚点、不含精灵/展示图尺寸也不留白：框边线切过靠上的几名成员身体、最上方那个整体落在框外；成员坐标重合（含单成员组）时更退化成 0×0 空框，既不绘制也点不中（v1 用精灵真实尺寸 + 外扩 pad）
      why: 两侧都亲自跑了探针（v1: SceneEditor + _group_geometry；v2: SceneEditorV2 + group_bounds，同一场景 雾津街头）。结论：核心成立，但报告的现象描述与影响都夸大了。\n\n成立部分：v1 scene_editor.py:13050-13090 确实用精灵/展示图的真实世界尺寸做底中锚矩形，13106-13112 再按屏幕像素外扩 pad；
      fix: 包围盒要在拿得到渲染尺寸与 view_scale 的那一层算：把 tools_overlays.py:96 的 group_bounds 换成由 page.refresh_group_boxes（tools/editor/editors/scene_v2/page.py:346-363）用 view._content_spec 的同一份尺寸组底中锚矩形（无内容的点实体退到把手半径方块），再按屏幕像
- REAL [low] v2 整组位移无条件平移 patrol.route，不读 entityGroups[].editor.movePatrol；但该开关在 v2 里根本没有入口（v2 从不调 load_group_props），所以只有「先在旧画布关掉开关、再
      why: 证伪五个方向逐条查完，四条都没能推翻它，只有「用户能否察觉」这一条把严重度压下来了。

1. v2 里没有换名/换位置的实现：全仓 `grep -rn movePatrol` 只有 6 处命中，全在 `src/data/types.ts:629`、`tools/editor/editors/scene_editor.py`（v1）与 `tools/editor/tests/test_scene_g
      fix: 在 tools/editor/editors/scene_v2/tools_transform.py 的 translate_group 里读一次组的 editor.movePatrol（与 v1 同口径：`is not False` 缺省为真），为假时把成员的 patrol 键从 vals 里剔除后再交给 translate_entities（translate_entities 保持通用、不感
- REAL [low] 新画布没有「显示场景分组框」开关：分组虚线框与把手在 z=900000 常驻背景之上，任何工具下都关不掉（老画布 scene_editor.py:11389 的复选框既隐藏框也停掉其命中）
      why: 五条证伪路线全部走不通，实测成立。(1) 换名/换位置：全仓 grep 与运行时双查都为空——scene_v2/ 下 grep group_boxes_visible = 0 命中；探针里对 SceneEditorV2 做 findChildren(QAbstractButton/QCheckBox)，页内 31 个复选框全部来自共享的 ScenePropertyPanel，无一与分组框显隐有关；
      fix: 在 tools/editor/editors/scene_v2/view.py 加 set_group_boxes_visible(bool)：存标志位、对 _group_boxes 全体 setVisible，并在 sync_group_boxes 里对新建的 GroupBoxItem 重新套用（否则新建组会无视当前开关）。再在 tools/editor/editors/scene_v2/pag
- REAL [low] 组框和它的把手在所有工具下都画，却只有「分组」工具能点——默认「选择」工具下点边线/把手会静默落到底下的实体上（v1 是随时可点，且专门只在能点时才画把手）
      why: 核心事实用探针复现了，不能证伪；但报告的一半框错了，严重度要降。

已确认成立的部分：v2 组框图元 overlays.py:99-108 `GroupBoxItem.paint` **无条件**画框+把手（每个工具下都画），而命中只有 `GroupBoxTool`（tools_overlays.py:170-180）会做。探针 refute_groupbox_selecttool.py 吃真实场
      fix: 两条都最小：一是 tools/editor/editors/scene_v2/overlays.py 的 `GroupBoxItem.paint`，把手改成只在 `self._selected` 时画（照抄 v1 scene_editor.py:2216 的门控），消掉假承诺；二是想真恢复 v1 手感，就在 tools/editor/editors/scene_v2/tools_builtin.p
- REAL [low] v2 的分组框只从 scene.entityGroups 建，成员身上带 group 标签、场景没有 entityGroups 条目的兼容分组在新画布上完全不显示；同一份数据在老画布上有框（已实测），且该形态被 validator 与运行时
      why: 跑了两个探针分别驱动两套画布，结论成立但报告者的定性夸大了。事实层面（已实测，非只读代码）：同一份「hotspots[].group='yexun'、无 entityGroups 键」的场景，v1 SceneEditor._load_scene 后 canvas._group_boxes == ['yexun'] 且 _chk_group_boxes 默认勾选；v2 SceneEditorV2.l
      fix: tools/editor/editors/scene_v2/page.py:346-363 的 refresh_group_boxes 换成与成员名册同源的口径：把 gid 集合取成 entityGroups ∪ 成员 group 标签（等价于 ProjectModel.scene_group_ids_for_scene，或照 tools_transform.py:191-204 group_me
- REAL [medium] v2 画布没有中键拖拽平移：SceneView 的 mousePress/Move/Release 只把事件转给当前工具，而所有工具遇到非左键一律返回 False，中键按住拖动毫无反应（DRAG-06 的 Ctrl+滚轮缩放与适配两项其实做
      why: 尽力证伪但没能推翻。(1) 全仓 grep MiddleButton|ScrollHandDrag|_middle_pan|ClosedHandCursor 在 tools/editor/ 下只命中 map_editor.py、quest_editor.py、scene_editor.py(v1)、move_entity_map_picker.py，scene_v2/ 下零命中；scene_v2/
      fix: 在 tools/editor/editors/scene_v2/view.py 的 mousePressEvent / mouseMoveEvent / mouseReleaseEvent 里，于转发给 self.tools 之前加中键分支（照抄 scene_editor.py:3756-3760 / 3825-3838 / 3907-3911 或 map_editor.py:70-97 的口径）
- REAL [low] v2 工具栏按钮的按下态是错的：初始无人勾选、点过的按钮全部保持按下、再点当前工具还会把勾选甩到别的按钮上（状态栏文字仍然正确）
      why: 亲自跑探针复现成功。page.py:317-322 每个工具 action 都 setCheckable(True)，但既没有 QActionGroup（探针里 10 个 action 的 actionGroup() 全是 None），也没有把 tools.tool_changed 回写成 setChecked（scene_v2/ 全目录 grep setChecked 零命中，QActionGr
      fix: tools/editor/editors/scene_v2/page.py:317-322：建一个 QActionGroup(self) 且 setExclusive(True) 把十个 action 收进去，同时留一份 tool→action 映射，把 view.tools.tool_changed 连到对应 action.setChecked(True)，这样代码侧选工具（:322 的初始 s