# engine2d / RHI 迁移审查(对齐 master)· 2026-09-25

分支 `claude/festive-cray-g72t2o` 对 master `5f5a639`(= 合并基)。运行时从 Pixi 8.17(WebGL)整体换到
`src/engine2d`(Pixi 同名 API)→ `src/rendering/rhi`(luma.gl,只有 WebGPU)。审查基准:**行为与 master 一致**
(画面、输入、时序、资源生命期、报错可见性);已登记的差异只有 engine2d 卡「与 Pixi 的已知差异」那几条。

## 改动面(src,不含测试)

| 块 | 规模 | 性质 |
|---|---|---|
| `src/engine2d/**` | 161 文件 / +25.8k | 新写:Pixi 同名 API 的 2D 层(场景树、合批、滤镜、遮罩、RT、图形、文字、事件、Ticker、Assets、层级/组件) |
| `src/rendering/rhi/**` | 9 文件 / +3.8k | 新写:RHI 接口、luma WebGPU 后端、空后端、渲染图、MSAA |
| 着色器移植(GLSL → WGSL) | ~20 文件 | SceneLightingPass、CharacterLitSprite、EntityShadow、CharacterShadingFilter、vfx*、BackgroundDebugFilter、EntityLightingFilter、backgroundSway、shadowPrefix、DepthOcclusionFilter、LitBackground、Burn*、Water*、overlayBlend、GiBounce、breathingOverlay、contactAo |
| 行为改动 | Game.ts(+51/−139)、Renderer.ts、Npc/Hotspot/Player、InteractionSystem、VfxRenderer、types.ts、F2 调试 | 预热改管线预建、GL 诊断改 RHI 诊断、NPC/热点在场改 setActive、实体身份组件、层级页 |
| import 换源 | ~80 文件 | `from 'pixi.js'` → engine2d,应为纯机械替换 |
| 编辑器 | anim_preview、parallax_editor | 迁到 engine2d(制作人批准) |

## 需要回归的项目(自动对照)

1. `npx tsc --noEmit` 零错;`npx vitest run` 只剩基线(build_helpers 拒绝盘符根,master 同样失败)。
2. `tools/engine2d_parity`:engine2d 对 Pixi WebGL 核心用例逐位一致(29)。
3. `tools/render_parity/run.mjs`:master 的 src(Pixi WebGL)对本分支(engine2d)着色器用例逐像素(176)。
4. `tools/render_parity/game_sweep.mjs`:master 对本分支逐场景截图 + 新增报错(全部场景)。
5. `tools/rhi_smoke`:RHI 真机冒烟(含上屏、MSAA)。
6. 编辑器:`tsc -p tools/anim_preview`、`tsc -p tools/parallax_editor`、`npm run test:anim-preview`、真机加载。
7. validate-data / 素材审计 / 编辑器 pytest(数据格式零改动的旁证)。

## 需要仔细检查的项目(人工 / 子代理审查)

**A. 引擎核心语义(对 Pixi 8.17 源码逐条比)**
- 变换:局部 / 世界矩阵、pivot / anchor / skew、世界矩阵版本缓存的失效条件、toGlobal/toLocal、getBounds / getLocalBounds、
  sortableChildren / zIndex、addChildAt / swap / reparent、destroy 选项(children / texture / textureSource)与事件。
- 渲染树:visible / renderable / alpha / tint / blendMode 的继承与合批打断、culling(cullable / cullArea)、onRender、
  isRenderGroup / cacheAsTexture(若被用到)、setActive 与 visible 的分工。
- 合批与管线:纹理槽上限、混合模式(含非预乘纹理的混合变体)、roundPixels、模板遮罩嵌套 / 反向遮罩 / 精灵遮罩、
  颜色掩码、滤镜链(bounds / padding / resolution / antialias / clipToViewport / filterArea)、RT 渲染(clear / clearColor /
  transform / 嵌套 render)、generateTexture / extract、背景清屏。
- 资源:纹理上传(image / canvas / video / buffer、预乘、alphaMode、mipmap、更新 updateId / resourceId)、纹理与缓冲的释放
  (切场景后 GPU 资源不增长)、采样器(addressMode / scaleMode)、UniformGroup 的 UBO 布局与动态更新、Geometry 动态更新。
- 图形 / 文字 / 精灵:GraphicsContext 全部指令(含渐变、描边对齐、圆角、贝塞尔、洞)、Text(中文字体、换行、描边、投影、
  字距 / 行高、resolution、动态改字)、HTMLText、NineSlice、Mesh、Texture 的 frame / trim / rotate / defaultAnchor。
- 事件:hitTest / hitArea / eventMode / interactiveChildren / cursor / pointer capture / 拖拽 / 滚轮 / 多点、事件顺序与冒泡、
  坐标换算(resolution / autoDensity / 画布 CSS 缩放)。
- 应用:Ticker 优先级与 deltaTime / maxFPS / 暂停、ResizePlugin(resizeTo、0 尺寸)、Application.destroy、渲染抛错守护。

**B. RHI**
- 资源作用域与延迟释放(录制期销毁、跨帧)、只读 / 只写约束、readback、渲染图剔除与瞬时池、设备丢失、诊断上报、
  画布尺寸 / 零面积、MSAA(新)、管线就绪与 skippedDraws。

**C. 着色器移植(GLSL → WGSL)**
- 每个移植着色器逐分支对 master 的 GLSL:uniform 布局 / 默认值、纹理 y 朝向(WebGPU 第 0 行在上)、采样(Level / Grad /
  整数坐标)、精度 / 除零 / pow 负底 / NaN、宏开关与变体、铁律 0(光照一律世界空间)。重点是 render_parity 用例**没覆盖**的
  参数组合与分支。

**D. 游戏侧行为**
- 实体在场改 setActive:所有读在场的地方(交互、气泡、阴影、光照、VFX 锚点、排序、调试、存档无关)与 master 同义;
  隐藏期间动画 / onRender / 组件是否该停。
- Game.ts:揭幕前闸(管线就绪)、诊断、renderer.init 参数、切场景 / 销毁流程、冻结期 eventMode。
- 小游戏(水、糖画、纸扎、物件查看 / 接触 AO / 小虫)、演出(视差、过场叠层、渐变、燃烧、呼吸)、UI(滚动遮罩、按钮命中、
  输入框、Toast、书架 / 书页)、光照(时段外观、实体灯、probe、阴影、GI)、VFX(粒子 / 光柱 / 雷)。
- 性能:帧时间、每帧分配、draw 数、切场景后内存 / GPU 资源不涨。

## 审查记录

(按轮次追加:发现 → 对抗核实 → 修复提交 → 复审)

### 第一轮(2026-09-25,云端)· 发现 + 对抗核实完成,修复未做

**方法**:15 个分片审查员逐片对照 master(独立检出)与 Pixi 8.17 源码(`node_modules/pixi.js/lib`)。每条发现都交给
一个"复现"核实员,要求写出对照复现(engine2d 与 Pixi 并排,或走 NullRhiDevice);高危再加一个"反驳"核实员。
最后由完备性评审员列缺口。工作流是 `.claude/workflows/engine2d-migration-review.js`。
**结果**:共 45 条发现,确认 42 条(按同一根因去重后为 27 项),驳回 3 条,无存疑。

- 复现用例全在 `.claude/review-repros/engine2d-r1/`。跑法:`npx vitest run --config .claude/review-repros/vitest.config.mts [路径]`。
  这个目录被主配置排除,不进全量测试。
  ⚠ 各复现的写法不统一:有的写成"偏差存在就通过",有的写成"与 Pixi 一致才通过"。以用例里的断言为准。
- 每条的完整推理、复现路径和核实员给的修复建议都在 `.claude/review-repros/engine2d-r1/findings.json`。

| # | 严重度 | 游戏击中 | 问题 | 位置 | 复现 |
|---|---|---|---|---|---|
| D1 | 严重 | 是 | 纹理没有回收机制(Pixi 8.17 的 GCSystem 没移植)。FillGradient 这类没显式 destroy 的纹理常驻显存:面板开关、按钮悬停每次加一张;master 空闲约 60 秒回收 | `src/engine2d/gpu/GpuTextures.ts:42` | `.claude/review-repros/engine2d-r1/verify-api-usage-sweep-0/gradgc.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-graphics-0/gradientGc.test.ts` |
| D2 | 高 | 是 | mipmap 从不生成(autoGenerateMipmaps / mipLevelCount 无效)。HUD 三把火图集按约 1/4 缩放时细线闪烁,master 用三线性 mipmap | `src/engine2d/gpu/GpuTextures.ts:68` | `.claude/review-repros/engine2d-r1/verify-framebuilder-filters-masks-1/mip.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-renderer-resources-0/mip.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-rhi-0/mip.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-shader-uniforms-textures-0/mip.test.ts` |
| D3 | 中 | 是 | 几何 / 缓冲没有回收:不带 destroyBuffers 销毁或没销毁的几何,其 GPU 缓冲一直不释放(EntityShadow 每个带影实体 6 个;切场景、燃烧、呼吸会累积) | `src/engine2d/gpu/GpuBuffers.ts:24` | `.claude/review-repros/engine2d-r1/verify-api-usage-sweep-1/bufleak.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-renderer-resources-1/gc.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-shader-uniforms-textures-1/geomleak.test.ts` |
| D4 | 中 | 是 | Sprite.roundPixels 每帧现读;master 的 SpritePipe 在首次绘制时定值(玩家精灵在 master 上从不取整)。另:渲染器级 roundPixels 没并入合批元素(低) | `src/engine2d/sprite/Sprite.ts:141` | `.claude/review-repros/engine2d-r1/verify-collect-batch-1/round.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-collect-batch-3/roundpx.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-text-sprite-mesh-1/roundpixels_verify.test.ts` |
| D5 | 中 | 是 | 非 dynamic 的 RenderTexture 改尺寸后,精灵四边形按新尺寸画,但滤镜包围盒还停在旧尺寸(master 两者一致) | `src/engine2d/sprite/Sprite.ts:142` | `.claude/review-repros/engine2d-r1/verify-collect-batch-2/rtResize.test.ts` |
| D6 | 中 | 是 | 设备丢失后渲染永久停止;master 的 WebGL 会恢复上下文并重新上传 | `src/rendering/rhi/backends/luma/LumaRhiDevice.ts:1214` | `.claude/review-repros/engine2d-r1/verify-renderer-resources-2/deviceLost.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-rhi-2/deviceLost.test.ts` |
| D7 | 中 | 是 | 编译或校验失败的管线仍被拿去画,导致整帧命令缓冲作废(黑屏),而不是只丢这一次绘制 | `src/rendering/rhi/backends/luma/LumaRhiDevice.ts:811` | `.claude/review-repros/engine2d-r1/verify-rhi-1/failedPipelineDraw.test.ts (plus .claude/review-repros/engine2d-r1/verify-rhi-1/lumaDebugOff.test.ts)` |
| D8 | 低 | 是 | Culler 跳过未激活子树,NPC / 热点隐藏后 culled 标记停在旧值(master 的 visible=false 会重算成 false),每帧仍驱动着色 | `src/engine2d/culling/Culler.ts:49` | `.claude/review-repros/engine2d-r1/verify-events-app-assets-0/culler.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-game-integration-0/cullInactive.test.ts` |
| D9 | 低 | 是 | 滤镜区域包围盒把 setActive(false) 子树算进去,用的是它最后一次绘制的旧变换 | `src/engine2d/gpu/FrameBuilder.ts:951` | `.claude/review-repros/engine2d-r1/verify-framebuilder-filters-masks-0/inactive.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-hierarchy-entities-0/filterBounds.test.ts` |
| D10 | 低 | 是 | 纹理源每次重建 GPU 纹理都多挂一对 destroy / unload 监听,旧的不摘(按改尺寸次数泄漏) | `src/engine2d/gpu/GpuTextures.ts:77` | `.claude/review-repros/engine2d-r1/verify-api-usage-sweep-2/listeners.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-renderer-resources-3/listeners.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-rhi-3/listenerLeak.test.ts` |
| D11 | 低 | 是 | extract.* 返回去预乘的像素,master(Pixi WebGL)返回预乘值;extract.base64 的 jpg MIME 不对 | `src/engine2d/gpu/WebGPURenderer.ts:469` | `.claude/review-repros/engine2d-r1/verify-renderer-resources-4/extract.test.ts` |
| D12 | 低 | 是 | 每次绘制都新建 GPUBindGroup,并在 setBindings 里分配字符串 / Set(没有绑定组缓存,性能) | `src/rendering/rhi/backends/luma/LumaRhiDevice.ts:749` | `.claude/review-repros/engine2d-r1/verify-rhi-4/bindgroup.test.ts` |
| D13 | 低 | 是 | LumaRhiDevice.destroy 后画布上下文仍是已配置状态,Resize / Intersection 观察器还在跑 | `src/rendering/rhi/backends/luma/LumaRhiDevice.ts:1306` | `.claude/review-repros/engine2d-r1/verify-rhi-6/destroy.test.ts` |
| D14 | 低 | 否 | FilterEffect.priority 是 0(Pixi 是 1),同一容器上遮罩与滤镜的先后取决于赋值顺序 | `src/engine2d/scene/Container.ts:133` | `.claude/review-repros/engine2d-r1/verify-framebuilder-filters-masks-3/order.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-scene-transform-0/prio.test.ts` |
| D15 | 低 | 否 | destroy() 会复位遮罩的 includeInBuild / measurable;Pixi 保持遮罩隐藏 | `src/engine2d/scene/Container.ts:1383` | `.claude/review-repros/engine2d-r1/verify-scene-transform-1/maskDestroy.test.ts` |
| D16 | 低 | 否 | 无着色器的非合批 Mesh 在 isSimple 时用恒等变换,没走 textureMatrix.mapCoord | `src/engine2d/gpu/FrameBuilder.ts:504` | `.claude/review-repros/engine2d-r1/verify-collect-batch-5/meshTrim.test.ts` |
| D17 | 低 | 否 | 反向遮罩弹出后恢复成 INVERSE 模板模式;Pixi 恢复 MASK_ACTIVE | `src/engine2d/gpu/FrameBuilder.ts:645` | `.claude/review-repros/engine2d-r1/verify-framebuilder-filters-masks-2/inverse-pop.test.ts` |
| D18 | 低 | 否 | HTMLVideoElement 源从不上传(width===0 的判断读的是视频元素的布局宽度) | `src/engine2d/gpu/GpuTextures.ts:144` | `.claude/review-repros/engine2d-r1/verify-renderer-resources-5/video.test.ts` |
| D19 | 低 | 否 | 脱离场景树单独渲染的根节点,本分支会应用它自己的 blendMode / tint;Pixi 忽略根节点的混合,并沿用上一次的根顶点色 | `src/engine2d/gpu/collect.ts:44` | `.claude/review-repros/engine2d-r1/verify-collect-batch-4/rootblend.test.ts` |
| D20 | 低 | 否 | 精灵遮罩(以及数值 / 颜色遮罩)一律按模板遮罩处理 | `src/engine2d/gpu/collect.ts:210` | `.claude/review-repros/engine2d-r1/verify-framebuilder-filters-masks-4/spritemask.test.ts` |
| D21 | 低 | 否 | 几何属性格式默认 float32x2,不从 WGSL 的 @location 类型推断;stride 只累加着色器用到的属性 | `src/engine2d/shader/Geometry.ts:99` | `.claude/review-repros/engine2d-r1/verify-shader-uniforms-textures-2/geomFormat.test.ts` |
| D22 | 低 | 否 | Sprite / NineSlice 构造参数 anchor: 0 或 null 的行为与 Pixi 不同 | `src/engine2d/sprite/Sprite.ts:37` | `.claude/review-repros/engine2d-r1/verify-text-sprite-mesh-2/anchor.test.ts` |
| D23 | 低 | 否 | HTMLText 在生成中途文字又变了会再生成一次;master 的 Pixi 会丢掉这次改动(打字机字幕末尾字可能少)——需制作人定:对齐 master 还是登记为已知差异 | `src/engine2d/text/html/HTMLText.ts:142` | `.claude/review-repros/engine2d-r1/verify-api-usage-sweep-3/htmltext_midgen.test.ts` |
| D24 | 低 | 否 | EventEmitter.off(event, fn, null) 什么都删不掉(eventemitter3 把假值 context 当通配) | `src/engine2d/utils/EventEmitter.ts:45` | `.claude/review-repros/engine2d-r1/verify-api-usage-sweep-5/off-null-ctx.test.ts`<br>`.claude/review-repros/engine2d-r1/verify-events-app-assets-1/offnull.test.ts` |
| D25 | 低 | 否 | UnifiedCharacterShader 没有 WGSL 程序,会被静默地用缺省网格程序画(该路径已被 UNIFIED_CHAR_PATH_ENABLED=false 关闭) | `src/rendering/lighting/UnifiedCharacterShader.ts:599` | `.claude/review-repros/engine2d-r1/verify-shaders-lighting-0/glonly.test.ts` |
| D26 | 低 | 否 | uploadImage / createTexture 的 flipY 选项被静默忽略(luma 写死 flipY:false) | `src/rendering/rhi/backends/luma/LumaRhiDevice.ts:1167` | `.claude/review-repros/engine2d-r1/verify-rhi-5/flipy.test.ts` |
| D27 | 低 | 否 | NullRhiDevice 放行了几种 LumaRhiDevice 会拒绝的用法,单测能过的代码在真 GPU 上会丢帧 | `src/rendering/rhi/backends/null/NullRhiDevice.ts:221` | `.claude/review-repros/engine2d-r1/verify-rhi-7/nullFidelity.test.ts` |

**修复方向**(核实员建议摘要,修之前先读 findings.json 里的全文):

- **D1**:Port Pixi's texture GC with the same defaults, and restore the ImageSource flag on gradient sources.  (a) In src/engine2d/graphics/fill/FillGradient.ts:180,238, set `autoGarbageCollect: true` on the gradient's CanvasSource (or use a source subclass that sets it). This matches Pixi, where the gradient canvas lives in an ImageSource with autoGarbageCollect=true.  (b) In GpuTextures.get(), record a last-used time on every access: `e.lastUsed = performance.now()`. Add a `gc(now)` method to GpuTextures and call it from the renderer's post-render step, throttled to every 30s (gcFrequency). For each entry whose `source.autoGarbageCollect` is true and whose `now - lastUsed >= 60000` (gcMaxUnusedTime), call `source.unload()`. That emits 'unload', which reaches the existing `release()` listener: it calls onRelease, destroys the RHI texture and deletes the entry. Also remove the per-entry 'destroy'
- **D2**:Match Pixi 8.17 GpuTextureSystem behaviour:  1. **RHI.** Add `generateMipmaps(texture: RhiTexture): void` to RhiDevice.    - LumaRhiDevice: call `this.device.generateMipmapsWebGPU(handle)`. It needs RENDER_TARGET and SAMPLED usage and mipLevels > 1.    - NullRhiDevice: log `generate mips <label>` so tests can assert on it. 2. **Creation.** In GpuTextures.get, when a texture is being created (the `if (!e)` branch, lines 62-78): if `source.autoGenerateMipmaps` is set, do the following. The flag is checked at creation time, as Pixi does, which is also when HUD's pre-first-render assignment takes effect.    - Set `source.mipLevelCount = Math.floor(Math.log2(Math.max(source.pixelWidth, source.pixelHeight))) + 1`.    - Pass `mipLevels: source.mipLevelCount` to createTexture.    - Add `RhiTextureUsage.RENDER_TARGET` to the usage. 3. **After each upload.** In `get`, after `this.upload(...)`: if 
- **D3**:Recommended fix: match Pixi 8.17's buffer GC in GpuBuffers. - Add `lastUsed: number` to Entry and set it to the renderer's `gcNow` (performance.now() captured in prerender) in `get()`. - Add `autoGarbageCollect = true` to engine2d Buffer. - In WebGPURenderer postrender, every `gcFrequency` (30000 ms), call `buffers.gc(now, gcMaxUnusedTime = 60000)`. That call runs `e.buffer.destroy(); entries.delete(buf); buf.off('destroy', ...)` for entries where `now - e.lastUsed >= 60000 && buf.autoGarbageCollect`. - `get()` already re-creates and re-uploads a missing entry (new entry has `updateId -1`), so a collected buffer that is used again comes back transparently, as in Pixi. - Make the destroy listener removable (store the handler on the entry) so collected entries don't keep a closure on the Buffer. - Expose `gcActive`/`gcMaxUnusedTime`/`gcFrequency` renderer options with Pixi's defaults, and 
- **D4**:Latch the value per view the way Pixi's pipes do:  1. In engine2d `Sprite` (and likewise the batchables of `Mesh`, `NineSliceSprite`, `Text` and `HTMLText`), keep a private flag such as `_batchableInit = false`. 2. In `collectRenderables`, only when `!_batchableInit`, set `b.roundPixels = (renderer.roundPixels ? 1 : 0) | this._roundPixels` and set `_batchableInit = true`. Stop assigning `b.roundPixels` on later collects. 3. Reset `_batchableInit = false` wherever Pixi drops `_gpuData`: in `unload()` and in `destroy()`.  For exact parity on NineSlice/Text/HTMLText, which Pixi GC-manages, a reset after 60 s unused would also be needed. That is optional, since the game does not depend on it.  Add a regression test like the repro: render a sprite with `roundPixels = false`, set it to true, render again, and expect the round bit in the uploaded vertex data to stay 0.
- **D5**:For master parity, cache the quad bounds the way Pixi's SpritePipe does. Add a dedicated flag to Sprite: `private _visualBoundsDirty = true;`. Override `onViewUpdate() { super.onViewUpdate(); this._visualBoundsDirty = true; }`. Take care that the anchor callback, the texture setter and the constructor all go through `onViewUpdate`. In `collectRenderables`, replace `b.bounds = this.visualBounds;` with `if (this._visualBoundsDirty) { updateQuadBounds(this._visualBounds, this._anchor, this._texture); this._visualBoundsDirty = false; } b.bounds = this._visualBounds;`. Keep the public `visualBounds` getter recomputing in place on the same object, as Pixi's does. Do not key the refresh off `_didViewChangeTick`: Container also increments it for child and structure changes, which in Pixi do not refresh a sprite's visual bounds. Add a regression test on NullRhiDevice: resize a non-dynamic RT afte
- **D6**:Minimal fix that keeps parity with master: 1. **Listen for loss.** In engine2d's createRenderer (src/engine2d/gpu/createRenderer.ts), subscribe to `rhi.lost`. 2. **Recreate the device on the same canvas.** Call `luma.createDevice` again with the same createCanvasContext options. The simplest way is to make LumaRhiDevice able to rebind: swap in the new `luma` Device, clear `_isLost`, and recreate the swapchain, MSAA and depth targets. Alternatively, construct a new LumaRhiDevice and hand it to the WebGPURenderer. 3. **Add a contextChange-style reset in WebGPURenderer.** Drop every cached GPU object: per-TextureSource GPU textures and samplers, vertex, index and uniform buffers, pipelines, bind groups, and stencil or MSAA targets. Mark sources dirty so the next render re-uploads them from their CPU resources. This is what Pixi's runners.contextChange does, e.g. GlTextureSystem.mjs:53-67. 4
- **D7**:Keep a failed pipeline to its own draws, as in master. All changes are in src/rendering/rhi/backends/luma/LumaRhiDevice.ts.  (1) Add a `failed` flag to LumaRhiRenderPipeline and LumaRhiComputePipeline.  (2) Catch validation and internal errors even though luma has debug off. In `createShader` and `createRenderPipeline`, get the native device with `const gpu = (this.luma as unknown as {handle: GPUDevice}).handle`. Around the `this.luma.createShader(...)` call and around the `this.luma.createRenderPipeline(...)` call: - call `gpu.pushErrorScope('validation')` and `gpu.pushErrorScope('internal')`; - after the call, pop both scopes; - if either pop returns an error, set the pipeline's `failed = true`, reject `ready` with that error, and report it once through `report`. For the pipeline, feed these popErrorScope promises into `waitPipelineReady` instead of relying on `linkStatus`.  (3) When a
- **D8**:Recommended fix, game side: in Game.ts change line 10741 to `if (child.culled || !child.activeSelf) continue;` and line 10781 to `if (h.container.culled || !h.present) continue;`. This restores master's result, where a hidden entity is skipped by the shading drive, without changing engine2d.  Alternative, to make the engine2d Culler match Pixi's result for hidden nodes: in Culler._cullRecursive (src/engine2d/culling/Culler.ts:49) replace the bare return with `if (!container._activeSelf) { container.culled = !!(container.cullable && container.measurable && container.includeInBuild); return; }`. This mirrors Pixi, where a cullable invisible node gets empty bounds and so culled = true, and a non-cullable one gets culled = false.
- **D9**:src/engine2d/gpu/FrameBuilder.ts:951 — change the guard to `if (!c._activeSelf || c.localDisplayStatus !== 7 || !c.measurable) return;`. This matches collect.ts/getBounds pruning and never reads the stale groupTransform of inactive subtrees. With it, a setActive(false) subtree is excluded exactly as Pixi excludes a visible=false one. Add the repro (the texture size with setActive(false) should equal the visible=false result, 4x4) as a regression test next to src/engine2d/gpu/msaa.test.ts.
- **D10**:In GpuTextures, register the source listeners once per source instead of every time an entry is created. Add `private readonly hooked = new WeakSet<TextureSource>();` and replace lines 77-78 with:  ```ts if (!this.hooked.has(source)) {   this.hooked.add(source);   source.once('destroy', () => this.release(source));   source.on('unload', () => this.release(source)); } ```  The unload listener has to stay registered across cycles, because the entry is recreated on the next get(). TextureSource.destroy() already calls removeAllListeners() (TextureSource.ts:162), so nothing leaks after destroy. This keeps the listener count constant, as Pixi's managed-hash guard does. A regression test is to assert that listenerCount('unload') is still 1 after several render/unload and RenderTexture.resize/render cycles.
- **D11**:1. In WebGPURenderer.readPixels (src/engine2d/gpu/WebGPURenderer.ts:465-469), remove the `if (a > 0 && a < 255) { ... }` block. It should only swizzle BGRA to RGBA and copy the premultiplied bytes as-is, matching Pixi 8.17 WebGL getPixels. Update the doc comment, which currently says 未预乘, to say the output is premultiplied. 2. In createExtract's base64, apply Pixi's defaults and mapping: ```ts const IMAGE_TYPES = { png: 'image/png', jpg: 'image/jpeg', webp: 'image/webp' } as const; const format = opts?.format ?? 'png'; const quality = opts?.quality ?? 1; return c.toDataURL(IMAGE_TYPES[format], quality); ```
- **D12**:Minimal fix in LumaRenderPassEncoder.setBindings / the compute equivalent: 1. Pass `{ _bindGroupCacheKeys: { [group]: keyObj } }` to `this.pass.setBindings(...)`. 2. Get keyObj from a per-device interning cache keyed on the resolved resource identities in shaderLayout order. For buffers, include offset and size (the uniform arena offsets repeat frame to frame, so cross-frame hits are common). For textures, include the texture handle plus its current sampler handle, because `_toLumaBindings` mutates `tex.handle.setSampler`. 3. The interning cache can be a nested Map/WeakMap chain ending in a stable `{}` key object. luma stores bind groups in a WeakMap keyed by that object per bind-group layout. 4. Invalidate or drop entries when an RhiBuffer or RhiTexture is released: key through WeakMaps on the Luma resource wrapper, and create a fresh wrapper-level key object on destroy/recreate. 5. Sep
- **D13**:In LumaRhiDevice.destroy() (src/rendering/rhi/backends/luma/LumaRhiDevice.ts, just before `this.luma.destroy();`), tear down the canvas context. This call destroys the framebuffer and depth attachment, unconfigures the GPUCanvasContext, and stops the Resize/Intersection observers and the DPR matchMedia listener: ```ts try { this.luma.getDefaultCanvasContext().destroy(); } catch (e) { this.report(e, 'warning'); } this.luma.destroy(); ``` Use the report severity that fits the RHI diagnostics enum, or an empty catch. Add a regression test modelled on tmp/review/verify-rhi-6/destroy.test.ts. It should assert that after rhi.destroy() no ResizeObserver, IntersectionObserver or matchMedia listener is still live and that unconfigure() was called.
- **D14**:In src/engine2d/scene/Container.ts:133 change `priority = 0;` to `priority = 1;` in class FilterEffect, matching Pixi 8.17 FilterEffect.mjs:7. MaskEffect stays at 0, so addEffect's sort always places the mask outside the filters.
- **D15**:In src/engine2d/scene/Container.ts destroy() (line 1383), replace the `if (this._maskEffect) { this._maskEffect.reset(); this._maskEffect = null; }` block with just `this._maskEffect = null;`, which matches Pixi Container.mjs:1142. Keep effect.reset() in the mask setter (Container.ts:1243), which mirrors Pixi's returnMaskEffect → StencilMask.reset(). Add a regression test: after content.destroy(), maskG.includeInBuild and maskG.measurable stay false and parent.getLocalBounds() excludes maskG.
- **D16**:In src/engine2d/gpu/FrameBuilder.ts:504, always bind the texture matrix the way Pixi 8.17 GlMeshAdaptor/GpuMeshAdapter do: `uTextureMatrix: texture.textureMatrix.mapCoord`, and drop the `isSimple ? Matrix.IDENTITY :` shortcut. Also make sure `textureMatrix.update()` has run for the current texture before binding (Pixi's MeshPipe does this on texture update). Keep the `isSimple` check only in the batched UV path, where Pixi's BatchableMesh.uvs uses it. Add the repro above as a regression test.
- **D17**:In src/engine2d/gpu/FrameBuilder.ts:644-646, the popMaskEnd branch should always call `this.setStencilMode('active', maskStackIndex); this.colorMask = 15;`, matching Pixi's StencilMaskPipe.execute, where popMaskEnd has no inverse. Optionally drop `inverse` from the popMaskEnd instruction in collect.ts:231 and from its type in collect.ts:22. For setMask parity (Container.ts:1251-1254), store inverse on the container, like Pixi's `_maskOptions.inverse`, so it is set even when no mask exists yet and survives a mask swap. Then read it in collect.pushMask/popMask from the container instead of from the MaskEffect.
- **D18**:In GpuTextures.upload (src/engine2d/gpu/GpuTextures.ts:144), change the guard to use the intrinsic size: `if (!source.resourceWidth || !source.resourceHeight) return;` in place of `if (img.width === 0) return;`. This still skips unloaded images (naturalWidth is 0) and videos with no frame yet (videoWidth is 0, readyState < HAVE_CURRENT_DATA), and it lets loaded videos upload, matching Pixi's resourceWidth-based uploaders. As an option, also clamp the copy extent to min(texture size, resourceWidth/resourceHeight) the way Pixi's gpuUploadImageSource does.
- **D19**:In src/engine2d/gpu/collect.ts prepareTree, stop deriving the root's blend from its own localBlendMode. When the root has no parent, set `root.groupBlendMode = 'normal'`, which is Pixi's default for a detached render-group root. When the root has a parent, the closest match to Pixi is to leave root.groupBlendMode (and groupColorAlpha) as last computed by the parent tree's pass rather than overwriting them. Keep `root.groupColorAlpha = 0xffffffff` for a parentless root, which already matches Pixi's constructor default. Children must keep using the tempContainer semantics (normal/white) as they do now. Add a regression test: a detached Sprite with blendMode 'add' rendered as root must produce a batch with blendMode 'normal'.
- **D20**:Smallest change consistent with Pixi's mask selection: in the `set mask` setter (`src/engine2d/scene/Container.ts:1238`), fail loudly for the mask kinds engine2d does not implement instead of silently using stencil: - `if (typeof value === 'number') throw new Error('engine2d: ColorMask (numeric mask) not supported')` - `if (value instanceof Sprite) throw new Error('engine2d: AlphaMask (Sprite mask) not supported; use a Graphics mask')`  To avoid a circular import, check a duck-typed flag or the Sprite class via a late import.  A full implementation would port Pixi's `AlphaMaskPipe`: render the mask into a RenderTexture sized to the mask bounds and apply a MaskFilter through the filter stack, using the sprite texture's alpha (and channel). It would also need the `ColorMaskPipe` (per-channel color write mask). Neither is needed by the game today.
- **D21**:(a) In Geometry.addAttribute (Geometry.ts:90), keep `format: opt.format` and let it stay undefined. Adjust getSize() and the bounds code to cope with a missing format. (b) In Pipelines.layout, resolve each attribute's format as `attr.format ?? WGSL_TO_VERTEX_TYPES[program.attributes.find(a=>a.name===name).type] ?? 'float32'`, as Pixi's ensureAttributes does. Write it back to the attribute with `??=` so geometryVersion and getSize see it. (c) Compute the implicit stride per buffer over ALL geometry attributes that share that buffer, not only the ones the shader uses (Pixi's ensureStartAndStride). Keep building the RHI attribute list from the shader-declared subset only, because luma rejects attributes the shader does not use. For an attribute the shader lacks and whose format is undefined, Pixi only warns, so fall back to 'float32x2' for the stride sum or skip it with a warning.
- **D22**:In src/engine2d/sprite/Sprite.ts:38, change `if (anchor !== undefined) this.anchor = anchor;` to `if (anchor) this.anchor = anchor;` and leave the `else if (texture.defaultAnchor)` branch as it is. This matches the constructor in Pixi 8.17's Sprite.mjs:33-36.
- **D23**:Recommended: keep the divergence and record it as accepted. It is already documented in the header comment at src/engine2d/text/html/HTMLText.ts:5-6, and no current data exercises it. Strict Pixi 8.17 parity would reintroduce master's bug where the typewriter can freeze on truncated text. Only if bit-exact master behaviour is required: delete lines 142-143 of src/engine2d/text/html/HTMLText.ts (`// 生成期间文字 / 样式又变了...` and `if (gpuText.currentKey !== this.styleKey) this._didTextUpdate = true;`). Pixi's text change during generation is then dropped the same way.
- **D24**:In src/engine2d/utils/EventEmitter.ts:45, change the filter to eventemitter3's truthiness check: `const kept = list.filter((l) => l.fn !== fn || (once && !l.once) || (context && l.ctx !== context));`. Optionally, to fully match eventemitter3's addListener (`context || emitter`), change line 86 to `ctx: ctx || this`, so on(evt, fn, 0 or '' or false) binds `this` to the emitter as Pixi does. This does not change the internal emit->removeListener(once) path, because the stored l.ctx is always truthy after that change.
- **D25**:Smallest fix that keeps Pixi 8.17 semantics: in src/engine2d/gpu/FrameBuilder.ts drawCustom, use meshProgram only when `shader` is null. If a shader is present but its `gpuProgram` is missing, throw, the same way the filter path does at FrameBuilder.ts:744-745. For example: `if (shader && !shader.gpuProgram) throw new Error('[engine2d] Shader 没有 WGSL 程序(gpuProgram)');`. Pixi's WebGPU renderer also cannot run a glProgram-only shader, so a loud failure is the correct equivalent. Optionally, make UnifiedCharacterLighting.createShader return null (or throw) while UnifiedCharacterShader has no WGSL port. Porting UNIFIED_FRAG to WGSL is only needed if the path is ever re-enabled.
- **D26**:Smallest safe fix, since no caller uses flipY: in `LumaRhiDevice.uploadImage`, and so also for `createTexture` since it routes through `uploadImage`, reject the flag. Add `if (opts.flipY) throw new RhiError('unsupported', 'uploadImage: flipY 不受支持(luma WebGPU 固定 flipY:false)')` and stop forwarding `flipY` to `copyExternalImage`. Either do the same in `NullRhiDevice.uploadImage` so the null backend matches, or remove `flipY` from `RhiTextureDesc` and from the `uploadImage` opts type. If real support is ever needed, bypass luma: call `(device.handle as GPUDevice).queue.copyExternalImageToTexture({source: image, flipY: true}, {texture: t.handle.handle, premultipliedAlpha}, [w, h])` directly. The CPU-adapter fallback would still need its own flip, because luma's `_copyExternalImageData` ignores flipY too.
- **D27**:Test-only change, in src/rendering/rhi/backends/null/NullRhiDevice.ts: (a) In the render-pass encoder, track `let indexBuffer: NullBuffer | null = null`. In setIndexBuffer(b): if b is null, clear it. Otherwise call assertAlive, require b.usage & RhiBufferUsage.INDEX, and require b.indexFormat, throwing RhiError('invalid-usage') like Luma 760-770. In drawIndexed, after the pipeline check, run the same stream loop that draw uses (`for (const s of pipeline.streams) if (!streams.has(s)) throw ...`) and throw if indexBuffer is null. (b) Optionally add a `bindingsSet` flag: reset it in setPipeline, set it in setBindings, and throw in draw/drawIndexed when it is false and the pipeline declares bindings. This needs a bindings hint in NullRenderPipeline. It can be skipped if that is too heavy. (c) In createTexture, throw 'unsupported' when width or height > this.caps.maxTextureSize, and throw 'in

**判为"可接受 / 待定"的差异**(没进上表,第二轮要确认,或请制作人定):
- **局部包围盒缓存**:Pixi 在 addChildAt、设遮罩 / 滤镜、改 visible 时不更新局部包围盒缓存(会过期),本分支每次重新计算。
  场景变换随机对照里剩下的差异(例如 `scene-transform/fuzz.test.ts` 的种子 139)都属于这一类。
  本分支的值等于新算的正确值。建议登记为已知差异,或在第二轮逐条确认游戏不依赖旧值。
- **颜色解析**:`hsl()` 字符串、带透明度的 4 / 8 位十六进制(Pixi 用 colord 把透明度舍到两位小数)、大于 0xFFFFFF 或负数、
  null(Pixi 会抛错),本分支都与 Pixi 不同。游戏和 `public/` 下的 JSON 都没用到,见 `graphics/color.test.ts`。
- **模糊滤镜 legacy 那条复现失败**:原因是 Pixi 在 node 里生成 GL 程序要用 `document`,属于测试环境问题,不是偏差。
- **被驳回的 3 条**:
  - 物件查看的颜色 / 透明度被乘两次;
  - Text 从舞台移除而不 destroy 时纹理不释放。这一条被驳回,但归到 D1 的回收机制里一起处理;
  - 渲染不可见根节点时不清目标。

### 第二轮要补的(完备性评审员汇总,去掉了误报)

每个评审员只看到自己那批分片的覆盖说明,所以"事件 / 文字 / 特效没人审"这类判断不成立:15 片都有逐行对照记录。
下面是真正的缺口,按优先级排列:

1. **资源回收全类排查**(D1 / D3 是其中两例)。Pixi 8.17 的 GCSystem 默认开,空闲 60 秒回收,管的对象有:纹理、缓冲、几何、
   Canvas / HTML 文字、GraphicsContext、NineSlice。逐类确认游戏是显式销毁,还是靠 master 的自动回收;
   再用 A/B 工具实测:切场景 N 次、开关面板 N 次、空闲超过 60 秒,对比 GPU 资源数与内存。
2. **遮罩 × 滤镜 × RT 的真实组合**(物件查看小游戏)。
   - 结构:`viewLayer.mask` 下挂 `objectRoot.filters=[contactAo]` 和 `bgSprite.filters=[ColorMatrix, Blur]`;
     contactAo 在自定义 apply 里把 RT 做 legacy blur 输出到另一张 RT,再用 4 张 RT 合成;`clearMask` 取代了 master 的 renderTarget.bind 清屏。
   - 要查:滤镜结束后回到父 pass 时,模板是 load 且参考值正确;中途首次加模板的目标;池化纹理复用时带着旧的模板状态。
   - 补一个对照用例。
3. **采样器快照语义**。master 的 GLSL 总是用纹理源自己当前的样式采样,本分支 `*Sampler` 资源是当时拍下的副本。
   纹理样式在着色器建好之后才改的点(HUD.ts:1162、backgroundSway.ts:368、UITextures.ts:41,还有 114 处 samplerOf)要逐一确认。
4. **UniformGroup 与 WGSL 结构体布局**。特效和光照已核对;其余着色器(contactAo、shadowPrefix uLightPx 这类 f32 / vec2 数组)要补。
5. **WebGPU 设备限制**。`createDevice` 没要 `requiredLimits`,按默认限制跑:单张 2D 纹理最大 8192(master 的 WebGL 一般 16384),
   每阶段 16 张采样纹理,uniform 绑定 64 KiB,没有 float32-filterable。
   ⚠ 要用真实素材核对背景和各类 RT 的尺寸,超限会在 createTexture 抛错。这条只能在本地有素材时查。
6. **RT 里的 roundPixels**。master 在 WebGL 下给 RT 用翻转投影,半像素取整方向和本分支相反,可能让整张精灵偏 1 像素
   (WorldFilterPipeline、热点滤镜链下的 SpriteEntity / Hotspot)。要补对照用例。
7. **真实图片解码上传**。经 Assets / createImageBitmap 走的真实 PNG(含半透明图、`*.normal.png` 预乘)上传后的字节要和 master 一致。
   按不变量,法线图 r 均值应为 0.500 而不是 0.749。只能在本地有素材时查。
8. **中文排版**。用游戏真实的 TextStyle 和 `public/**/*.json` 里的中文文本(对话、档案、字幕),对 Pixi 8.17 逐行比较换行、宽度、高度。
9. **HTMLText 生成中途改字**(D23),请制作人定:对齐 master,还是登记为已知差异。
