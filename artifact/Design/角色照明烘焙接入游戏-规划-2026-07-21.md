# 角色照明烘焙接入游戏 — 规划(2026-07-21)

> 前置:实验室已转正 `tools/character_lighting_lab/`(理论文档 A1–A9 见 artifact,
> 工具 README 有全部机制)。本文只回答一件事:**实验数据怎么变成游戏里的角色光照**。
> 状态:规划稿,未动工。动工前按 §0 走 feature-iteration(不改玩法规则,改呈现)。

## 一、现状与接缝(为什么不是从零接)

游戏里已存在三块相关设施,决定了接入姿势:

1. **光环境系统**:`SceneLightEnv`(静态) + `lightEnvCurve.ts`(手调折线:玩家位置
   → 弧长 t → 插值光环境)→ `resolveLightEnv` → shadowField / 实体阴影逐帧消费。
   **接缝就在这**:probe 烘焙是"换生产端"——同样输出 ResolvedLightEnv 语义的量,
   消费端(阴影、着色)不动。手调曲线降级为无烘焙场景的回落项。
2. **深度已在船上**:场景媒体自带 `raw_depth_rg.png`,`DepthOcclusionFilter` 已消费。
   烘焙管线应改为**读写这份正典深度**,不再私养缓存(单一深度源,防漂移)。
3. **坐标桥是现成的**:游戏实体位置(scene worldWidth 单位)↔ 背景像素的映射
   SceneManager 已有;实验室 work-res(512宽)只是同一张图的缩放。
   桥 = 实体世界坐标 → 背景像素 → ×(512/native) → 地面深度场 d_walk → q → M → 伪世界
   → probe 三线性。全程每帧每实体几十次乘加,CPU 白给。

## 二、目标形态(一句话)

> 每个进入场景的角色/NPC,每帧从该场景的 **L2 probe 烘焙**里取一份辐照度,
> 以 **整体 tint(L0)** 起步、**片元滤镜(带法线)** 进阶,替代手调光环境曲线;
> 无烘焙的场景自动回落旧曲线,玩家无感切换。

## 三、运行时载荷(每场景,随场景媒体加载)

放 `public/resources/runtime/scenes/<id>/lighting/`,由实验室新增 `--export-runtime` 产出:

| 文件 | 内容 | 尺寸估计 |
|---|---|---|
| `lighting.json` | 标定(M/ppu/cx/cy/theta)+ probe 网格 + ambient SH + **背景内容哈希** + 版本号 | ~4KB |
| `probes_l2.bin` | (P,9,4) f16:E_hit rgb + 覆盖率(维持三件套语义,运行时闭包可调) | ~120KB |
| `probes_l2amb.bin` | miss 环境拆分项 | ~90KB |
| `ground_d.png` | d_walk 地面深度场,RG16 编码,work-res | ~300KB |

合计 ~0.5MB/场景,30 场景 ~15MB(走 DVC/OSS 同现有媒体)。
**不进游戏**:体素、mesh、点云、BIN/L1 cache、walkable(游戏有自己的 collision)、RT。

**防腐门(A2 教训,必须做)**:`lighting.json` 携带 background.png 内容哈希;
运行时不符 → 禁用烘焙 + console warn;validator 升 error。杜绝"背景重画烘焙静默过期"。

## 四、运行时系统

新建 `src/systems/CharacterLightingSystem.ts`,实现 IGameSystem,构造函数注入,
EventBus 听 `scene:changed` 载/卸载荷(§1 八条对照:分层✓ 解耦✓ DI✓ 数据驱动✓
统一生命周期✓;serialize/deserialize 无状态直通)。

对外只有一个查询面:
```ts
sampleE(worldX, worldY, opts?) → {rgb: E, cov} | null   // null = 本场景无烘焙
```
内部:坐标桥 + 8 probe 三线性 + 闭包(E_hit + s·E_amb,s 沿用实验默认)。

**消费端两阶段**:
- **阶段A·tint(先上线)**:每实体每帧 1 次 sampleE(胸口高度)→ 折算成 tint 色乘到
  sprite(显示定标:场景锚点处 tint≈白,只表达相对偏移——A1 的补救,防"角色变泥")。
  覆盖玩家 + 全部 NPC,零 shader 工作量。
- **阶段B·片元滤镜(验收后)**:玩家(可选主要 NPC)换 Pixi Filter,片元级
  probe 插值 + 隆起法线(char/normal.png 管线化),身体出现受光梯度。
  实验室 CHAR_FS 裁剪移植,只保留 L2 路径。

**与旧系统的优先级**:场景有烘焙 → 本系统接管 tint/光环境语义;无 → lightEnvCurve
原样生效。禁止两者同时乘(双重记账,同 A8 纪律)。

## 五、内容工作流(编辑器往返红线)

- 烘焙参数(pitch/ppu/relief/EV/max_gain_ev/fold/probe 网格)= 内容,存
  `lighting.json` 的 params 块;**实验室查看器就是这个域的编辑器**
  (同 scene_depth_editor 先例):调参 → 页内重建 → `--export-runtime`。
- 策划流程:新场景背景定稿 → `./dev.sh char-lighting -- --build ... --export-runtime`
  → 进游戏即生效。默认参数已可全自动,调参是可选项。
- validator 新增三查:载荷齐全性 / 哈希一致 / json schema。素材审计登记新文件类型。

## 六、分期与验收门

| 期 | 内容 | 验收门 |
|---|---|---|
| P0 决策对齐 | ①旧光环境曲线的处置(回落 or 废弃);②tint 显示定标锚点方案(场景中性点自动 or 每场景一标);③白天室外要不要先补 hero 太阳(A8)还是先只上阴天/夜/室内 | 拍板即过 |
| P1 数据通道 | pipeline `--export-runtime` + 正典深度统一 + 哈希门 + validator/审计 | validate-data 绿;temple+雾津街头两场景载荷落位 |
| P2 运行时 MVP | CharacterLightingSystem + 阶段A tint + debug 面板开关(烘焙/曲线/关 三态对比) | tsc 绿;headless-runner 真跑:角色走近烛火 tint 变暖、进暗巷变暗、无烘焙场景回落无异常 |
| P3 灰度验收 | 两个试点场景制作人定性验收(融入>准确);β/锚点微调 | 制作人过目 |
| P4 扩面+进阶 | 批量烘焙全部场景;阶段B 片元滤镜(玩家);按需 hero 光 | 逐项独立验收 |

每期收尾照惯例:`npx tsc --noEmit` + 局部回归 review;P2 起 headless-runner 必跑。

## 七、风险与明确不做

**风险**:①与透视缩放系统的叠加顺序(tint 与 scale 无冲突,滤镜阶段注意 filter 链顺序);
②尺度感不匹配(ppu 是拍板参数,试点期用查看器现场校);③31 场景批烘的个别翻车
(标定失败的场景先回落曲线,不阻塞)。

**明确不做(本轮)**:游戏内 RT/BIN/L1(实验室专用)、SH 升阶、mesh 进游戏、
实验室碰撞替换游戏碰撞、3D 检视进编辑器、每帧动态重烘。

## 八、动工暗号

P0 三个决策点拍板后,从 P1 开始;每期独立可交付,可随时停在任一期末。

## 九、进展记录

**2026-07-22 · P1 完成(数据通道全绿)**:
- `pipeline.export_runtime`:实验室烘焙 → `public/resources/runtime/scenes/<id>/lighting/`
  (lighting.json + L2 三分账 bin + ground_d.png RG16,~430KB/场景);
  CLI `--export-runtime` + 查看器「⇪ 导出到游戏」按钮 + `/api/export` 端点。
- 防腐门:validator 新增 `_lighting_payload_issues`(结构/文件尺寸/背景哈希),
  哈希以**游戏侧背景**为准,导出前做像素级一致校验(实验室副本是 PIL 重编码,
  字节哈希天然不同——已踩过一次假红)。正反向已测。
- 载荷相对原规划的增强:含 **SAM3 门控后的光源列表**(NEE surfels)——P0 决策③
  的"hero 灯"数据面已白送,运行时可选择消费。
- 试点 temple + 雾津街头 已导出;三件套收尾门全绿(830 editor tests / 审计 0 / validate 0 err)。
- 下一步 = P2(CharacterLightingSystem + tint MVP),**等 P0 三决策拍板**。

**2026-07-22 · P0 拍板 + P2 运行时 MVP 完成(真机验证通过)**:
- P0 决策:①旧光环境曲线**保留作回落**(无载荷/哈希过期场景走旧 tone);②tint 锚点
  =自动中值 refLuma(手动锚点工具后补,见 TODO);③太阳=F2 可开关+方向可设的
  独立解析直射光,**与投影阴影方位角互不耦合**;④tone 通道=**切换不叠加**
  (`uBakedMode>0.5` 走烘焙 tint,否则走旧 tone);⑤实验室接触阴影**禁止进游戏**,
  阴影/AO/遮挡与本系统正交不动。
- 落点:`src/core/CharacterLightingSystem.ts`(IGameSystem;载荷加载含 SHA-1 哈希门
  +SPA 兜底形状检查;L2 probe base+nee 合账三线性采样;地面 RG16 双线性;太阳按
  仰角/方位余弦入账;tint=E/refLuma 锚定,clamp [0.35,1.8]);
  `EntityLightingFilter.setBakedTint`(uBakedTint/uBakedMode);Game 每帧对玩家+
  entityLayer 深度滤镜 `applyBakedTint`;F2「角色照明(烘焙)」区(开关/强度/太阳
  开关/日方位/日仰角滑条,noRefresh+原地 sync,只改运行时)。
- 真机验证(temple,页内探针+瞬移):载荷激活 1680 probe/3 光源/refLuma 0.038;
  tint 空间方差实测 0.35→1.76(烛火边暖橙、暗角压到下限),香炉边 vs 右侧暗区
  截图对比明显;逐帧 uniform 写入与 sampleTint 精确一致;太阳背向=零贡献、
  正对=有贡献、仰角改变余弦——方向解耦成立;无载荷场景(teahouse/dev_room)
  回落无异常。收尾门:tsc 绿 / npm test 绿 / validate-data 0 err / 审计 0。
- 已知坑(验证方法学):嵌入式浏览器隐藏 pane 时 rAF 停,游戏 update 不跑——
  页内读 uniform 会拿到旧帧值,须先截图逼帧再同步读;F2 状态行只在按钮/滑条
  回调 sync,绕过按钮直写系统字段会显示脱节(非 bug)。
- TODO(后续):①手动锚点工具(游戏里走到点位一键钉锚,写回 lighting.json);
  ②F2 RT 对比模式(dev-only,可选);③其余场景用户自迁+验收;④验收通过后
  给旧 scene_depth_editor 打废弃标记。

**2026-07-22 · P2c 完成:实验室 CHAR_FS 逐像素搬进游戏(P2 tint 模型被推翻重做)**:
- 用户拍板推翻 P2 的 tint 归一化模型(E/refLuma+clamp 是自创,非实验室算法):
  **游戏所有 sprite=albedo,着色=albedo×E/π×β×增益,与查看器逐像素同式**;
  实验室保持现状只作预览基准,职责=对齐预览与运行时;旧滤镜的 tone/keylight 淘汰,
  **保留**:接触 AO/投影阴影/光环境曲线(无载荷场景回落)。
- 数据通道 v2(pipeline.export_runtime 增量,烘焙/查看器零触碰):atlas_l1|l2|bin.bin
  (查看器 atlas4 同布局四分账列块)+ probes_valid + vol_rad|vol_emit(体素卷
  Z 切片平铺 2D 图集)+ ground_d,~25MB/场景;validator 门升 v2;temple+雾津街头已导。
- 运行时:`src/rendering/CharacterShadingFilter.ts`(CHAR_FS 全量移植:L1/L2/BIN
  probe 三线性四分账、RT gather+A7 折叠+miss 双策略、NEE 解析直射+阴影 march、
  鼓包法线+压平、β/增益曝光、太阳独立方向加项;遮挡沿用 P2a depthConfig 契约;
  sampler3D→平铺图集手写三线性数学等价);`spriteNormalAtlas.ts`(法线零贴图:
  按 stage_character 同数学 EDT+高斯+梯度对图集逐格现算,TextureSource uid 缓存);
  CharacterLightingSystem 重写为载荷/纹理管理器+逐帧驱动(footQ=ground_d 双线性,
  quad 尺寸随实体实际显示尺寸含透视缩放);Game 挂接玩家+NPC,载荷异步就绪
  onReady 重挂,F2 开关热切换新旧滤镜。
- F2「角色照明(烘焙)」全参数:模式 RT/L1/L2/BIN、NEE、miss不计入、折叠、法线显示、
  太阳开关,β/增益/miss强度/隆起/压平/spp/步长/步数/日方位/仰角/日强度滑条。
- 真机验证(temple):v2 载荷激活(1680 probes/3 lights/vol 192×107×64);玩家+NPC
  暗调物理着色+烛火侧暖边(实验室观感,与 P2 tint 满亮度完全不同);法线显示=橄榄色
  鼓包渐变(实验室一致);RT 模式渲染正常且≈L2;着色开关双向热切换;无载荷场景回落。
  四门全绿(tsc/npm test 40+1 文件/validate 0 err/审计 0)。
- 坑(已修):Pixi GlProgram 靠 fragment 源里含 `#version 300 es` 字样才走 GLSL3
  编译——不写就按 GLSL1 编,texelFetch/位运算/数组初始化全炸且只在运行时报错。
  新滤镜 shader 必须显式带版本头+`precision highp`。
- 补充拍板(同日):**非 bake 着色参数=场景配置**——「⇪ 导出照明」把查看器面板
  当前 11 个运行时参数(mode/spp/step/msteps/fold/miss_mode/nee/beta/amb/
  bulge/flatten)写入 lighting.json `shading` 块;游戏加载即用之初始化,F2 打开
  见到的就是场景配置值,F2 改动纯测试、场景重载回配置。CLI 导出走 SHADING_DEFAULTS
  (mode=L2);validator 必含 shading;真机验通(手改 json 四值全被接管)。
  太阳为游戏侧参数不入载荷。
- 再拍板(同日):**pgain(预览亮度)永不出实验室**——它是实验室显示增益
  (背景+人同乘,人:背景比例不变),游戏背景是原画不动,乘到角色上就破坏比例。
  游戏角色曝光只认 β;uPGain 从滤镜/params/F2 全部摘除,SHADING_DEFAULTS 与
  查看器导出参数均不含 pgain。各场景亮度=实验室调 β 后导出。已重导两场景,
  真机验通(params 无 pgain、F2 无增益滑条、β 曝光正常),四门全绿。

**2026-07-22 · P3 完成:光源驱动阴影(方向/多影/过渡/半影全自动)**:
- 原理:影子无"手调方向"概念,方向/浓度/软度/长度全为光源表(载荷 lights,
  SAM3 门控 surfel:世界坐标+radiance+面积)的函数;mask 的积分在烘焙端已收敛
  为矩(质心/面积),运行时零积分、每帧每灯 O(1)。
- resolver(`CharacterLightingSystem.resolveShadowLights`):脚点→q(ground_d 同源)
  →逐灯 方位/仰角(q→M-world 经 depthConfig R 基,Game 于 rebuild 注入)、
  照度份额 w=lum·area/D²、角尺寸 tanα=√(A/π)/D;环境稀释 w_amb=ambScale×lum(L0);
  太阳=无穷远光源并入同一分母(白天自动压制烛影)。份额<0.02 或超 K 不占槽。
- 多影+过渡(Game.driveEntryShadows):每实体 K 槽(默认3,主槽兼接触斑),
  **槽按光源身份绑定**防权重交叉时方向对调;时间低通(τ=150ms,方位最短弧);
  过渡=份额归一化的物理交叉淡化,无插值特技。手调模式/无载荷场景原路回落。
- 半影:DeferredEntityShadow 的锥采样即面光源几何——softRadius=tanα、
  softSamples≥6,脚下锐/影尾糊/本影截断全自动;低仰角(地面烛火)时包围盒
  按 0.9/tanθ 自适应拉长(封顶3.5)免截断,仰角钳 [12°,80°]。
- **权重模型返工(用户纠错"光源功率应是环境巅峰")**:初版用 surfel 功率算份额
  是记账错误——烛火往场景倒的能量大头在画作光晕(base 账),surfel 只是火苗内核
  的 emit 增量,份额算出 2% 荒谬值,ambScale=0.15 是错上补丁。重做为**能流模型**:
  主光方向/强度 = probe E 场 L1 能流向量(sampleFluxLum:三线性 L0..L3 亮度,
  含 base+amb+nee 全账);方向性 δ=0.488|L1|/(0.282·L0);surfel 与能流同向
  (<35°)时合并、继承其锐 tanα;稀释=(1−δ)×本地 L0,ambScale 回归 1.0 诚实
  单位。temple 实测烛边主光份额 0.21→0.39。
- **验收方法学(用户抓的第二个错)**:数值活着≠视觉存在。白底渲染阴影层
  (extract.canvas + getLocalBounds 局部系)看形状才算数;由此发现并修掉两个
  埋没影子的老问题:①接触斑半径 w×0.65(148 宽 sprite → 直径 190wu 巨晕,
  暗地上读作"无 AO")→ 0.28 贴脚;②softness=1 的整层 BlurFilter 把细影糊没
  → auto 模式钳 0.35(半影职责已归锥采样)。浓度=darkness×√份额(感知压缩);
  仰角钳 [25°,80°](12° 时影子摊成超长薄条不可读)。
- **影子形状红线(用户第三轮纠错后定案)**:最清晰的影子=角色 mask 剪影经光向
  剪切变形(脚边钉住、头边偏移),模糊在剪影上做——deferred 逐像素与重建面求交
  会把形状啃烂,**auto 模式 K 槽一律 PlanarEntityShadow**(entry.shadow 熄灭;
  planar 的 angleRad 是屏幕系,resolver 增 screenAngleDeg=M-world 水平光向取反经
  R^T 投屏)。planar 接触椭圆 1.3/0.6→0.55/0.26 贴脚;cast 剪影 UV 改片元反解
  平行四边形(标量 uniform);EntityShadow.setU 补 UniformGroup.update()。
- **⚠extract 取证陷阱(本轮误诊三次的根因,入库防再犯)**:extract.canvas 对共享
  GlProgram 的 mesh 离线重画会拿到串台/陈旧 uniform(cast 借 contact 的 →剪影
  渲成整图集条纹)——纯取证假象,真实合成帧一直正确。**阴影/网格类验收只信真实
  帧截图**;此前"deferred 形状全烂/aUV 缓冲不同步"的部分证据来自该假象。
- 修后真机(真实帧):连贯的剪切人形剪影,方向随位置背光翻转,贴脚接触椭圆,
  剪影上模糊;太阳开启份额 1.0 独占;四门全绿。F2 角色照明区新增:影子跟灯开关+
  影子槽K/环境稀释/影子平滑滑条。
- 明确不做(需另拍板):二阶矩椭圆光源(要动 stage_lights=实验室冻结区)、
  手摆画外光源(实验室编辑器扩展)、角色间互影。
