---
target: entity-refactor-engine
date: 2026-08-10
session: 过场校验清单（顺带修复）
---

现象: validator 手抄了第二份 actor 引用清单（`actor_actions` 四项），与登记面
`ENTITY_REF_PARAMS` 漂开——jumpEntityTo/attachToSocket/detachFromSocket/cameraFollowActor
的 target 悬垂时一声不吭（运行时那步静默跳过，校验全绿）；真实数据现存一例：
过场「赌坊输钱」jumpEntityTo target='赌坊看门人'。
证据: 已改为 `_ref_keys_by_kind()` 直读 ENTITY_REF_PARAMS（actor / emote_subject /
bubble_speaker 三档），手抄清单删除；语义级 parity 护栏
tools/editor/tests/test_action_manifest_parity.py::test_every_registered_bare_entity_ref_param_is_actually_validated
（喂悬垂 id 必须报，反向验证过：退回旧清单立刻红）。全项目仅 +1 warning。
建议: 卡里点明「actor 类引用只有 ENTITY_REF_PARAMS 一个真相源，validator 读它、不得另抄」，
并记下 npc_soft 刻意豁免（软引用命中不了就当显示名，报不报由调用点定）。
